# Clippy 配置：这里只保留当前版本支持的规则项。
disallowed-methods = [
    { path = "Option::unwrap", reason = "请改用 ? 或显式处理错误" },
    { path = "Result::unwrap", reason = "请改用 ? 或显式处理错误" },
    { path = "Result::expect", reason = "请改用 ? 并补充上下文" },
]
