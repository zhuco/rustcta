# 交易所网关补全任务清单

状态日期：2026-06-09

本文件把已收集的数据转成后续可执行任务。机器可读版本见 [交易所网关补全任务清单.csv](交易所网关补全任务清单.csv)。

如果要按单个交易所开工，先看 [adapter 工作包索引](adapter工作包索引.md)，它会合并当前实现、明确任务和剩余核验项。

## 数据来源

- 当前项目实现证据：[交易所功能盘点矩阵](交易所功能盘点矩阵.md)。
- 官方能力结论：[官方能力核验台账](官方能力核验台账.md)。
- 产品线核验：[产品线官方核验 P0 主流交易所](产品线官方核验_P0_主流交易所.md)。
- 链上合约产品线核验：[产品线官方核验 P1 链上合约交易所](产品线官方核验_P1_链上合约交易所.md)。
- 衍生品产品线核验：[产品线官方核验 P2 衍生品交易所](产品线官方核验_P2_衍生品交易所.md)。
- CEX 合约边界核验：[产品线官方核验 P3 CEX 合约边界](产品线官方核验_P3_CEX合约边界.md)。
- 区域现货交易所核验：[产品线官方核验 P4 区域现货交易所](产品线官方核验_P4_区域现货交易所.md)。
- 区域现货 CEX 第二批核验：[产品线官方核验 P5 区域现货 CEX 第二批](产品线官方核验_P5_区域现货_CEX第二批.md)。
- 剩余区域现货 CEX 核验：[产品线官方核验 P6 剩余区域现货 CEX](产品线官方核验_P6_剩余区域现货_CEX.md)。
- WebSocket 细项：[P0 第一批](WebSocket官方核验_P0_第一批.md)、[P0 第二批](WebSocket官方核验_P0_第二批.md)、[P1 第三批](WebSocket官方核验_P1_第三批.md)、[P2 区域现货](WebSocket官方核验_P2_区域现货交易所.md)、[P3 P2 公共 WS 缺口](WebSocket官方核验_P3_P2公共WS缺口交易所.md)、[P4 CEX 盘口细项](WebSocket官方核验_P4_CEX盘口细项.md)、[P5 衍生品/链上盘口细项](WebSocket官方核验_P5_衍生品链上盘口细项.md)、[P6 补充交易所盘口细项](WebSocket官方核验_P6_补充交易所盘口细项.md)、[P7 补充交易所盘口细项二](WebSocket官方核验_P7_补充交易所盘口细项二.md)、[P8 补充交易所盘口细项三](WebSocket官方核验_P8_补充交易所盘口细项三.md)、[P9 剩余交易所盘口细项](WebSocket官方核验_P9_剩余交易所盘口细项.md)。
- 核心交易接口核验：[核心交易官方核验 P0 第一批](核心交易官方核验_P0_第一批.md)、[P1 第二批](核心交易官方核验_P1_第二批.md)、[P2 第三批](核心交易官方核验_P2_第三批.md)、[P3 第四批](核心交易官方核验_P3_第四批.md)。
- 仓位接口核验：[仓位接口官方核验 P0 第一批](仓位接口官方核验_P0_第一批.md)、[P1 第二批](仓位接口官方核验_P1_第二批.md)。
- 账户/余额接口核验：[账户接口官方核验 P0 第一批](账户接口官方核验_P0_第一批.md)、[P1 第二批](账户接口官方核验_P1_第二批.md)。
- 费率接口核验：[费率接口官方核验 P0 第一批](费率接口官方核验_P0_第一批.md)、[P1 第二批](费率接口官方核验_P1_第二批.md)、[P2 第三批](费率接口官方核验_P2_第三批.md)。
- 高级订单能力核验：[高级订单能力官方核验 P0 第一批](高级订单能力官方核验_P0_第一批.md)、[P1 第二批](高级订单能力官方核验_P1_第二批.md)、[P2 第三批](高级订单能力官方核验_P2_第三批.md)、[P3 第四批](高级订单能力官方核验_P3_第四批.md)、[P4 第五批](高级订单能力官方核验_P4_第五批.md)、[P5 第六批](高级订单能力官方核验_P5_第六批.md)、[P6 第七批](高级订单能力官方核验_P6_第七批.md)。

## 当前任务分布

| 优先级 | 类型 | 数量 | 说明 |
| --- | --- | ---: | --- |
| P0 | 公共订单簿 WebSocket | 0 | 已完成：P0 公共盘口候选全部补齐 channel/interval/depth/sequence/resync 边界；`binanceus` Spot 和 `binancecoinm` COIN-M public WS spec 已补入。 |
| P0 | 产品线项目未实现 | 4 | 官方支持细分合约/期权产品线但当前项目未接；OKX Futures/Options 与 MEXC Contract/Perpetual 已于 2026-06-09 进入当前 adapter，MEXC delivery-state contract 仍按独立产品线缺口计入本段。 |
| P1 | 已有 WS 的结构化细项 | 0 | 已完成：P1 已有 WS 结构化细项全部补齐 channel/interval/depth/sequence/checksum/resync 边界。 |
| P2 | 产品线项目未实现 | 34 | 官方有 spot/OpenBook/合约/margin/leveraged/perps 线索，但当前 adapter 未接或需要独立 profile；`kucoin` 合约产品线已委托 `kucoinfutures`，不计入项目未实现。 |
| P2 | 明确交易所不支持 | 55 | 已核验可写 `交易所不支持现货` 或 `交易所不支持合约`。 |
| P2 | 公共订单簿 WebSocket | 0 | 已完成：P2 公共订单簿 WS 候选全部补齐结构化字段或明确 runtime 边界。 |
| P2 | 已有 WS 的结构化细项 | 0 | 已完成：已有 payload/parser 证据的 WS 结构化字段已补齐。 |
| P2 | 明确公共 WS 不支持 | 17 | 已核验可写 `交易所不支持公共 WS 行情` 或 profile 不承接公共 WS runtime；矩阵中公共 WS unsupported 边界已全部落地。 |
| P2 | 核心交易项目未实现/未启用 | 44 | Core-A/Core-B 传统/区域 CEX private REST 组和 Core-C 链上/session/特殊签名组仍有 operation 停留在 `离线` request-spec/source 边界；缺 credential/profile guard、signer/session、parser、reconciliation/dry-run guard 时不启用 live write。 |
| P2 | 核心交易当前交易所不支持 | 18 | 已核验当前交易/私有接口 runtime 不应接入，或 shared request 缺必要 side/context，不能无损映射。 |
| P2 | 仓位项目未实现/未启用 | 15 | 官方支持仓位/保证金/账户持仓读取，已补 request-spec/source 离线边界；剩 shared runtime、parser、签名/会话或链上账户扫描未启用。 |
| P2 | 仓位当前交易所不支持 | 4 | 当前官方资料或 profile 不支持稳定仓位接口 runtime。 |
| P3 | 账户/余额项目未实现/未启用 | 45 | 官方支持账户/余额 readback，但当前项目未启用 private read runtime；部分已补离线 request-spec/source 边界。DEX/链上 profile 可通过 wallet/address/account id/node/indexer 读取，未接入时写项目未实现或离线边界；`bitbank` 已提升为 credential-gated runtime。 |
| P3 | 账户/余额当前 profile 不支持 | 0 | 余额 readback 不因认证、wallet/account-id、node/indexer 或下游 venue source 而判死；当前没有 adapter 留在本段。 |
| P3 | 费率项目未实现/未启用 | 26 | 官方或 adapter 证据支持 fee source，但当前项目未接标准 `get_fees` runtime；本段已有 request-spec/source fixture 的 adapter 在矩阵中写 `离线`，剩 parser/config loader/account-effective guard。`hitbtc`、`fmfwio`、`latoken`、`woo`、`toobit` 已在本轮进入 credential-gated runtime，Fee-A 的 `映射` 项也已收口。 |
| P3 | 费率当前 profile 不支持 | 35 | 当前 profile 不承接稳定共享费率 runtime，需默认/配置费率兜底。 |
| P3 | 区域现货 WS 结构化细项 | 0 | 官方已确认 orderbook WS 细项，当前 mapping 缺 interval/depth/channel/sequence 风险字段；`bitbank`、`bithumb`、`bitopro`、`bitso`、`btcmarkets`、`coincheck`、`coinone`、`independentreserve`、`luno`、`upbit` 已补。 |
| P3 | CEX WS 结构化细项 | 0 | 官方已确认 CEX orderbook WS 细项，当前 mapping 缺 interval/depth/channel/sequence/checksum 结构化字段；`bitfinex`、`bitflyer`、`bitmart`、`bitstamp`、`cryptocom`、`phemex`、`poloniex`、`coinex`、`coinmate`、`coinmetro`、`coindcx` 已补。 |
| P3 | 衍生品/链上 WS 结构化细项 | 0 | 已完成：`aark`、`modetrade`、`woofipro`、`apex`、`aster`、`blofin`、`bullish`、`derive`、`dydx`、`hyperliquid`、`lighter`、`paradex` 的 channel/interval/depth/sequence/checksum/resync 边界已落地。 |
| P3 | 补充交易所 WS 结构化细项 | 0 | 已完成：`aftermath`、`alpaca`、`apollox_dex`、`arkham`、`biconomy`、`bigone`、`bitbns`、`bitrue`、`bittrade`、`bitunix`、`blockchaincom` 的公共订单簿 WS 结构化边界已落地。 |
| P3 | 补充交易所 WS 结构化细项二 | 0 | 已完成：`bybiteu`、`bsx`、`btcturk`、`bydfi`、`coinstore`、`cointr`、`cryptomus`、`deepcoin`、`delta`、`digifinex`、`exmo` 的公共订单簿 WS 结构化边界已落地。 |
| P3 | 补充交易所 WS 结构化细项三 | 0 | 已完成：`fmfwio`、`foxbit`、`gemini`、`grvt`、`hashkey_global`、`hibachi`、`hitbtc`、`hollaex`、`krakenfutures`、`kucoinfutures`、`mercado` 的公共订单簿 WS 结构化边界已落地。 |
| P3 | 剩余交易所 WS 结构化细项 | 0 | 已完成：`myokx`、`ndax`、`novadax`、`okxus`、`onetrading`、`oxfun`、`pacifica`、`tapbit`、`tokocrypto`、`toobit`、`wavesexchange`、`weex`、`xt`、`zaif` 的公共订单簿 WS 结构化边界已落地。 |
| P4 | 高级订单能力项目未实现/未启用 | 11 | 官方支持批量、改单或等价 endpoint，但当前项目只到 spec/signing/session 边界，或 live runtime 仍未启用；已完成/运行态或明确 unsupported audit boundary 的 adapter 已从本段移出。 |
| P4 | 高级订单能力当前不支持 | 40 | 已核验当前 profile 不支持原生批量、改单或 OCO/OTO 语义；本轮已补齐 CEX.IO、Binance.US、HitBTC-family cancel-list、BTCBOX、BtcTurk、Bybit EU 等剩余 unsupported operation 边界。 |

## 本批已完成：BingX / Bitvavo / Bitget / Gate.io / Binance / OKX / MEXC / KuCoin / HTX / Huobi / LBank / CoinW / Backpack / BitMEX / Deribit / WhiteBIT / WOO X / Coinbase / Coinbase Exchange / AscendEX / CEX.IO / P2B / LATOKEN / Paymium / Bithumb / Bitopro / Bitso / BTC Markets / Coincheck / Coinone / Independent Reserve / Luno / Upbit / Bitfinex / bitFlyer / BitMart / Bitstamp / Crypto.com / Phemex / Poloniex / CoinEx / Coinmate / Coinmetro / CoinDCX / Aark / BSX / Bullish / GRVT / Hibachi / Lighter / ModeTrade / Cod3x / Derive / dYdX / OX.FUN

| adapter | 完成项 | 实现证据 | 验证 |
| --- | --- | --- | --- |
| `bitbank` | P3 账户/余额 runtime 完成：`GET /v1/user/assets` 已接入 credential-gated signed REST runtime，解析 `data.assets` 为 shared Balance snapshots，并支持按请求资产过滤。 | `crates/rustcta-exchange-gateway/src/adapters/bitbank/private.rs`、`mod.rs`、`tests.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/bitbank/assets_success.json`、`tests/fixtures/exchanges/bitbank/request_specs/get_balances_assets.json`、`docs/交易所网关/适配器/bitbank_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py` for `bitbank`/`derive`/`dydx`/`oxfun`；`python3 scripts/generate_exchange_gateway_matrix.py --status-date 2026-06-09`；`cargo test -p rustcta-exchange-gateway bitbank --lib --message-format short`。 |
| `aark` | P4 高级订单边界加固：Orderly `PUT /v1/order` amend 与 `POST /v1/batch-order` batch-place 保持 project-unimplemented/spec-only，`place_order_list` 与 `batch_cancel_orders` 保持 true unsupported；未启用 live write。 | `tests/fixtures/exchanges/aark/unsupported_boundary.json` 新增 `advanced_order_boundaries`，绑定 amend/batch-place request/parser fixtures 和剩余 account onboarding/Ed25519/permission/reconciliation/dry-run 缺口；adapter test 覆盖 project/unsupported 分离。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/aark/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway aark --lib --message-format short`。 |
| `grvt` / `hibachi` / `lighter` / `bullish` | P4 底部高级订单边界加固：GRVT/Hibachi/Lighter 的 amend/batch request-spec/source/parser evidence 与 runtime-disabled capabilities 已复核；Bullish amend/cancel-all offline guard 及 batch/order-list unsupported guard 已补断言。未启用 live write。 | GRVT/Lighter `unsupported_boundary.json` 新增 structured `advanced_order_boundaries`；Hibachi 补 amend/batch request_specs/parser fixtures 和 explicit runtime guards；Bullish adapter test 覆盖 amend、cancel-all、batch-place、batch-cancel、order-list fail-closed operation。四者 adapter 文档/测试同步。 | `python3 scripts/validate_exchange_endpoint_mapping.py` for `grvt` / `hibachi` / `lighter` / `bullish`；`python3 -m json.tool` for 4 个 unsupported fixtures；`cargo test -p rustcta-exchange-gateway grvt --lib --message-format short`；`cargo test -p rustcta-exchange-gateway hibachi --lib --message-format short`；`cargo test -p rustcta-exchange-gateway lighter --lib --message-format short`；`cargo test -p rustcta-exchange-gateway bullish --lib --message-format short`。 |
| `coinmate` / `bsx` / `modetrade` | P4 底部高级订单边界加固：Coinmate replace-limit amend、BSX amend/batch-cancel、ModeTrade Orderly amend/batch-place 均补齐 request/source/parser/capability evidence，但保持 runtime-disabled 或 explicit unsupported；未启用 live write。 | 三者各自 `endpoint_mapping.yaml`、`unsupported_boundary.json`、request_specs/parser fixtures、adapter tests 和 adapter 文档；runtime guard 明确列 Coinmate nonce/side-price/reconciliation、BSX account headers/EIP-712/session/safe cancel/reconciliation、ModeTrade Orderly account onboarding/Ed25519/permission/dry-run/reconciliation 缺口。 | `python3 scripts/validate_exchange_endpoint_mapping.py` for `coinmate` / `bsx` / `modetrade`；`python3 -m json.tool` for 3 个 unsupported fixtures；`cargo test -p rustcta-exchange-gateway coinmate --lib --message-format short`；`cargo test -p rustcta-exchange-gateway bsx --lib --message-format short`；`cargo test -p rustcta-exchange-gateway modetrade --lib --message-format short`。 |
| `derive` / `dydx` / `oxfun` | P4 底部高级订单边界加固：Derive `private/replace` amend、dYdX chain tx amend/batch、OX.FUN private WS `placeorders` batch-place 均补齐 request/source/parser/reconciliation 证据，但保持 runtime-disabled 或 explicit unsupported，不启用 live write。 | Derive/dYdX/OX.FUN 各自 `endpoint_mapping.yaml`、adapter tests、request_specs/parser/signing 或 WS fixtures、adapter 文档；runtime guard 明确列 session/JWT/Stark 或 wallet signer/account sequence/private WS session/dry-run/reconciliation 缺口。 | `cargo test -p rustcta-exchange-gateway derive --lib --message-format short`；`cargo test -p rustcta-exchange-gateway dydx --lib --message-format short`；`cargo test -p rustcta-exchange-gateway oxfun --lib --message-format short`。 |
| `cod3x` / `coinmetro` / `coinone` | P4 当前不支持高级订单边界加固：`amend_order`、`place_order_list`、`batch_place_orders`、`batch_cancel_orders` 保持 explicit unsupported；Cod3x/Coinmetro 的 `cancel_all_orders` 也保持 unsupported，Coinone 的 native `cancel_all_orders` 单独保留。 | 强化三者 `unsupported_boundary.json`、`endpoint_mapping.yaml` unsupported operation、adapter 文档和定向测试；Coinone 收回 gateway-composed `batch_cancel_orders`，不把顺序单撤伪装为 native batch。 | `python3 scripts/validate_exchange_endpoint_mapping.py` for 3 个 mapping；`python3 -m json.tool` for 3 个 unsupported fixtures；`cargo test -p rustcta-exchange-gateway cod3x/coinmetro/coinone --lib --message-format short`；`cargo check -p rustcta-exchange-gateway --lib --message-format short`。 |
| `btcbox` / `btcturk` / `bybiteu` | P4 当前不支持高级订单边界加固：`amend_order`、`place_order_list`、`batch_place_orders`、`batch_cancel_orders` 和 audit-only `cancel_all_orders` 不提升 runtime。 | 三个 `unsupported_boundary.json` 补齐 advanced-order operation 证据；`endpoint_mapping.yaml` 复核/收窄 unsupported batch/amend/order-list 边界；adapter tests 新增 capability flags 与 runtime `Unsupported` operation 断言；单交易所文档补不支持原因。 | `python3 scripts/validate_exchange_endpoint_mapping.py` for `btcbox` / `btcturk` / `bybiteu`；`cargo test -p rustcta-exchange-gateway btcbox/btcturk/bybiteu --lib --message-format short`；scoped `rustfmt --edition 2021 --check`；`cargo check -p rustcta-exchange-gateway --lib --message-format short`。 |
| `hollaex` / `gemini` / `foxbit` / `exmo` / `equation` / `derive_chain_perps` | P4 当前不支持高级订单边界加固：`amend_order`、`place_order_list`、`batch_place_orders`、`batch_cancel_orders` 和 audit-only `cancel_all_orders` 保持 explicit unsupported，不暴露 runtime/capability。 | 在现有 adapter tests 中补可执行断言，覆盖 capability flags、runtime `Unsupported` operation 名称和已有 `endpoint_mapping.yaml` unsupported 边界；未改变矩阵运行态。 | `cargo test -p rustcta-exchange-gateway hollaex/gemini/foxbit/exmo/equation/derive_chain_perps --lib --message-format short`；6 个 mapping `validate_exchange_endpoint_mapping.py`；`python3 scripts/generate_exchange_gateway_matrix.py`；`cargo check -p rustcta-exchange-gateway --lib --message-format short`。 |
| `coinsph` / `coinspot` / `cryptomus` | P4 当前不支持高级订单边界加固：`amend_order`、`place_order_list`、`batch_place_orders`、`batch_cancel_orders`、`cancel_all_orders` 均保持 explicit unsupported，不提升 live runtime。 | 新增/强化 `unsupported_boundary.json`、补 `cancel_all_orders` endpoint mapping、补 capabilities flags 与 runtime `Unsupported.operation` 定向测试；Coins.ph 同步暴露 `capabilities_v2` unsupported endpoints，CoinSpot 增加 adapter 自有 order-list guard。 | `python3 scripts/validate_exchange_endpoint_mapping.py` for 3 个 mapping；`cargo test -p rustcta-exchange-gateway coinsph/coinspot/cryptomus --lib --message-format short`；`cargo check -p rustcta-exchange-gateway --lib --message-format short`。 |
| `cex` / `hollaex` / `p2b` / `paymium` / `yobit` / `zebpay` / `bitteam` / `bitbns` | P2 核心交易 request-spec/source 边界收口：place/cancel/query/open/fills 均从项目未实现推进为矩阵 `离线`；`cex`/`hollaex`/`zebpay` 的 cancel-all 也为 `离线`。 | 8 个 `endpoint_mapping.yaml` 的 P2 core endpoints 改为 `support: spec_only`，补齐缺失 query/fills/source-boundary fixtures；runtime 仍返回 guarded `Unsupported`，不伪造 live 成功。 | `python3 scripts/validate_exchange_endpoint_mapping.py`；`python3 scripts/generate_exchange_gateway_matrix.py --status-date 2026-06-09`；P2 core JSON fixtures parse；目标矩阵抽查。 |
| `binance` | P0 Spot 公共订单簿 WS 完成：`bookTicker`、partial depth `5/10/20`、diff depth `@100ms` 订阅 payload、100ms/1000ms、`lastUpdateId`/`U`/`u`、REST depth snapshot 重建策略和 parser fixture。 | `crates/rustcta-exchange-gateway/src/adapters/binance/streams.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/binance/ws/public_depth_delta.json`、`docs/交易所网关/适配器/binance_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/binance/endpoint_mapping.yaml crates/rustcta-exchange-gateway/src/adapters/okx/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway binance --lib --message-format short`。 |
| `binance` | P4 USD-M 高级订单 batch 完成：native `/fapi/v1/batchOrders` 已接入 `batch_place_orders` max 5 与 `batch_cancel_orders` max 10，同一市场/撤单同一 symbol、partial failure report 和缺失 item reconciliation；Spot batch 继续写 unsupported。 | `crates/rustcta-exchange-gateway/src/adapters/binance/private.rs`、`toolchain.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/binance/request_specs/futures_batch_place_orders.json`、`tests/fixtures/exchanges/binance/request_specs/futures_batch_cancel_orders.json`、`docs/交易所网关/适配器/binance_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/binance/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway binance --lib --message-format short`。 |
| `binance` | P0 Options 产品线边界加固：Options 是官方产品线，但当前 `binance` adapter 只承接 Spot + USD-M；COIN-M 委托 `binancecoinm`。 | `endpoint_mapping.yaml` 的 `options_product` 保持 `project_unimplemented_product_line`，新增 `tests/fixtures/exchanges/binance/request_specs/product_line_options_boundary.json`，列出 Options metadata/orderbook/account/position/risk/order lifecycle/reconciliation 必补项和 unsafe promotion 条件。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/binance/endpoint_mapping.yaml`；`python3 -m json.tool tests/fixtures/exchanges/binance/request_specs/product_line_options_boundary.json`；`cargo test -p rustcta-exchange-gateway binance --lib --message-format short`。 |
| `binancecoinm` | P4 COIN-M 高级订单 batch 完成：native `/dapi/v1/batchOrders` 已接入 `batch_place_orders` max 5 与 `batch_cancel_orders` max 10，同一 COIN-M 市场/撤单同一 symbol、partial failure report 和缺失 item reconciliation；order-list 继续写 unsupported。 | `crates/rustcta-exchange-gateway/src/adapters/binancecoinm/private.rs`、`toolchain.rs`、`mod.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/binancecoinm/request_specs/batch_place_orders.json`、`tests/fixtures/exchanges/binancecoinm/request_specs/batch_cancel_orders.json`、`tests/fixtures/exchanges/binancecoinm/signing_vectors/batch_place_orders.json`、`tests/fixtures/exchanges/binancecoinm/signing_vectors/batch_cancel_orders.json`、`docs/交易所网关/适配器/binancecoinm_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/binancecoinm/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway binancecoinm --lib --message-format short`。 |
| `bybit` | P4 V5 高级订单 runtime 完成：`amend_order`、`batch_place_orders`、`batch_cancel_orders` 已接入 signed REST；batch create/cancel 限同一 market type、单批最多 20，并返回 partial failure report 与缺失 item reconciliation。`place_order_list` 继续写 unsupported。 | `crates/rustcta-exchange-gateway/src/adapters/bybit/private.rs`、`mod.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/bybit/request_specs/amend_order.json`、`tests/fixtures/exchanges/bybit/request_specs/batch_place_orders.json`、`tests/fixtures/exchanges/bybit/request_specs/batch_cancel_orders.json`、`tests/fixtures/exchanges/bybit/signing_vectors/amend_order_qty.json`、`tests/fixtures/exchanges/bybit/signing_vectors/batch_place_orders.json`、`tests/fixtures/exchanges/bybit/signing_vectors/batch_cancel_orders.json`、`docs/交易所网关/适配器/bybit_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/bybit/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway bybit_ --lib`。 |
| `latoken` | P4 batch runtime 完成：`/v2/auth/order/placeBulk` 与 `/v2/auth/order/cancelBulk` 已接入 credential-gated signed REST runtime；max 50、per-item success/failure parser 和 partial report 已覆盖。`amend_order`、`place_order_list` 继续写 unsupported。 | `crates/rustcta-exchange-gateway/src/adapters/latoken/private.rs`、`transport.rs`、`toolchain.rs`、`mod.rs`、`tests.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/latoken/request_specs/batch_place_orders.json`、`tests/fixtures/exchanges/latoken/request_specs/batch_cancel_orders.json`、`tests/fixtures/exchanges/latoken/parser/batch_place_orders_ack.json`、`tests/fixtures/exchanges/latoken/parser/batch_cancel_orders_ack.json`、`docs/交易所网关/适配器/latoken_adapter.md`。 | `cargo test -p rustcta-exchange-gateway latoken --lib --message-format short`；`python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/latoken/endpoint_mapping.yaml`。 |
| `apollox_dex` | P4 batch-place runtime 完成：V1 `/fapi/v1/batchOrders` 已接入 credential-gated signed REST runtime；max 5、HMAC query、mock request 和 partial-failure report 已覆盖。batch-cancel、amend/order-list 继续写 unsupported。 | `crates/rustcta-exchange-gateway/src/adapters/apollox_dex/private.rs`、`transport.rs`、`toolchain.rs`、`mod.rs`、`tests.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/apollox_dex/request_specs/batch_place_orders.json`、`tests/fixtures/exchanges/apollox_dex/parser/batch_place_orders_ack.json`、`docs/交易所网关/适配器/apollox_dex_adapter.md`。 | `cargo test -p rustcta-exchange-gateway apollox_dex --lib --message-format short`；`python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/apollox_dex/endpoint_mapping.yaml`。 |
| `novadax` | P4 batch runtime 完成：native `/v1/orders/batch-create` 与 `/v1/orders/batch-cancel` 已接入 credential-gated signed REST runtime；mock tests 覆盖 Nova HMAC headers、request body 和 ack/fallback parser。 | `crates/rustcta-exchange-gateway/src/adapters/novadax/private.rs`、`transport.rs`、`mod.rs`、`tests.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/novadax/request_specs/batch_place_orders.json`、`tests/fixtures/exchanges/novadax/request_specs/batch_cancel_orders.json`、`docs/交易所网关/适配器/novadax_adapter.md`。 | `cargo test -p rustcta-exchange-gateway novadax --lib --message-format short`；`python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/novadax/endpoint_mapping.yaml`。 |
| `okx` | P0 Spot/Swap/Futures/Options 公共订单簿 WS 完成：`books5`、`books`、`bbo-tbt` 订阅 payload、10ms/100ms、`seqId`、REST books snapshot 重建策略和 parser fixture；VIP/login-only TBT 深度仍是剩余项。 | `crates/rustcta-exchange-gateway/src/adapters/okx/streams.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/okx/ws/public_books5_snapshot.json`、`docs/交易所网关/适配器/okx_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/binance/endpoint_mapping.yaml crates/rustcta-exchange-gateway/src/adapters/okx/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway okx --lib --message-format short`。 |
| `okx` | P0 Futures/Options 产品线完成：当前 `okx` adapter 已声明 Spot + Swap + Futures + Options，补入 Futures/Options instruments/books、positions、fees、place/cancel/cancel-all/query/open/fills、amend，并复用 OKX V5 REST 对账；private WS 仍按剩余边界单独评估。 | `crates/rustcta-exchange-gateway/src/adapters/okx/mod.rs`、`parser.rs`、`private.rs`、`types.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/okx/request_specs/get_positions_futures.json`、`tests/fixtures/exchanges/okx/request_specs/place_order_futures.json`、`tests/fixtures/exchanges/okx/request_specs/get_positions_option.json`、`tests/fixtures/exchanges/okx/request_specs/place_order_option.json`、`docs/交易所网关/适配器/okx_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/okx/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway okx --lib --message-format short`。 |
| `okx` | P4 native batch place/cancel 完成：`POST /api/v5/trade/batch-orders` 与 `POST /api/v5/trade/cancel-batch-orders` 已暴露到 shared batch trait，单批最多 20、同一 market type、partial failure report 和缺失 item reconciliation 已覆盖；cancel-all 继续复用 cancel-batch。 | `crates/rustcta-exchange-gateway/src/adapters/okx/private.rs`、`toolchain.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/okx/request_specs/batch_place_orders.json`、`tests/fixtures/exchanges/okx/request_specs/batch_cancel_orders.json`、`docs/交易所网关/适配器/okx_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/okx/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway okx --lib --message-format short`。 |
| `bingx` | 当前任务清单无待办；Spot + USDT linear perpetual REST、public/private WS specs、listen-key、batch、amend/杠杆/模式切换边界已实现。 | `crates/rustcta-exchange-gateway/src/adapters/bingx/endpoint_mapping.yaml`、`capabilities_v2.yaml`、`streams.rs`、`private.rs`、request specs 和 signing vectors。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/bingx/endpoint_mapping.yaml`；`CARGO_TARGET_DIR=/tmp/rustcta-bingx-target cargo test -p rustcta-exchange-gateway bingx_ --lib --message-format short`。 |
| `bitvavo` | P3 `public_ws_struct` 完成：`book` channel、无固定 ms、REST depth 初始化、`nonce + 1` 连续性、无 checksum、gap/reconnect REST snapshot 重建。 | `crates/rustcta-exchange-gateway/src/adapters/bitvavo/endpoint_mapping.yaml`、`streams.rs`、`tests/fixtures/exchanges/bitvavo/ws/public_book.json`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/bitvavo/endpoint_mapping.yaml`；`CARGO_TARGET_DIR=/tmp/rustcta-bitvavo-target cargo test -p rustcta-exchange-gateway bitvavo --lib --message-format short`。 |
| `bitbank` | P4 same-pair batch cancel runtime 完成：`batch_cancel_orders` 已接入 signed private REST，mock test 覆盖 HMAC headers、same-pair request body、success response parser 和 cancelled-state fallback；batch place/amend/order-list 继续写 unsupported。 | `crates/rustcta-exchange-gateway/src/adapters/bitbank/private.rs`、`transport.rs`、`mod.rs`、`tests.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/bitbank/batch_cancel_orders_success.json`、`docs/交易所网关/适配器/bitbank_adapter.md`。 | `cargo test -p rustcta-exchange-gateway bitbank --lib --message-format short`；`python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/bitbank/endpoint_mapping.yaml`。 |
| `bitvavo` | P2 `explicit_unsupported` 完成：当前官方 API 口径写 `交易所不支持合约`。 | `docs/交易所网关/适配器/bitvavo_adapter.md`、`tests/fixtures/exchanges/bitvavo/unsupported_boundary.json`。 | 同 Bitvavo 定向测试。 |
| `bitvavo` | P4 `advanced_trading_unsupported` 完成：native batch、shared amend、order-list 不进入当前 runtime。 | `crates/rustcta-exchange-gateway/src/adapters/bitvavo/mod.rs`、`tests/fixtures/exchanges/bitvavo/unsupported_boundary.json`。 | 同 Bitvavo 定向测试。 |
| `bitget` | P0 public order book WS 完成：Spot/USDT Perpetual `books1`、`books5`、`books15`、`books`，10ms/200ms、`seq`、CRC32 和 REST snapshot 重建已结构化。 | `crates/rustcta-exchange-gateway/src/adapters/bitget/endpoint_mapping.yaml`、`streams.rs`、`stream_tests.rs`、`tests/fixtures/exchanges/bitget/ws/public_depth_books5.json`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/bitget/endpoint_mapping.yaml crates/rustcta-exchange-gateway/src/adapters/gateio/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway bitget --lib --message-format short`。 |
| `gateio` | P0 public order book WS 完成：Spot/USDT Perpetual `spot.order_book_update`、`futures.order_book_update`，20ms/100ms、20/100 档、`u` sequence 和 REST snapshot 重建已结构化。 | `crates/rustcta-exchange-gateway/src/adapters/gateio/endpoint_mapping.yaml`、`streams.rs`、`stream_tests.rs`、`tests/fixtures/exchanges/gateio/ws/public_spot_order_book_update.json`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/bitget/endpoint_mapping.yaml crates/rustcta-exchange-gateway/src/adapters/gateio/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway gateio --lib --message-format short`。 |
| `mexc` | P0 public order book WS 完成：Spot `bookTicker` 10ms、aggregated depth 10ms、limit depth 5/10/20，`fromVersion`/`toVersion`/`version` sequence 和 REST snapshot 重建已结构化；Contract public depth spec 同步声明。 | `crates/rustcta-exchange-gateway/src/adapters/mexc/streams.rs`、`endpoint_mapping.yaml`、`stream_tests.rs`、`tests/fixtures/exchanges/mexc/ws/spot_aggre_depth_10ms.json`、`docs/交易所网关/适配器/mexc_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/mexc/endpoint_mapping.yaml crates/rustcta-exchange-gateway/src/adapters/kucoin/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway mexc --lib --message-format short`。 |
| `mexc` | P0 Contract/Futures 产品线完成：当前 `mexc` adapter 已声明 Spot + Perpetual，补入 Contract REST symbol/depth、balances、positions、fees、place/cancel/cancel-all/query/open/fills；private WS 保持 unsupported，走 REST 对账。 | `crates/rustcta-exchange-gateway/src/adapters/mexc/public.rs`、`private.rs`、`mod.rs`、`endpoint_mapping.yaml` 和 private/public tests。 | 同 MEXC 定向测试。 |
| `kucoin` | P0 public order book WS 结构化细项完成：Spot OBU BBO depth=1、depth5/50 100ms、increment real-time、`sequenceStart`/`sequenceEnd`/`sequence` 和 REST snapshot replay 已落地；2026-06-17 UTC UTA 500 档 10ms 仍只作为未来迁移项。 | `crates/rustcta-exchange-gateway/src/adapters/kucoin/streams.rs`、`endpoint_mapping.yaml`、`stream_tests.rs`、`tests/fixtures/exchanges/kucoin/ws/obu_increment.json`、`docs/交易所网关/适配器/kucoin_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/mexc/endpoint_mapping.yaml crates/rustcta-exchange-gateway/src/adapters/kucoin/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway kucoin --lib --message-format short`。 |
| `kucoin` | P3 fee mapping 完成：signed Spot `GET /api/v1/trade-fees` 已进入 `get_fees` mapping、request spec fixture 和 private test；batch place/cancel 继续按 gateway-composed sequential 边界记录。 | `crates/rustcta-exchange-gateway/src/adapters/kucoin/private.rs`、`private_tests.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/kucoin/request_specs/trade_fees.json`。 | 同 KuCoin 定向测试。 |
| `htx` / `huobi` | P0 public order book WS 完成：Spot `mbp.refresh.20` 100ms、`mbp.20` event-driven、BBO mapping-only；USDT-M `depth.size_20.high_freq` 30ms、depth 20/150、`seqNum`/`prevSeqNum`/`version`、REST snapshot continuity rebuild 已结构化。 | `crates/rustcta-exchange-gateway/src/adapters/htx/streams.rs`、`parser.rs`、`endpoint_mapping.yaml`、`crates/rustcta-exchange-gateway/src/adapters/huobi/endpoint_mapping.yaml`、`tests/fixtures/exchanges/htx/ws/public_spot_mbp_refresh.json`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/htx/endpoint_mapping.yaml crates/rustcta-exchange-gateway/src/adapters/huobi/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway htx --lib --message-format short`。 |
| `lbank` | P0 public order book WS 完成：Spot `depth` channel、10/50/100 档、官方未给固定 ms、无 sequence/checksum、REST `/v2/depth.do` snapshot fallback 和重订阅 resync 已结构化。 | `crates/rustcta-exchange-gateway/src/adapters/lbank/streams.rs`、`endpoint_mapping.yaml`、`capabilities_v2.yaml`、`tests/fixtures/exchanges/lbank/ws/public_spot_depth.json`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/lbank/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway lbank --lib --message-format short`。 |
| `coinw` | P0 public order book WS 完成：Spot method1 `spot/level2_20`、method2 `depth_snapshot`/`depth` incremental、`seq`/`startSeq`/`endSeq`；Futures `depth` 100 档已解析，官方未见 sequence/checksum 的风险和 REST snapshot/resubscribe 边界已记录。 | `crates/rustcta-exchange-gateway/src/adapters/coinw/streams.rs`、`parser.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/coinw/ws/spot_depth_snapshot.json`、`spot_depth_incremental.json`、`futures_depth_100.json`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/coinw/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway coinw --lib --message-format short`。 |
| `backpack` | P1 public order book WS 结构化细项完成：`bookTicker.{symbol}`、`depth.{symbol}`、`depth.200ms/600ms/1000ms.{symbol}`、`U/u`、REST `lastUpdateId` snapshot replay 和 gap resync 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/backpack/streams.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/backpack/ws/public_depth_realtime.json`、`public_depth_200ms.json`、`public_book_ticker.json`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/backpack/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway backpack --lib --message-format short`。 |
| `bitmex` | P1 public order book WS 结构化细项完成：`orderBookL2_25` 25 档 100ms、`orderBook10`、`orderBookL2` table diff、`partial/insert/update/delete`、keys `[symbol,id,side]`、无 sequence/checksum 风险和 REST L2 snapshot resync 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/bitmex/streams.rs`、`parser.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/bitmex/ws/public_orderbook_l2_25_partial.json`、`public_orderbook10.json`、`public_orderbook_l2_delete.json`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/bitmex/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway bitmex --lib --message-format short`。 |
| `deribit` | P1 public order book WS 结构化细项完成：`book.{instrument}.{group}.{depth}.{interval}`、depth 1/10/20、`raw/100ms/agg2`、`change_id`/`prev_change_id` 连续性和 REST `get_order_book` resync 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/deribit/streams.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/deribit/ws/public_book_grouped.json`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/deribit/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway deribit --lib --message-format short`。 |
| `whitebit` | P1 public order book WS 结构化细项完成：`depth_subscribe` 100ms 增量、100 档、10s snapshot keepalive、`update_id` 连续性、无 checksum 和 REST snapshot/resync 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/whitebit/streams.rs`、`parser.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/whitebit/ws/public_depth_update.json`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/whitebit/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway whitebit --lib --message-format short`。 |
| `woo` | P1 public order book WS 结构化细项完成：`bbo@{symbol}` 10ms、`orderbookupdate@{symbol}@100` 200ms、`orderbook@{symbol}@100` 1s/100 档、`seq/sequence`、无 checksum 和 REST `/v3/public/orderbook` resync 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/woo/streams.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/woo/ws/public_bbo.json`、`public_orderbookupdate.json`、`public_orderbook_100.json`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/woo/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway woo --lib --message-format short`。 |
| `coinbaseexchange` | P1 public order book WS 结构化细项完成：`level2_batch` 50ms、`level2`、`full`、`level3` sequence 边界、subscription payload 和 REST `/products/{product_id}/book` snapshot/resync 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/coinbaseexchange/streams.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/coinbaseexchange/ws/public_level2_snapshot.json`、`public_level2_batch_update.json`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/coinbaseexchange/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway coinbaseexchange --lib --message-format short`。 |
| `latoken` | P2 public order book WS 完成：STOMP `/v1/book/{base}/{quote}` 订阅、`nonce` sequence 映射、gap/regression reconnect 和 REST `/v2/book/{base}/{quote}` rebuild 已落地；官方未给固定 ms/depth/checksum。 | `crates/rustcta-exchange-gateway/src/adapters/latoken/streams.rs`、`parser.rs`、`toolchain.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/latoken/unsupported_boundary.json`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/latoken/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway latoken --lib --message-format short`。 |
| `paymium` | P2 public order book WS 完成：socket.io v1.3 public `stream`/`bids_asks` 边界、connect-only subscription spec、changed price levels、zero-amount delete、无 sequence/checksum 和 REST `/data/eur/depth` full rebuild 已落地；runtime 由 `PAYMIUM_PUBLIC_STREAMS_ENABLED` gate 控制。 | `crates/rustcta-exchange-gateway/src/adapters/paymium/streams.rs`、`parser.rs`、`mod.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/paymium/ws/public_book_update.json`、`public_book_update_socketio.json`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/paymium/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway paymium --lib --message-format short`。 |
| `bithumb` | P3 regional spot public WS 结构化细项完成：`orderbook` Public WS、snapshot/realtime 开关、level 聚合、无固定 ms、无 sequence/checksum 和 REST snapshot resync 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/bithumb/streams.rs`、`tests.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/bithumb/ws/orderbook_realtime.json`、`docs/交易所网关/适配器/bithumb_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/bithumb/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway bithumb --lib --message-format short`。 |
| `bitopro` | P3 regional spot public WS 结构化细项完成：1s updated full-book snapshots、默认 5 档、可选 1/5/10/20/30/50、snapshot-only 语义、无 sequence/checksum 和 REST snapshot resync 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/bitopro/streams.rs`、`parser.rs`、`stream_tests.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/bitopro/ws/public_orderbook_subscription.json`、`docs/交易所网关/适配器/bitopro_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/bitopro/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway bitopro --lib --message-format short`。 |
| `bitopro` | P4 batch/cancel-all runtime 完成：`batch_place_orders`、`batch_cancel_orders` 与 `cancel_all_orders` 已接入 signed private REST，mock test 覆盖 request body/DELETE path、HMAC headers、success parser 和 ack/cancel fallback；amend/order-list 继续写 unsupported，single place/cancel 仍离线 request-spec。 | `crates/rustcta-exchange-gateway/src/adapters/bitopro/private.rs`、`transport.rs`、`mod.rs`、`private_tests.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/bitopro/batch_place_orders_success.json`、`tests/fixtures/exchanges/bitopro/batch_cancel_orders_success.json`、`tests/fixtures/exchanges/bitopro/cancel_all_orders_success.json`、`docs/交易所网关/适配器/bitopro_adapter.md`。 | `cargo test -p rustcta-exchange-gateway bitopro --lib --message-format short`；`python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/bitopro/endpoint_mapping.yaml`。 |
| `bitso` | P3 regional spot public WS 结构化细项完成：`orders` top20、`diff-orders`、strict sequence continuity、无固定 interval/checksum 和 REST `/order_book` snapshot replay/resync 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/bitso/streams.rs`、`tests.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/bitso/ws_public_orders.json`、`ws_public_diff_orders.json`、`docs/交易所网关/适配器/bitso_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/bitso/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway bitso --lib --message-format short`。 |
| `btcmarkets` | P3 regional spot public WS 结构化细项完成：WS v2 `orderbook` latest-state event、50 bids/50 asks、5s heartbeat、无固定 ms、无 sequence/checksum 和 REST `/v3/markets/{marketId}/orderbook` rebuild 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/btcmarkets/streams.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/btcmarkets/ws_orderbook.json`、`docs/交易所网关/适配器/btcmarkets_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/btcmarkets/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway btcmarkets --lib --message-format short`。 |
| `coincheck` | P3 regional spot public WS 结构化细项完成：`{pair}-orderbook` diff stream、约 0.1s when trades occur、无固定 depth、无 sequence/checksum、zero-amount delete fixture 和 REST `/api/order_books` rebuild 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/coincheck/streams.rs`、`stream_tests.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/coincheck/ws/public_orderbook_diff.json`、`docs/交易所网关/适配器/coincheck_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/coincheck/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway coincheck --lib --message-format short`。 |
| `coinone` | P3 regional spot public WS 结构化细项完成：`ORDERBOOK` topic、首包 last-order-book snapshot、change-only update、`data.id`/`d.i` freshness、无固定 ms/depth、无 checksum 和 REST snapshot resync 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/coinone/streams.rs`、`stream_tests.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/coinone/ws_public_orderbook_snapshot.json`、`tests/fixtures/exchanges/coinone/ws_public_orderbook_update.json`、`docs/交易所网关/适配器/coinone_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/coinone/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway coinone --lib --message-format short`。 |
| `independentreserve` | P3 regional spot public WS 结构化细项完成：`orderbook-{primary}` Subscribe/query 边界、`NewOrder`/`OrderChanged`/`OrderCanceled` event stream、`Nonce` continuity、60s heartbeat、无固定 ms/depth/checksum 和 REST `/Public/GetOrderBook` rebuild 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/independentreserve/streams.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/independentreserve/ws/public_orderbook_new_order.json`、`docs/交易所网关/适配器/independentreserve_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/independentreserve/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway independentreserve --lib --message-format short`。 |
| `luno` | P3 regional spot public WS 结构化细项完成：credential-required market stream URL、初始全量 book、atomic updates、`sequence` continuity、无固定 ms/depth/checksum 和错序 reconnect/resync 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/luno/streams.rs`、`tests.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/luno/ws/market_stream_initial_orderbook.json`、`tests/fixtures/exchanges/luno/ws/market_stream_update.json`、`docs/交易所网关/适配器/luno_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/luno/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway luno --lib --message-format short`。 |
| `upbit` | P3 regional spot public WS 结构化细项完成：region public WS URLs、JSON array ticket/type/codes subscription、`{code}.{count}` units 1/5/15/30、无固定 ms、无 sequence/checksum 和 REST `/v1/orderbook` rebuild 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/upbit/streams.rs`、`tests.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/upbit/ws/public_orderbook.json`、`tests/fixtures/exchanges/upbit/ws/orderbook_snapshot.json`、`docs/交易所网关/适配器/upbit_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/upbit/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway upbit --lib --message-format short`。 |
| `bitfinex` | P3 CEX public WS 结构化细项完成：`book` channel、`prec=P0..P4`、`freq=F0/F1`、`len=1/25/100/250`、`SEQ_ALL`/`OB_CHECKSUM`/bulk flags、snapshot/update/delete、sequence gap/checksum rebuild 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/bitfinex/streams.rs`、`tests.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/bitfinex/ws_public_book_subscribe.json`、`ws_public_book_seq_snapshot.json`、`ws_public_book_update.json`、`ws_public_book_checksum.json`、`docs/交易所网关/适配器/bitfinex_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/bitfinex/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway bitfinex --lib --message-format short`。 |
| `bitflyer` | P3 CEX public WS 结构化细项完成：JSON-RPC `lightning_board_snapshot_{product_code}` 与 `lightning_board_{product_code}`、无固定 ms/depth/sequence/checksum、REST `/v1/getboard` rebuild 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/bitflyer/streams.rs`、`parser.rs`、`tests.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/bitflyer/ws/public_board_snapshot_subscribe.json`、`public_board_snapshot_message.json`、`public_board_delta_message.json`、`docs/交易所网关/适配器/bitflyer_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/bitflyer/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway bitflyer --lib --message-format short`。 |
| `bitmart` | P3 CEX public WS 结构化细项完成：Spot `spot/depth/increase100` 100ms/100 档、`spot/depth5/20/50` 500ms、`spot/bookTicker` BBO real-time、`version` gap rebuild、无 checksum 风险已落地。 | `crates/rustcta-exchange-gateway/src/adapters/bitmart/streams.rs`、`stream_tests.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/bitmart/ws_spot_depth_increase_snapshot.json`、`ws_spot_depth_increase_update.json`、`ws_spot_book_ticker.json`、`docs/交易所网关/适配器/bitmart_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/bitmart/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway bitmart --lib --message-format short`。 |
| `bitstamp` | P3 CEX public WS 结构化细项完成：WebSocket v2 `order_book_{market}` 与 `diff_order_book_{market}`、无固定 ms/depth/sequence/checksum、REST `order_book` 和 `order_data` gap recovery 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/bitstamp/streams.rs`、`tests.rs`、`mod.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/bitstamp/ws/public_order_book_snapshot.json`、`public_diff_order_book.json`、`docs/交易所网关/适配器/bitstamp_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/bitstamp/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway bitstamp --lib --message-format short`。 |
| `cryptocom` | P3 CEX public WS 结构化细项完成：`book.{instrument}.{depth}` delta、10ms/100ms、depth 10/50、`SNAPSHOT_AND_UPDATE`、`u/pu` continuity 和 REST snapshot rebuild 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/cryptocom/streams.rs`、`stream_tests.rs`、`mod.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/cryptocom/ws/public_book_delta_subscribe.json`、`public_book_delta_snapshot.json`、`public_book_delta_update.json`、`docs/交易所网关/适配器/cryptocom_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/cryptocom/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway cryptocom --lib --message-format short`。 |
| `phemex` | P3 CEX public WS 结构化细项完成：`orderbook.subscribe`/`orderbook_p.subscribe`、20ms/120ms/full 100ms、depth 0/1/5/10/30、`sequence` continuity、60s snapshot self-check 和 REST/WS snapshot rebuild 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/phemex/streams.rs`、`parser.rs`、`stream_tests.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/phemex/ws_public_orderbook_snapshot.json`、`ws_public_orderbook_incremental.json`、`docs/交易所网关/适配器/phemex_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/phemex/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway phemex --lib --message-format short`。 |
| `poloniex` | P3 CEX public WS 结构化细项完成：Spot/Futures V3 `book_lv2` real-time 20-level、Spot `id/lastId` 与 Futures `id/lid` continuity、无 checksum 和 REST snapshot resubscribe/rebuild 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/poloniex/streams.rs`、`parser.rs`、`mod.rs`、`stream_tests.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/poloniex/ws/public_spot_book_lv2.json`、`public_futures_book_lv2.json`、`docs/交易所网关/适配器/poloniex_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/poloniex/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway poloniex --lib --message-format short`。 |
| `coinex` | P3 CEX public WS 结构化细项完成：`depth.subscribe`、约 200ms、limit 5/10/20/50、merge interval、full/incremental、zero quantity delete、signed CRC32 checksum 和 REST `/spot/depth` rebuild 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/coinex/streams.rs`、`stream_tests.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/coinex/ws/public_depth_subscribe.json`、`public_depth_full.json`、`public_depth_incremental.json`、`docs/交易所网关/适配器/coinex_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/coinex/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway coinex --lib --message-format short`。 |
| `coinmate` | P3 CEX public WS 结构化细项完成：`order_book-{PAIR}`、无固定 ms/depth/sequence/checksum、REST `/orderBook` snapshot fallback 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/coinmate/streams.rs`、`tests.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/coinmate/ws/public_order_book.json`、`docs/交易所网关/适配器/coinmate_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/coinmate/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway coinmate --lib --message-format short`。 |
| `coinmetro` | P3 CEX public WS 结构化细项完成：`pairs` query subscription、`bookUpdate` `seqNumber`/CRC32 checksum、`tick` BBO ask/bid、无固定 ms/depth 和 REST `/exchange/book/{pair}` rebuild 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/coinmetro/streams.rs`、`mod.rs`、`tests.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/coinmetro/ws/book_update.json`、`docs/交易所网关/适配器/coinmetro_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/coinmetro/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway coinmetro --lib --message-format short`。 |
| `coindcx` | P3 CEX public WS 结构化细项完成：Socket.IO `join`、Spot `B-BTC_USDT@orderbook@20`、Spot/Futures `@50` boundary、`depth-update` parsing、zero quantity delete、无固定 ms/sequence/checksum 和 REST snapshot rebuild 已落地。 | `crates/rustcta-exchange-gateway/src/adapters/coindcx/streams.rs`、`tests.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/coindcx/ws/public_orderbook_spot_join_20.json`、`public_orderbook_spot_join_50.json`、`public_orderbook_futures_join_50.json`、`public_depth_update_socketio.json`、`docs/交易所网关/适配器/coindcx_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/coindcx/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway coindcx --lib --message-format short`。 |
| `aark` | P3 衍生品/链上 public WS 结构化细项完成：Orderly EVM `wss://ws-evm.orderly.org/ws/stream/{account_id}`、`{symbol}@orderbookupdate` 200ms、`ts`/`prevTs` continuity、无 checksum 和 signed REST/orderbook snapshot resync 已落地为 spec-only 边界。 | `crates/rustcta-exchange-gateway/src/adapters/aark/streams.rs`、`tests.rs`、`endpoint_mapping.yaml`、`tests/fixtures/exchanges/aark/ws/public_orderbook_subscribe.json`、`public_orderbook_update.json`、`docs/交易所网关/适配器/aark_adapter.md`。 | `python3 scripts/validate_exchange_endpoint_mapping.py crates/rustcta-exchange-gateway/src/adapters/aark/endpoint_mapping.yaml`；`cargo test -p rustcta-exchange-gateway aark --lib --message-format short`。 |

## P0 公共订单簿 WebSocket

这些直接影响跨所套利上线。每个任务至少要补：

- `endpoint_mapping.yaml` 结构化字段：channel、订阅方式、推流间隔、档位、sequence/checksum、snapshot 重建。
- WebSocket 订阅 payload 和 parser fixture。
- REST snapshot + delta 重建策略。
- 断线重连后的重新订阅和 book resync。

| adapter | 产品线 | 当前项目状态 | 必补内容 |
| --- | --- | --- | --- |
| `binance` | Spot | 已补 public WS runtime metadata/parser fixture | Spot depth/bookTicker、100ms/1000ms、5/10/20 档、update id 重建已落地；剩余私有 user stream 不在本项。 |
| `okx` | Spot/Swap/Futures/Option | Spot/Swap/Futures/Option public WS 已补 | `books5`、`books`、`bbo-tbt`、10ms/100ms、`seqId` 和 REST snapshot 已落地；剩余 VIP/login-only TBT depth channel 与 private WS。 |
| `bitget` | Spot/USDT Perpetual | native（2026-06-09 已补） | 已补 `books1`/`books5`/`books15`/`books` 订阅 payload、parser fixture、10ms/200ms、`seq`、CRC32 和 REST snapshot 重建；fixture 为 `tests/fixtures/exchanges/bitget/ws/public_depth_books5.json`。 |
| `gateio` | Spot/USDT Perpetual | native（2026-06-09 已补） | 已补 `spot.order_book_update` 与 `futures.order_book_update` 订阅 payload、parser fixture、20ms/100ms、20/100 档、`u` sequence、无官方 checksum 风险和 REST snapshot 重建；fixture 为 `tests/fixtures/exchanges/gateio/ws/public_spot_order_book_update.json`。 |
| `mexc` | Spot/USDT Perpetual | native（2026-06-09 已补） | 已补 Spot `bookTicker`/aggregated depth 10ms、limit depth 5/10/20、`fromVersion`/`toVersion`/`version` parser fixture 和 REST snapshot 重建；Contract public depth spec 已声明。 |
| `binanceus` | Spot | spec_only（2026-06-09 已补） | 已补 Binance.US Spot `bookTicker`、partial depth 5/10/20、diff depth 100ms/1000ms、`lastUpdateId`/`U/u` 和 REST depth snapshot 重建。 |
| `binancecoinm` | COIN-M | spec_only（2026-06-09 已补） | 已补 COIN-M partial/diff depth、5/10/20 档、100/250/500ms、`U/u/pu` 和 REST `/dapi/v1/depth` snapshot 重建。 |
| `bybit` | Spot/Linear/Inverse/Option | native parser（2026-06-09 已补） | 已补 `orderbook.1`/`orderbook.50` 订阅、1/50/200/1000 档 interval、`u/seq/cts`、snapshot/delta 和 REST snapshot resync；当前共享 runtime 覆盖 Spot/Linear，Inverse/Option 保留 V5 mapping 边界。 |
| `kucoin` | Spot | native（2026-06-09 已补 OBU 细项） | 已补 Spot OBU BBO/5/50/increment、`sequenceStart`/`sequenceEnd`/`sequence`、snapshot replay 和 parser fixture；2026-06-17 UTC UTA `depth=increment@10ms` 500 档仍是未来迁移项，不写成当前已可用。 |
| `htx` / `huobi` | Spot/USDT-M | parser_only（2026-06-09 已补结构化字段） | 已补 Spot `mbp.refresh.20` 100ms、`mbp.20`、BBO mapping-only；USDT-M `depth.size_20.high_freq` 30ms；`seqNum`/`prevSeqNum`/`version` 与 REST snapshot 重建。 |
| `lbank` | Spot | native（2026-06-09 已补结构化字段） | 已补 `depth` 10/50/100 档、官方未给固定 ms、无 sequence/checksum 风险、REST depth snapshot fallback。 |
| `coinw` | Spot/Futures | native（2026-06-09 已补结构化字段） | 已补 Spot `spot/level2_20`、`depth_snapshot`/`depth`、`seq`/`startSeq`/`endSeq`；Futures 100 档 depth 和无 sequence/checksum 风险边界。 |

## P0 产品线项目未实现

这些不能写成 `交易所不支持`。官方已确认支持，当前项目需要补 adapter 或扩展现有 adapter。委托记录：`kucoin` 合约产品线由 `kucoinfutures` adapter 承接，不计入本段项目未实现。已完成记录：OKX Swap/Futures/Options 与 MEXC Contract/Perpetual 已进入当前 adapter，不计入本段。

| 交易所 | 官方支持 | 当前缺口 | 建议边界 |
| --- | --- | --- | --- |
| Binance | Options | USD-M Futures 已在 `binance` adapter 接 REST/公共 WS；Options 未接，当前只有 product-line source boundary。 | 保持 `options_product` 项目未实现；已列 Options metadata/orderbook/account/position/risk/order lifecycle/reconciliation 必补项和 unsafe promotion 条件，后续单独评估是否进网关或拆独立 profile。 |
| Bitget | Margin/USDC-FUTURES/COIN-FUTURES | USDT-FUTURES/Perpetual REST 与公共 WS 已接；USDC-FUTURES、COIN-FUTURES 和 Spot-Margin 仍未接。 | 已审计：现有 runtime 将 `MarketType::Perpetual` 固定为 `productType=USDT-FUTURES`/`marginCoin=USDT`，不能伪装成 USDC 或币本位 futures；`usdc_futures_product`、`coin_futures_product`、`margin_product` 保持 `project_unimplemented_product_line`，绑定 source-boundary fixture 并列 required endpoints/unsafe promotion 条件。 |
| Gate.io | Delivery、Options | Spot + USDT Perpetual REST/公共 WS/账户/仓位/订单生命周期已接；Delivery/Options 仍缺独立 metadata/orderbook/account/position/order lifecycle runtime。 | 保持 `delivery_futures_product`、`option_product` 项目未实现；mapping 已绑定 product-line source boundary，不能复用 `/futures/usdt` perpetual runtime 伪装 Delivery/Options。 |
| MEXC | Delivery Contract | Spot + Contract/Perpetual REST/公共 WS/账户/仓位/订单生命周期已接；delivery-state contract 仍缺独立 metadata/status filter、settlement、account/position/order lifecycle runtime。 | 保持 `delivery_contract_product` 项目未实现；mapping 已绑定 product-line source boundary，不能把现有 perpetual contract runtime 伪装成 Delivery Contract。 |

## B 组新增永续/衍生品候选收口

| adapter | 官方产品线结论 | 当前收口 |
| --- | --- | --- |
| `kcex` | 官方网站/支持页可见 Spot 与永续合约交易，但本轮未找到稳定公开 OpenAPI/WS 开发文档。 | 已完成 2026-06-08：新增 `kcex` adapter shell、endpoint mapping、unsupported boundary fixture 和适配器文档；所有 REST/WS/private/trading runtime 保持 `*_unverified_openapi` unsupported，等待官方 OpenAPI 稳定性核验。 |

## 高速盘口优先候选

这些官方已经给出 10ms、20ms 或 50ms 级别盘口能力，补 WebSocket 时优先落地：

| 交易所 | 最快盘口 | 当前项目处理 |
| --- | --- | --- |
| OKX | `bbo-tbt` 10ms；受限 `books-l2-tbt`/`books50-l2-tbt` 10ms。 | Spot/Swap/Futures/Options 已补 `bbo-tbt`、`books5`、`books` 订阅和 fixture；VIP/登录 TBT 深度仍按限制标注，未启用 runtime。 |
| Bybit | 1 档 10ms，50 档 20ms。 | 2026-06-09 已补 `orderbook.1` 和 `orderbook.50` 订阅/解析；REST snapshot 用于重建。 |
| Bitget | Spot `books1` 10ms。 | 已完成：public WS 支持 `books1`、`books5`、`books15` 和 `books`，mapping 写入 10ms/200ms、`seq`、CRC32、REST snapshot 重建和 parser fixture。 |
| MEXC | bookTicker 和 aggregated depth 可选 10ms。 | 2026-06-09 已补 Spot bookTicker/aggre depth 10ms subscription spec、解码后 payload parser fixture、`fromVersion`/`toVersion` 和 REST snapshot replay；Contract depth spec 已记录。 |
| WOO X | `bbo` 10ms，`orderbookupdate` 200ms。 | 当前 native 但缺结构化字段。 |
| Coinbase Exchange | `level2_batch` 50ms。 | 当前 native，补 50ms batch 与 sequence 边界。 |
| KuCoin UTA | 2026-06-17 UTC 上线 `depth=increment@10ms`，500 档。 | Spot OBU BBO/5/50/increment 已补；2026-06-17 前 UTA 500 档 10ms 只作为迁移任务，不写成当前已可用。 |
| Crypto.com | `book` delta 可选 10ms，depth 10/50。 | 当前 native 但缺结构化字段，补 `SNAPSHOT_AND_UPDATE`、`u/pu`。 |
| Phemex | orderbook depth 约 20ms，depth 0/1/5/10/30。 | 当前 native 但缺结构化字段，补 sequence 和 60s snapshot 自校验。 |
| BitMart | bookTicker BBO real-time；`depth/increase100` 100ms。 | 当前 spec_only，补 BBO 和 100 档增量 book。 |
| Lighter | `order_book/{MARKET_INDEX}` 每 50ms。 | 当前 parser_only，补 nonce continuity、snapshot/update 和 gap resubscribe。 |
| Paradex | order_book 可选 50ms/100ms，固定 15 levels。 | 当前 native，补 `seq_no`、feed_type 和 50ms/100ms refresh_rate。 |
| Aster | partial/diff depth 可 100ms，bookTicker real-time。 | 当前 declared，补 5/10/20 档和 `U/u/pu` 重建规则。 |
| BloFin | `books`/`books5` 100ms。 | 当前 native，补 200 depth/5 depth 和 `prevSeqId/seqId`。 |
| ApolloX DEX/APX | partial/diff depth 可 100ms，bookTicker real-time。 | 当前 spec_only，补 5/10/20 档、100ms 和 `U/u/pu`。 |
| BitTrade | BBO 变化推送，带 `seqId`。 | 当前 native，补 BBO/depth channel、step0-step5 和 gzip。 |

## P1 已有 WS 但缺结构化细项

这些适合先补 `endpoint_mapping.yaml`，再补 fixture，不一定要先重写 runtime。

| adapter | 当前证据 | 必补内容 |
| --- | --- | --- |
| `backpack` | native（2026-06-09 已补结构化字段） | 已补 realtime depth、200/600/1000ms 聚合 depth、bookTicker、`U/u` 和 REST snapshot replay。 |
| `bitmex` | native（2026-06-09 已补结构化字段） | 已补 orderBookL2_25 100ms、orderBook10、orderBookL2 table diff、action/key 和 REST snapshot resync。 |
| `deribit` | native（2026-06-09 已补结构化字段） | 已补 `book.{instrument}.{group}.{depth}.{interval}`、1/10/20、100ms/agg2/raw、change_id/prev_change_id 连续性。 |
| `orangex` | spec_only | 当前官方只见 `raw`，复核旧 100ms 证据来源。 |
| `whitebit` | native（2026-06-09 已补结构化字段） | 已补 100ms 增量、10s snapshot keepalive、`update_id` 连续性、REST snapshot resync。 |
| `woo` | native（2026-06-09 已补结构化字段） | 已补 bbo 10ms、orderbookupdate 200ms、orderbook 1s/100 levels、seq/sequence 和 REST snapshot resync。 |
| `kraken` | native（2026-06-09 已补结构化字段） | 已补 Spot `book` depth 10/25/100/500/1000、CRC32 top10；Futures `book_snapshot`/`book`、`seq` 和 REST snapshot resync。 |
| `coinbase` | native（2026-06-09 已补结构化字段） | 已补 Advanced Trade `level2`、heartbeat、`sequence_num` 连续性、无固定 book ms 和 REST `product_book` resync。 |
| `coinbaseexchange` | native（2026-06-09 已补结构化字段） | 已补 level2_batch 50ms、level2/full/level3 sequence 边界和 REST book snapshot resync。 |

## P2 公共订单簿 WebSocket

这些交易所官方已经确认有公共订单簿 WS 或等价 socket stream，但当前项目没有可直接用于套利的 runtime，或缺关键结构化字段。

| adapter | 官方盘口结论 | 必补内容 |
| --- | --- | --- |
| `ascendex` | `depth:<symbol>` 默认 300ms throttle，`bbo:<symbol>` 变更推送；snapshot 最多 500 档，`seqnum` 必须连续。 | 已补 interval、depth-snapshot/depth-snapshot-top100、BBO parser、seqnum 断档重建和 fixture。 |
| `cex` | Spot Trading WS `order_book_subscribe` 首包 snapshot，后续 `order_book_increment`，`seqId` 顺序应用。 | 已补 ws-public runtime、订阅 payload、snapshot/increment parser、seqId 连续性和 REST snapshot/断线重建。 |
| `coinsph` | bookTicker real-time；partial depth 5/10/20 档可 100ms，200 档 1000ms；diff depth 100ms/1000ms。 | 补 public stream endpoint、bookTicker、partial/diff depth、`U/u` snapshot 重建。 |
| `latoken` | STOMP `/v1/book/{base}/{quote}`，官方未给固定 ms/depth，靠 nonce 连续性检测。 | 已完成：STOMP public book subscribe frame、`nonce` sequence 映射、gap/regression reconnect、无 checksum 风险和 REST `/v2/book/{base}/{quote}` rebuild 已落地。 |
| `p2b` | `depth.subscribe` limit 1-100；increment 每 1s，full flag 每 60s；无 sequence/checksum。 | 已补 depth.update parser、full/incremental 语义、1s/60s 重建策略和无 sequence/checksum 风险字段。 |
| `paymium` | socket.io public stream 推 `bids`/`asks` 价位变更；官方未给固定 ms/depth/sequence。 | 已完成：socket.io v1.3 connect-only public stream spec、`bids_asks` 变更解析、零数量删除、无 sequence/checksum 风险和 REST `/data/eur/depth` 全量重建已落地；runtime env-gated。 |

## P2 明确公共 WS 不支持

这些已经可以写进单交易所文档，避免后续重复核验公共订单簿 WS：

| adapter | 写法 | 说明 |
| --- | --- | --- |
| `bit2c` | `交易所不支持公共 WS 行情` | 当前官方 API 只见 REST public/private。 |
| `bitteam` | `交易所不支持公共 WS 行情` | 当前官方 docs/OpenAPI 只给 REST/CCXT-style endpoints，未给 WSS endpoint 和订单簿订阅规格。 |
| `btcbox` | `交易所不支持公共 WS 行情` | 当前官方 API 资料是 HTTP Public/Private API。 |
| `cod3x` | `交易所不支持公共 WS 行情` | Cod3x-native profile 未建立稳定订单簿 WS API；下游 venue 走各自 adapter。 |
| `coinspot` | `交易所不支持公共 WS 行情` | 当前官方 API/V2 API 是 public/private REST。 |
| `derive_chain_perps` | `交易所不支持公共 WS 行情` | 本 profile 不承接 Derive Exchange WS runtime，避免和 `derive` adapter 重复。 |
| `equation` | `交易所不支持公共 WS 行情` | 当前 audit-only adapter 未见稳定 exchange-gateway 公共订单簿 WS。 |
| `indodax` | `交易所不支持公共 WS 行情` | 官方 PDF/GitHub API 覆盖 Public API 和 TAPI REST。 |
| `yobit` | `交易所不支持公共 WS 行情` | 官方 API v2/v3/TAPI/Defi API 是 REST。 |
| `zebpay` | `交易所不支持公共 WS 行情` | 当前官方 docs 覆盖 Spot/Futures REST market/book/orderBook，未见 WS stream。 |
| `zeta_markets` | `交易所不支持当前公共 WS runtime` | 官方文档声明 Zeta Markets 已于 2025-05 停止运营。 |
| `bitkan` | `交易所不支持公共 WS 行情` | 当前未找到 BitKan 官方稳定 API 文档公开说明订单簿 WebSocket，不启用 unverified runtime。 |
| `d8x` | `交易所不支持当前公共 WS runtime` | 当前只核到 CoinGecko-compatible REST orderbook，未找到稳定官方公共订单簿 WS 文档；payload fixture 不进入实盘 runtime。 |
| `mango_markets` | `交易所不支持当前公共订单簿 WS runtime` | 当前只确认 Solana `accountSubscribe`，不是统一交易所 orderbook channel；OpenBook/Serum 盘口需单独链上 indexer。 |

## P3 区域现货 WS 结构化细项

这些交易所官方已经查到公共订单簿 WS 细项，但多数没有 10ms/20ms；适合在 P0/P1 主流套利行情之后补入结构化 mapping。

| adapter | 官方盘口结论 | 必补内容 |
| --- | --- | --- |
| `bitbank` | `depth_diff`/`depth_whole`；whole 正常模式 200 asks/200 bids；无固定 ms。 | 已完成：`endpoint_mapping.yaml` 写入 Socket.IO join-room、200 档、无固定 ms、非连续 sequence、whole/diff 重建；`streams.rs` 暴露 `public_order_book_ws_policy()`；测试覆盖 policy 与 best-effort delta capability。 |
| `bithumb` | `orderbook` Public WS；snapshot/realtime 开关；无固定 ms。 | 已完成：orderbook request、snapshot/realtime、level 聚合、无 sequence/checksum 风险和 REST snapshot resync 已落地。 |
| `bitopro` | 更新时每秒推全量 order book；默认 5 档，可选 1/5/10/20/30/50。 | 已完成：1s updated full-book snapshot、limit 1/5/10/20/30/50、snapshot-only、无 sequence/checksum 和 REST snapshot resync 已落地。 |
| `bitso` | `orders` top20 和 `diff-orders`；sequence 严格递增。 | 已完成：orders/diff-orders、top20、strict sequence continuity、无 fixed interval/checksum 和 REST snapshot + replay/resync 已落地。 |
| `btcmarkets` | WS v2 `orderbook` 最新状态，最多 50 bids/50 asks；无固定 ms。 | 已完成：50 档、heartbeat 5s、无 sequence/checksum 的 latest-state 语义和 REST snapshot rebuild 已落地。 |
| `coincheck` | `[pair]-orderbook`；发生交易时约 0.1s；无固定 depth。 | 已完成：约 0.1s diff stream、无固定 depth、无 sequence/checksum、zero-amount delete fixture 和 REST snapshot rebuild 已落地。 |
| `coinone` | ORDERBOOK 首包最后订单簿，之后有变更才推；无固定 ms/depth。 | 已完成：ORDERBOOK topic、首包 snapshot、change-only update、order book id freshness、无 checksum 和 REST snapshot resync 已落地。 |
| `independentreserve` | `orderbook-{crypto}` 事件流；Nonce 严格递增；heartbeat 当前 60s。 | 已完成：Subscribe/query 订阅、NewOrder/OrderChanged/OrderCanceled 事件模型、Nonce gap/regression rebuild 已落地。 |
| `luno` | market stream 先推当前订单簿，再尽快推 updates；需要 API key credentials。 | 已完成：market stream URL、credential payload、sequence、全量初始 book、atomic updates 和错序重连已落地。 |
| `upbit` | orderbook units 支持 1/5/15/30；无固定 ms。 | 已完成：region URL、`{code}.{count}`、1/5/15/30、无 sequence/checksum 风险和 REST snapshot resync 已落地。 |

## P3 CEX WS 结构化细项

这些交易所已有 native/spec/payload/parser 证据，但缺套利需要的结构化字段。优先补 `endpoint_mapping.yaml` 的 channel、interval、depth、sequence/checksum 和重建策略。

| adapter | 官方盘口结论 | 必补内容 |
| --- | --- | --- |
| `bitfinex` | `book` 支持 `freq=F0/F1`、`len=1/25/100/250`、`prec=P0-P4`，可选 checksum/sequence/bulk。 | 已完成：`prec/freq/len`、`OB_CHECKSUM`、`SEQ_ALL`、snapshot/update/delete 和 REST snapshot rebuild 已落地。 |
| `bitflyer` | JSON-RPC `lightning_board_snapshot_{product_code}` + `lightning_board_{product_code}`；无固定 ms/depth。 | 已完成：snapshot+diff channel、REST getboard 重建、无 sequence/checksum 风险已落地。 |
| `bitmart` | `spot/depth/increase100` 100ms，`spot/depth5/20/50` 500ms，bookTicker BBO real-time。 | 已完成：depth increase、full depth、bookTicker、`version` 断档重建和无 checksum 风险已落地。 |
| `bitstamp` | WebSocket v2 real-time；项目已记录 `order_book`/`diff_order_book`；官方未给固定 ms/depth。 | 已完成：REST order_book 和 order_data gap recovery、无固定 ms/depth/sequence/checksum 风险已落地。 |
| `cryptocom` | `book.{instrument}.{depth}` delta 10/100ms，depth 10/50，`u/pu` 重建。 | 已完成：`SNAPSHOT_AND_UPDATE`、10ms/100ms、10/50 档、`u/pu` 和 REST snapshot rebuild 已落地。 |
| `phemex` | `orderbook.subscribe`/`orderbook_p.subscribe` 约 20ms，depth 0/1/5/10/30，带 `sequence`。 | 已完成：20ms/120ms/full 100ms、depth 参数、sequence continuity 和 60s snapshot 自校验已落地。 |
| `poloniex` | Spot/Futures V3 `book_lv2` real-time 20 level，`id/lastId` 或 `id/lid`。 | 已完成：20 档、spot/futures sequence 字段、重订阅重建已落地。 |
| `coinex` | `depth.subscribe` 约 200ms，limit 5/10/20/50，带 signed CRC32 checksum。 | 已完成：limit、merge interval、full/incremental、checksum 和 REST rebuild 已落地。 |
| `coinmate` | `order_book-{PAIR}`；官方未给固定 ms/depth/sequence/checksum。 | 已完成：无固定 ms/depth/sequence/checksum 和 REST snapshot fallback 已落地。 |
| `coinmetro` | `pairs` query 订阅 `bookUpdate`，有 `seqNumber` 和 CRC32 checksum；`tick` 含 ask/bid。 | 已完成：query-string subscribe、checksum rounding、BBO tick、无固定 ms/depth 和 REST rebuild 已落地。 |
| `coindcx` | Socket.IO depth update；现货示例 `@orderbook@20`，项目记录 spot/futures `@50`。 | 已完成：Socket.IO join、20/50 depth 边界、无 sequence/checksum、REST snapshot 重建已落地。 |

## P3 衍生品/链上 WS 结构化细项

这些交易所或 profile 已有 native/spec/payload/parser 证据，但缺跨所套利需要的结构化字段。`lighter` 和 `paradex` 有明确 50ms 级别盘口，应在主流 10ms/20ms 任务之后优先处理。

| adapter | 官方盘口结论 | 必补内容 |
| --- | --- | --- |
| `aark` | Orderly profile；`{symbol}@orderbookupdate` 每 200ms，`prevTs` 连续性，无 checksum。 | 已完成：Orderly EVM WS endpoint/account_id、200ms、`ts`/`prevTs` continuity、request/signed REST orderbook snapshot 重建已落地。 |
| `modetrade` | Orderly profile；`{symbol}@orderbookupdate` 每 200ms，`prevTs` 连续性，无 checksum。 | 同 Orderly 共享 mapping，补 account_id path、prevTs 和 snapshot fallback。 |
| `woofipro` | WOOFi Pro perps 走 Orderly；`orderbookupdate` 每 200ms。 | 同 Orderly 共享 mapping，避免和 WOO X/WOOFi Swap 混用。 |
| `apex` | `orderBook{limit}.H.{symbol}` high-frequency topic；limit 25/200；官方未给固定 ms。 | 25/200 档、snapshot+delta、乱序/缺包 snapshot 重建。 |
| `aster` | Binance-style bookTicker/partial/diff depth；100/250/500ms；5/10/20 档；`U/u/pu`。 | depth channel、100ms 档、REST snapshot + delta buffer 重建。 |
| `blofin` | `books` 200 depth 增量 100ms，`books5` 5 depth 100ms；`prevSeqId/seqId`。 | books/books5、100ms、200/5 档、prevSeqId 连续性和断档重建。 |
| `bullish` | `l1Orderbook`/`l2Orderbook` live realtime；有 sequenceNumber，未给固定 ms/depth。 | 已完成 2026-06-08：`endpoint_mapping.yaml`、`subscribe_public_stream` JSON spec、L1/L2 topics、keepalive/reconnect、`sequenceNumber`/`sequenceNumberRange` parser/test 和 REST hybrid snapshot resync metadata 已补。 |
| `derive` | public orderbook/ticker/trades WS；官方未给固定 ms/depth/sequence/checksum。 | 写清无固定 ms/depth/checksum 风险，REST/JSON-RPC snapshot 兜底。 |
| `dydx` | `v4_orderbook` 首包 snapshot 后推 price level 更新；`message_id`/`version`/`clobPairId`。 | v4_orderbook initial/update、断线重新订阅 snapshot 重建。 |
| `hyperliquid` | `l2Book`/`bbo`；REST `l2Book` snapshot 最多 20 levels/side；无传统 sequence/checksum。 | BBO/L2、20 levels REST snapshot、stale book 和 resync 风险字段。 |
| `lighter` | `order_book/{MARKET_INDEX}` 每 50ms；首包 snapshot 后只推变化；nonce 连续；无 checksum。 | 50ms、nonce continuity、gap resubscribe 和高速盘口标记。 |
| `paradex` | order_book 固定 15 levels，refresh_rate 50ms/100ms；`seq_no`。 | 50/100ms、15 levels、feed_type、seq_no 和高速盘口标记。 |

## P3 补充交易所 WS 结构化细项

这些交易所或 profile 已查到公共 orderbook WS、官方 SDK stream 或明确“不应启用”的边界。除 `apollox_dex` 外，多数没有固定 10ms/20ms；实现时重点是把无固定毫秒、无 checksum、snapshot fallback 写清楚。

| adapter | 官方盘口结论 | 必补内容 |
| --- | --- | --- |
| `aftermath` | `/perpetuals/ws/updates` 支持 orderbook deltas；未给固定 ms/depth/sequence/checksum。 | orderbook subscribe/unsubscribe、无固定 ms/depth、REST `getOrderbook` snapshot fallback。 |
| `alpaca` | Crypto Data API 支持 `orderbooks` WS；需 auth 后 subscribe；未给固定 ms/depth/sequence/checksum。 | auth+subscribe、REST latest orderbooks 重建和无 checksum 风险。 |
| `apollox_dex` | Binance-style bookTicker/partial/diff depth；100/250/500ms；5/10/20 档；`U/u/pu`。 | 100ms、5/10/20、bookTicker、REST snapshot + delta buffer 重建。 |
| `arkham` | Arkham Exchange 有 Websocket API，但公开页未给订单簿 channel 细项。 | 补完整 Exchange API reference；未补前不启用 runtime。 |
| `biconomy` | `depth.subscribe`，depth 5/10/15/20/30/50/100，`depth.update` 有 `is_full`。 | depth/precision、full/incremental、无 sequence/checksum。 |
| `bigone` | `subscribeMarketDepthRequest`，snapshot 后 update，`changeId/prevId` 连续。 | MarketDepth、changeId/prevId、protobuf 选项和断档重建。 |
| `bitbns` | 旧官方 SDK 资料无稳定 WS 规格，仅保留 socket 示例线索。 | 记录旧 SDK 风险，保持 spec-only，REST orderbook 重建。 |
| `bitrue` | Spot `market_${symbol}_simple_depth_step0`，gzip payload，server ping 15s。 | channel、gzip、ping 15s、REST depth fallback。 |
| `bittrade` | `market.<symbol>.depth.<type>` step0-step5 和 `market.<symbol>.bbo`；BBO 有 `seqId`。 | depth/BBO、step0-step5、seqId、gzip。 |
| `bitunix` | Futures `depth_books/book1/book5/book15`；未给固定 ms/sequence/checksum。 | futures 1/5/15 档、15s ping、REST depth fallback。 |
| `blockchaincom` | Exchange WS 支持 L2/L3 order book，消息带 `seqnum`。 | `l2`/`l3` subscribe、seqnum、snapshot/update 和 message limit。 |

## P3 补充交易所 WS 结构化细项二

这些交易所或 profile 的第二批补充细项已查到官方订单簿 WS 结构、明确低延迟能力，或明确当前不应启用公共 WS runtime。`bybiteu`、`bsx`、`bydfi`、`delta` 对跨所套利价值更高；`d8x` 按当前 profile 写 `交易所不支持当前公共 WS runtime`，避免把未核验 fixture 推入实盘。

| adapter | 官方盘口结论 | 必补内容 |
| --- | --- | --- |
| `bsx` | `book` channel 订阅后 snapshot，之后 25ms 推增量，每分钟 snapshot；payload 有 `gsn`。 | book channel、25ms、每分钟 snapshot、`gsn` 和 REST product book fallback。 |
| `btcturk` | `orderbook` 全量和 `obdiff` 差量；`CS` ChangeSet；官方未给固定 ms/depth/checksum。 | orderbook/obdiff 订阅、`CS`、REST orderbook 初始化和断档重建。 |
| `bybiteu` | Bybit V5 orderbook：1 档 10ms、50 档 20ms、200 档 100ms、1000 档 200ms；`u/seq/cts`。 | EU host 下 `orderbook.1`/`orderbook.50`、10ms/20ms、snapshot/delta 和 `u/seq/cts`。 |
| `bydfi` | Futures depth/limited depth 支持 1000ms 或 100ms；limited depth 10/50/100；未见 sequence/checksum。 | path stream 和 JSON SUBSCRIBE、100ms、10/50/100 档、REST snapshot fallback。 |
| `coinstore` | Spot depth stream 最小推送间隔 100ms；server message 有 session 序号 `S`；Futures 有 `future_snapshot_depth`。 | Spot depth stream、100ms、session 序号 `S`、futures snapshot depth 和 REST snapshot 重建。 |
| `cointr` | depth channel 200-300ms；`books/books1/books5/books15`；有 CRC32 checksum。 | 200-300ms、1/5/15/full 档、CRC32 checksum 和 snapshot+update 重建。 |
| `cryptomus` | `depth_subscribe` 按 `market:scale_index`；`depth_update` 有 `full_reload`；未给固定 ms/depth/sequence/checksum。 | `depth_subscribe`、`scale_index`、`full_reload`、无 sequence/checksum 风险和 REST orderbook fallback。 |
| `deepcoin` | public WS V2 `book25` 是 25 档增量行情；有 `LocalNo/ResumeNo`；未给固定 ms/checksum。 | `book25`、25 档、`LocalNo/ResumeNo`、REST books `sz` 1..400 snapshot fallback。 |
| `delta` | 新公共 WS `ob_l1` 100ms、`ob_l2` 15 档 500ms、`ob_updates` 100ms；`ob_updates` 有 `seq` 和 CRC32 `cs`。 | `ob_l1/ob_l2/ob_updates`、新 public endpoint、`seq +1` 校验、CRC32 和 resubscribe 重建。 |
| `digifinex` | Spot `depth.subscribe` 推 `depth.update`，首参数 true/false 区分 complete/update；Swap `orderbook.subscribe`。 | depth/orderbook subscribe、complete/update 标记、无固定 ms/depth/checksum 和 REST snapshot fallback。 |
| `exmo` | public WS 支持 `spot/order_book_snapshots` 和 `spot/order_book_updates`；top-25 positions 和 top-400 changes。 | snapshots/updates、25/400、无 sequence/checksum 风险和 REST order_book 重建。 |

## P3 补充交易所 WS 结构化细项三

这些交易所或 profile 的第三批补充细项已经查到官方订单簿 WS 结构、低延迟能力或明确不应启用的 runtime 边界。`gemini`、`hitbtc`、`fmfwio`、`kucoinfutures` 是本批最接近套利可用的 100ms/real-time 候选；`mango_markets` 按当前 profile 写 `交易所不支持当前公共订单簿 WS runtime`。

| adapter | 官方盘口结论 | 必补内容 |
| --- | --- | --- |
| `fmfwio` | HitBTC v3 family：`orderbook/D5-D20/100ms`、`orderbook/top/100ms`、`orderbook/full`；partial 有 sequence `s`。 | D5/D10/D20、top BBO、100/500/1000ms、sequence `s` 和 REST snapshot 重建。 |
| `foxbit` | REST/WS v3 有 public orderbook payload；本批未核到固定 ms/depth/sequence/checksum。 | 无固定 ms/sequence/checksum 风险、depth 参数复核和 REST orderbook fallback。 |
| `gemini` | bookTicker real-time；partial depth 5/10/20 支持 100ms/1s；diff depth 支持 100ms/1s；有 `lastUpdateId/U/u`。 | `bookTicker`、`depth5/10/20@100ms`、`depth@100ms`、snapshot 参数和 `U/u` 重建。 |
| `grvt` | `v1.book.s` 支持 `instrument@rate-depth`，示例 500ms/50 档；有 `sequence_number/prev_sequence_number`。 | `v1.book.s/v1.book.d`、500ms/50 档、sequence/prev_sequence 和 gap resubscribe。 |
| `hashkey_global` | `depth` topic 300ms，最多 200 档；REST depth limit max 200；未见 sequence/checksum。 | depth topic、300ms、200 档、10s ping 和 REST depth fallback。 |
| `hibachi` | market WS `orderbook/{symbol}` payload；官方公开页未给固定 ms/depth/sequence/checksum。 | `orderbook/{symbol}`、无固定 ms/depth/sequence/checksum 风险和 REST orderbook fallback。 |
| `hitbtc` | v3 public WS 支持 `orderbook/D5-D20/100ms`、`orderbook/top/100ms`、`orderbook/full`；partial 有 sequence `s`。 | D5/D10/D20、top BBO、100/500/1000ms、sequence `s` 和 REST snapshot 重建。 |
| `hollaex` | public WS 支持 `orderbook` 和 `orderbook:{symbol}`；未给固定 ms/depth/sequence/checksum。 | `orderbook:{symbol}`、HollaEx 白标 profile 风险、无固定 ms/depth/checksum 和 REST orderbook fallback。 |
| `krakenfutures` | `book` feed 支持 snapshot+delta；消息有 `seq`；未给固定 ms/depth/checksum。 | `book` feed、snapshot/delta、`seq` 连续性和 REST/orderbook 重建。 |
| `kucoinfutures` | increment real-time；level2Depth5/50 100ms；UTA `obu` 支持 BBO real-time、5/50 100ms、increment real-time；有 sequence。 | `/contractMarket/level2`、`level2Depth5/50`、UTA `obu`、100ms 和 sequenceStart/sequenceEnd 重建。 |
| `mercado` | 官方 API v4 说明有 WebSocket API，项目已有 orderbook payload helper；本批未核到固定 ms/depth/sequence/checksum，REST orderbook max 1000。 | WS 入口风险、无固定 ms/depth/sequence/checksum 和 REST `/api/v4/{symbol}/orderbook` fallback。 |

## P3 剩余交易所 WS 结构化细项

这些是公共订单簿 WebSocket 细项队列的最后一批。`myokx`/`okxus` 继承 OKX V5 的 10ms BBO 和 100ms `books5` 能力，但区域 host、凭证资格和 VIP 限制必须单独标注；`oxfun`、`pacifica` 是本批 100ms 候选。

| adapter | 官方盘口结论 | 必补内容 |
| --- | --- | --- |
| `myokx` | OKX V5：`bbo-tbt` 10ms、`books5` 100ms、`books` 100ms；TBT 深度 10ms 有登录/VIP 限制。 | EEA host、`books/books5/bbo-tbt`、10ms/100ms、seq/checksum、REST snapshot 和 VIP 限制。 |
| `ndax` | WSGateway 支持 `SubscribeLevel1`/`SubscribeLevel2` 和 `GetL2Snapshot`；Level2 snapshot 可指定 `Depth`。 | WSGateway envelope、Level1/Level2、Depth 参数、无固定 ms/checksum 风险和 snapshot 重建。 |
| `novadax` | Socket.IO `MARKET.{symbol}.DEPTH.LEVEL0` 每秒推深度数据；未给可选档位/sequence/checksum。 | Socket.IO SUBSCRIBE、DEPTH.LEVEL0、1s、无 sequence/checksum 和重新订阅全量替换。 |
| `okxus` | OKX V5：`bbo-tbt` 10ms、`books5` 100ms、`books` 100ms；TBT 深度 10ms 有登录/VIP 限制。 | U.S. host、`books5/bbo-tbt`、10ms/100ms、seq/checksum、REST snapshot 和区域限制。 |
| `onetrading` | `ORDER_BOOK` snapshot/update 和 `BOOK_TICKER` BBO；官方未给固定 ms/sequence/checksum。 | ORDER_BOOK/BOOK_TICKER、BBO、无固定 ms/sequence/checksum 和重新订阅 snapshot。 |
| `oxfun` | `depthUpdate:<marketCode>` 100ms；snapshot + diff；payload 有 `seqNum` 和 `checksum`。 | 100ms、snapshot + diff、`seqNum`、checksum 和断档 resubscribe。 |
| `pacifica` | `book` 每 100ms；`bbo` 顶层变化推送；`agg_level` 1/10/100/1000/10000；payload 有 `li`。 | book/bbo、100ms、agg_level、`li` ordering、无 checksum 和 REST book fallback。 |
| `tapbit` | Spot/USDT Perpetual orderBook topic 支持 5/10/50/100/200 档；payload `version` 严格递增；未给固定 ms。 | 已完成 2026-06-08：`endpoint_mapping.yaml`、`subscribe_public_stream` spec、stream runtime capability、parser/test 已补 `spot/orderBook`、`usdt/orderBook`、5/10/50/100/200 档、`version` sequence、5s ping 和 REST snapshot fallback。 |
| `tokocrypto` | MBX streams 支持 partial depth 5/10/20 和 diff depth；1000ms 或 100ms；diff 有 `U/u`。 | `depth5/10/20@100ms`、`depth@100ms`、`U/u`、REST `lastUpdateId` snapshot 重建。 |
| `toobit` | `depth` snapshot 300ms 且双边 300 档；`diffDepth` 每秒；USDT-M `wholeBookTicker` BBO；版本 `v` 不保证唯一。 | depth/diffDepth/wholeBookTicker、300ms/1s、300 档、版本风险和 REST depth fallback。 |
| `wavesexchange` | WX matcher order book updates 更新间隔 100ms；订阅参数 `d` 指定 depth；消息有 update id `U`。 | `obs/obu`、100ms、depth `d`、update id `U`、订阅上限和无 checksum 风险。 |
| `weex` | depth channel 支持 15/200；订阅后自动 snapshot，后续增量；payload 有 `U/u` 和 `SNAPSHOT/CHANGED`。 | 已完成：`endpoint_mapping.yaml` 写入 `<symbol>@depth15/200`、`U/u`、`SNAPSHOT/CHANGED`、缺包重订阅和无固定 ms 风险；`streams.rs` 暴露 depth policy/channel builder，parser 将 `u` 写入标准 sequence。 |
| `xt` | Spot/Futures `depth_update` 支持 REST snapshot + diff 重建；Spot 用 `fi/i`，Futures 用 `fu/u`。 | `depth_update` topic、1000ms 证据、snapshot limit 500、`fi/i` 与 `fu/u` 连续性。 |
| `zaif` | WebSocket 提供实时板信息 asks/bids、trades 和 last_price；未给固定 ms/depth/sequence/checksum。 | `stream?currency_pair`、无固定 ms/depth/sequence/checksum、重连后全量状态或 REST depth 重建。 |

## P2 核心交易项目未实现/未启用

这些交易所官方支持下单、撤单、查单、open orders 或 fills readback，但当前项目至少还有一个核心交易 operation 仍没有可安全启用的 runtime。已部分运行的 adapter 只跟踪剩余 operation，不能把整套 lifecycle 写成 `交易所不支持下单`。

已完成/运行态记录：`weex` Spot/Perp place/cancel/batch/cancel-all/query/open/fills 已在矩阵中为运行态；`hitbtc`、`fmfwio` 的 Spot place/cancel/query/open/fills 已接 guarded HS256 private REST runtime；`latoken` 的 place/cancel/cancel-all/query/open/fills 和 batch place/cancel 已接 guarded HMAC private REST runtime；`bybiteu`、`myokx`、`okxus` 区域 profile 已改为 env-gated 复用 Bybit/OKX private REST runtime；`backpack`、`bithumb`、`coindcx`、`coinw`、`huobi`、`independentreserve`、`okx`、`orangex` 已按已有 guarded private REST/request-parser evidence 补 `support: native`，矩阵不再停留在 `映射`；`coinspot`、`indodax` 的 side-context 风险已改为明确不支持。

Core-A 复核后，以下传统 CEX adapter 已固定为 `spec_only`/source-boundary 离线证据。认证、KYC 或 API key 要求不是“不支持”理由；当前阻塞是 request builder、parser、dry-run guard、post-write readback reconciliation 或 profile runtime 未启用。

| adapter | 当前离线 operation | 必补内容 |
| --- | --- | --- |
| `bitbank` | `place_order`、`cancel_order` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 guarded signed GET runtime；写操作仍只保留 request-spec，需补显式 live-write/dry-run guard、ack/readback parser 和 post-write reconciliation 后再启用。 |
| `bitflyer` | `place_order`、`cancel_order`、`cancel_all_orders` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 guarded read-only HMAC REST runtime；写操作仍只保留 child-order place/cancel/cancel-all request-spec，需补显式 live-write/dry-run guard、ack/readback parser 和 post-write reconciliation 后再启用。 |
| `bittrade` | `place_order`、`cancel_order` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `BITTRADE_PRIVATE_REST_ENABLED` + API key/secret guarded read-only runtime；写操作仍只保留 request-spec，需补显式 live-write/dry-run guard、ack/cancel parser 和 post-write reconciliation。 |
| `btcturk` | `place_order`、`cancel_order` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `BTCTURK_PRIVATE_REST_ENABLED` + API key/secret guarded read-only runtime；写操作仍只保留 request-spec，需补显式 live-write/dry-run guard、ack/cancel parser 和 post-write reconciliation。 |
| `luno` | `place_order`、`cancel_order` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `LUNO_PRIVATE_REST_ENABLED` + Basic auth guarded read-only runtime；写操作仍只保留 request-spec，需补显式 live-write/dry-run guard、ack/cancel parser 和 post-write reconciliation。 |
| `tokocrypto` | `place_order`、`cancel_order` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `TOKOCRYPTO_PRIVATE_REST_ENABLED` + API key/secret guarded Binance-style read-only runtime；写操作仍只保留 request-spec，需补显式 live-write/dry-run guard、ack/cancel parser、listen-key/private stream reconciliation 和 post-write reconciliation。 |
| `zaif` | `place_order`、`cancel_order` | `get_open_orders`、`get_recent_fills` 已接 guarded TAPI HMAC read-only runtime；写操作仍只保留 trade/cancel request-spec，需补显式 live-write/dry-run guard、ack parser、query_order 无损语义复核和 post-write reconciliation 后再启用。 |

Core-B 复核后，以下传统/区域 CEX private REST adapter 已补官方 request-spec、source 或 response fixture，矩阵写 `离线`；当前仍不能提升 live runtime，原因是 credential/profile guard、签名烟测、order/fill parser、写后 readback reconciliation 或 live dry-run gate 不完整。

| adapter | 当前离线 operation | 必补内容 |
| --- | --- | --- |
| `bit2c` | `place_order`、`cancel_order` | `query_order`、`get_open_orders` 已接 guarded HMAC signed GET runtime；写操作仍缺 live-write/dry-run guard、ack/cancel parser 和 post-write readback reconciliation。 |
| `bitbns` | `place_order`、`cancel_order` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `BITBNS_PRIVATE_REST_ENABLED`/`RUSTCTA_BITBNS_PRIVATE_REST_ENABLED` + API key/secret guarded read-only runtime；写操作仍缺 live-write/dry-run guard、KYC/region guard、ack/cancel parser 和 post-write reconciliation。 |
| `bitso` | `place_order`、`cancel_order` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `BITSO_PRIVATE_REST_ENABLED` + API key/secret guarded HMAC read-only runtime；写操作仍缺 live-write/dry-run guard、ack/cancel parser、unknown-order policy 和 post-write reconciliation。 |
| `bitteam` | `place_order`、`cancel_order` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `BITTEAM_PRIVATE_REST_ENABLED` + Basic auth guarded read-only runtime；写操作仍只保留 request-spec，需补显式 live-write/dry-run guard、ack/cancel parser 和 post-write reconciliation。 |
| `blockchaincom` | `place_order`、`cancel_order`、`cancel_all_orders` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `BLOCKCHAINCOM_PRIVATE_REST_ENABLED` + API token guarded read-only runtime；写操作与 cancel-all 仍缺 live-write/dry-run guard、ack/cancel parser、过度撤单安全策略和 post-write reconciliation。 |
| `btcbox` | `place_order`、`cancel_order` | `query_order`、`get_open_orders` 已接 `BTCBOX_PRIVATE_REST_ENABLED` + API key/secret guarded HMAC form POST runtime；写操作仍缺显式 live-write/dry-run guard、ack/cancel parser 和 post-write readback reconciliation。 |
| `cex` | `place_order`、`cancel_order`、`cancel_all_orders` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `CEX_PRIVATE_REST_ENABLED` + API key/secret/user id guarded read-only runtime；写操作与 cancel-all 仍只保留 request-spec，需补显式 live-write/dry-run guard、ack/cancel parser、过度撤单安全策略和 post-write reconciliation。 |
| `coinmate` | `place_order`、`cancel_order`、`cancel_all_orders` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `COINMATE_PRIVATE_REST_ENABLED` + client id/public key/private key guarded read-only runtime；写操作与 cancel-all 仍只保留 request-spec，需补显式 live-write/dry-run guard、nonce serialization、ack/cancel parser、过度撤单安全策略和 post-write reconciliation。 |
| `foxbit` | `place_order`、`cancel_order` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `FOXBIT_PRIVATE_REST_ENABLED` + API key/secret guarded read-only runtime；写操作仍只保留 request-spec，需补显式 live-write/dry-run guard、ack/cancel parser、cursor/pagination 和 post-write reconciliation。 |
| `hollaex` | `place_order`、`cancel_order`、`cancel_all_orders` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `HOLLAEX_PRIVATE_REST_ENABLED`/`RUSTCTA_HOLLAEX_PRIVATE_REST_ENABLED` + API key/secret guarded read-only runtime；写操作与 cancel-all 仍缺 live-write/dry-run guard、white-label profile guard、ack/cancel parser、过度撤单安全策略和 post-write reconciliation。 |
| `mercado` | `place_order`、`cancel_order` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 guarded bearer private REST runtime；写操作仍缺 live-write/dry-run guard、ack/readback parser 完整闭环和 post-write reconciliation。 |
| `ndax` | `place_order`、`cancel_order` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `NDAX_PRIVATE_REST_ENABLED` + API key/secret/user/account/OMS guarded signed read-only runtime；写操作仍缺 live-write/dry-run guard、ack/cancel parser、private WS reconciliation 和 post-write reconciliation。 |
| `novadax` | `cancel_all_orders` | `place_order`、`cancel_order`、`query_order`、`get_open_orders`、`get_recent_fills` 已接 `NOVADAX_PRIVATE_REST_ENABLED` + API key/secret guarded HMAC private REST runtime；`cancel_all_orders` 仍保持离线，需补 symbol/account guard、过度撤单安全策略、parser 和 reconciliation 后再启用。 |
| `onetrading` | `place_order`、`cancel_order`、`cancel_all_orders` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `ONETRADING_PRIVATE_REST_ENABLED` + bearer token guarded read-only runtime；写操作与 cancel-all 仍缺 live-write/dry-run guard、ack/cancel parser、best-effort cancel-all reconciliation 和 post-write reconciliation。 |
| `p2b` | `place_order`、`cancel_order` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `P2B_PRIVATE_REST_ENABLED`/`P2PB2B_PRIVATE_REST_ENABLED` + API key/secret guarded read-only runtime；写操作仍只保留 request-spec，需补显式 live-write/dry-run guard、ack/cancel parser 和 post-write reconciliation。 |
| `paymium` | `place_order`、`cancel_order` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `PAYMIUM_PRIVATE_REST_ENABLED` + API key/secret guarded read-only runtime；写操作仍只保留 request-spec，需补显式 live-write/dry-run guard、ack/cancel parser 和 post-write reconciliation。 |
| `yobit` | `place_order`、`cancel_order` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `YOBIT_PRIVATE_REST_ENABLED`/`RUSTCTA_YOBIT_PRIVATE_REST_ENABLED` + TAPI key/secret guarded HMAC-SHA512 read-only runtime；写操作仍缺 live-write/dry-run guard、Trade/Cancel ack/parser 和 post-write reconciliation。 |
| `zebpay` | `place_order`、`cancel_order`、`cancel_all_orders` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `ZEBPAY_PRIVATE_REST_ENABLED`/`RUSTCTA_ZEBPAY_PRIVATE_REST_ENABLED` + client id/client secret/access token guarded read-only runtime；写操作与 cancel-all 仍缺 live-write/dry-run guard、region/profile guard、ack/cancel parser、cancel-all safety 和 post-write reconciliation。 |

Core-C 复核后，以下 DEX/链上/session/特殊签名 adapter 仍只到 `spec_only` 或 source-boundary 离线证据。认证、钱包签名或会话要求不是“不支持”理由；只有 profile 委托、停服、无稳定 API 或 shared request 无损映射不成立时才放入不支持段。

| adapter | 当前离线 operation | 必补内容 |
| --- | --- | --- |
| `aark` | `place_order`、`cancel_order` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `RUSTCTA_AARK_PRIVATE_REST_ENABLED` + Orderly account/key/secret guarded signed read-only runtime；写操作仍缺 Aark account onboarding、trade permission guard、ack/cancel parser、post-write reconciliation 和 live-dry-run guard。 |
| `modetrade` | `place_order`、`cancel_order` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `RUSTCTA_MODETRADE_PRIVATE_REST_ENABLED` + Orderly account/key/secret guarded signed read-only runtime；写操作仍缺 ModeTrade/Orderly account onboarding、trade permission guard、ack/cancel parser、post-write reconciliation 和 dry-run guard。 |
| `bsx` | `place_order`、`cancel_order` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `RUSTCTA_BSX_PRIVATE_REST_ENABLED` + API key/secret/wallet guarded read-only runtime；写操作仍缺 EIP-712/session signing、safe cancel shape、sequence/nonce guard、ack/cancel parser 和 post-write reconciliation。 |
| `grvt` | `place_order`、`cancel_order`、`cancel_all_orders` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `GRVT_PRIVATE_REST_ENABLED`/`RUSTCTA_GRVT_PRIVATE_REST_ENABLED` + session cookie/account id guarded read-only runtime；写操作与 cancel-all 仍缺 session renewal、external signer、sub-account scope、partial parser、write/readback reconciliation 和 live guard。 |
| `hibachi` | `place_order`、`cancel_order`、`cancel_all_orders` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `HIBACHI_PRIVATE_REST_ENABLED` + API key/account id guarded read-only runtime；写操作与 cancel-all 仍缺 contract-id/scale/nonce handling、ack/cancel parser、partial-failure reconciliation 和 dry-run guard。 |
| `lighter` | `place_order`、`cancel_order`、`cancel_all_orders` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `LIGHTER_PRIVATE_REST_ENABLED`/`RUSTCTA_LIGHTER_PRIVATE_REST_ENABLED` + bearer token/account index guarded read-only runtime；写操作与 cancel-all 仍缺 SDK-compatible signed tx builder、API-key private key/index/nonce、partial parser、post-write reconciliation 和 broadcast dry-run guard。 |
| `aftermath` | `place_order`、`cancel_order`、`query_order`、`get_open_orders`、`get_recent_fills` | Sui transaction/account source 已有离线边界；补 wallet/account-cap signer、transaction builder、account/order decoder、checkpoint freshness 和 indexer/readback reconciliation。 |
| `d8x` | `place_order`、`cancel_order`、`query_order`、`get_open_orders`、`get_recent_fills` | EVM wallet/contract/indexer source 已有离线边界；补 contract ABI pinning、wallet signer, nonce/gas/allowance guard、order/fill parser、reorg handling 和 reconciliation。 |
| `mango_markets` | `place_order`、`cancel_order`、`query_order`、`get_open_orders`、`get_recent_fills` | Solana Mango/OpenBook source 已有离线边界；补 MangoAccount/OpenBook decoder、transaction builder、slot/reorg policy、open-orders/fills parser 和 reconciliation。 |
| `apex` | `place_order`、`cancel_order`、`cancel_all_orders` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `APEX_PRIVATE_REST_ENABLED`/`RUSTCTA_APEX_PRIVATE_REST_ENABLED` + API key/secret/passphrase guarded read-only runtime；写操作与 cancel-all 仍缺 zkLink/L2 signer、account guard、ack/cancel parser、cancel-all safety 和 dry-run reconciliation。 |
| `apollox_dex` | `place_order`、`cancel_order`、`cancel_all_orders` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `RUSTCTA_APOLLOX_DEX_PRIVATE_REST_ENABLED` + API key/secret guarded read-only runtime；写操作与 cancel-all 仍缺 account/permission scope、request/body normalization、ack/cancel parser、listen-key/private stream reconciliation 和 dry-run guard。 |
| `arkham` | `place_order`、`cancel_order`、`cancel_all_orders` | `query_order`、`get_open_orders`、`get_recent_fills` 已接 `ARKHAM_PRIVATE_REST_ENABLED`/`RUSTCTA_ARKHAM_PRIVATE_REST_ENABLED` + API key/secret guarded read-only runtime；写操作与 cancel-all 仍缺 account scope、ack/cancel parser、cancel-all safety、private readback reconciliation 和 dry-run guard。 |
| `bullish` | `place_order`、`cancel_order`、`cancel_all_orders`、`get_open_orders`、`get_recent_fills` | Bullish command/private read specs 已固定；补 JWT lifecycle、nonce-window guard、command response parser、open/fills parser、reconciliation 和 dry-run guard。 |
| `derive` | `place_order`、`cancel_order` | Derive JSON-RPC/session source 已有离线边界；补 session/JWT/Stark signer、shared request full replace/create fields、order response parser、readback reconciliation 和 live guard。 |
| `dydx` | `place_order`、`cancel_order` | dYdX v4 chain/indexer source 已有离线边界；补 wallet signer、account number/sequence、chain id/subaccount guard、tx builder、broadcast dry-run 和 Indexer reconciliation。 |
| `oxfun` | `place_order`、`cancel_order` | Private WS/REST source 已有离线边界；补 authenticated private WS session lifecycle、request tag/order-id reconciliation、ack parser、REST/private-stream readback 和 dry-run guard。 |
| `pacifica` | `place_order`、`cancel_order`、`cancel_all_orders`、`get_open_orders` | Agent-wallet/session source 已有离线边界；补 account scope、agent-wallet signer、shared position/order mapping、private REST/WS reconciliation 和 cancel-all safety guard。 |
| `paradex` | `place_order`、`cancel_order` | JWT/Stark account source 已有离线边界；补 signer/session lifecycle、market/account scope、order response parser、open-order reconciliation 和 dry-run guard。 |
| `wavesexchange` | `place_order`、`cancel_order`、`cancel_all_orders`、`query_order`、`get_open_orders`、`get_recent_fills` | WX matcher signed order/source boundary 已固定；补 Waves signer、matcher fee/asset-pair normalization、cancel-all safety, order/fill parser、height/reorg handling 和 reconciliation。 |

## P2 核心交易当前交易所不支持

这些不是项目缺口，而是当前官方状态不应接 live trading runtime。

| adapter | 写法 | 说明 |
| --- | --- | --- |
| `zeta_markets` | `交易所不支持当前交易/私有接口 runtime` | 官方说明 Zeta Markets 已于 2025-05 停止运营，Final Epoch 写明 2025-05-01 04:00 UTC 后 trading disabled；只保留 legacy public scan。 |
| `bitkan` | `交易所不支持当前交易/私有接口 runtime` | 当前未核到稳定官方 BitKan-native exchange trading API、签名规则和订单生命周期文档；不要启用 unverified helper。 |
| `cod3x` | `交易所不支持当前交易/私有接口 runtime` | Cod3x 是 AI perps terminal/routing layer；下游 Hyperliquid/GMX/Lighter 走各自 adapter，不并入 `cod3x`。 |
| `coinspot` | `交易所不支持当前 cancel/query shared runtime` | CoinSpot cancel/query 依赖 buy/sell side 或历史上下文；shared request 缺 side/order-context，不能无损选择原生路径。 |
| `derive_chain_perps` | `交易所不支持当前 profile 交易/私有接口 runtime` | 本 profile 只记录 Derive Chain settlement/profile boundary；Derive Exchange API runtime 归 `derive` adapter。 |
| `indodax` | `交易所不支持当前 cancel_order shared runtime` | Indodax `cancelOrder` 必须带 buy/sell side；shared `CancelOrderRequest` 没有 side 字段，已禁用默认 side，避免错侧撤单。 |
| `equation` | `交易所不支持当前交易/私有接口 runtime` | 官方资料是 Equation perpetual protocol / Arbitrum contract examples；未给稳定 exchange-gateway 交易 API。 |
| `aark` | `交易所不支持当前 cancel-all/order-list runtime` | 单笔下单/撤单/query/open/fills 已为 Orderly signed `离线` 边界；cancel-all/order-list 没有当前无损 shared runtime。 |
| `aftermath` | `交易所不支持当前 cancel-all/order-list runtime` | Sui 交易下单/撤单/readback 已写离线边界；当前未确认可安全映射 shared cancel-all/order-list。 |
| `alpaca` | `交易所不支持当前 cancel-all 或 fills runtime` | 当前 profile 不承接对应 shared core operation。 |
| `bithumb` | `交易所不支持当前 cancel-all 或 fills runtime` | 当前 profile 不承接对应 shared core operation。 |
| `bsx` | `交易所不支持当前 cancel-all/order-list runtime` | 单笔 place/cancel/query/open/fills 已为 signed `离线` 边界；cancel-all/order-list 未确认无损 shared 语义。 |
| `coincheck` | `交易所不支持当前 cancel-all 或 fills runtime` | 当前 profile 不承接对应 shared core operation。 |
| `d8x` | `交易所不支持当前 cancel-all/order-list runtime` | EVM signed place/cancel/readback 已写离线边界；cancel-all/order-list 需要合约语义审计，当前不支持。 |
| `kcex` | `交易所不支持当前交易/私有接口 runtime` | 当前 profile 不承接稳定共享核心交易 runtime。 |
| `mango_markets` | `交易所不支持当前 cancel-all/order-list runtime` | Solana place/cancel/readback 已写离线边界；cancel-all/order-list 需要 open-orders 扫描和原子交易审计。 |
| `modetrade` | `交易所不支持当前 cancel-all/order-list runtime` | 单笔下单/撤单/query/open/fills 已为 Orderly signed `离线` 边界；cancel-all/order-list 没有当前无损 shared runtime。 |
| `upbit` | `交易所不支持当前 cancel-all 或 fills runtime` | 当前 profile 不承接对应 shared core operation。 |

## P2 仓位项目未实现/未启用

已完成/运行态或原生映射记录：`bybiteu` 复用 Bybit V5 guarded signed REST `/v5/position/list` runtime，补 `limit=50` cursor pagination、position parser 和 EU-host request-spec tests；`huobi` legacy profile 复用 HTX guarded signed REST `/linear-swap-api/v1/swap_cross_position_info` runtime，补 `huobi` exchange id、signed query/body、parser 和 request-spec tests；`backpack`、`coindcx`、`coinw`、`okx`、`orangex` 已有 credential-gated/read-only positions runtime，mapping 补 `support: native`、request-spec 和 parser fixture 证据，矩阵不再列入 P2 仓位缺口。默认无 credentials/enable flag 时仍 fail-closed，未伪造 live 成功。

这些交易所官方支持仓位、保证金或账户持仓读取，当前已补 request-spec、SDK/account object 或链上/indexer source 离线边界；剩余缺口是 parser、签名器、会话授权、链上账户扫描和 read-only runtime，不能直接视作实盘运行。

| adapter | 官方仓位结论 | 必补内容 |
| --- | --- | --- |
| `aark` | Orderly EVM `GET /v1/positions` 返回 margin ratio、free collateral、position_qty、mark price、liq price、funding、margin_mode 等字段；signed-read fixture 已补。 | Ed25519 signing、positions parser、margin mode/position side 映射和 read-only reconciliation。 |
| `modetrade` | ModeTrade 是 Orderly EVM profile，同源 Orderly `GET /v1/positions` 可作为账户仓位来源；signed-read fixture 已补。 | Ed25519 signing、positions parser、margin mode/position side 映射和 read-only reconciliation。 |
| `aftermath` | SDK account/account objects 返回 positions、available collateral、total equity，并按 account cap / account id 查询；Sui account source fixture 和 source-boundary parser 测试已补，矩阵仍为 `get_positions=离线`。 | Sui account cap/account id runtime、account object decoder、available collateral/total equity 映射、checkpoint freshness 和链上/indexer 对账。 |
| `d8x` | 链上 perpetual futures engine，仓位读取需要 wallet/contract/indexer 或 SDK 账户状态解析；wallet/contract/indexer source fixture 和 source-boundary parser 测试已补，矩阵仍为 `get_positions=离线`。 | EVM wallet/indexer account scan、contract/indexer freshness、positions parser、funding/PnL/liquidation fields、reorg handling 和 reconciliation。 |
| `grvt` | API docs 有 positions request schema，trading/auth 资料使用 session/account 模型；positions session spec fixture 已补。 | session-cookie auth、positions parser、account/sub-account scope、private WS/readback reconciliation。 |
| `lighter` | SDK/API `AccountApi.account` 返回 account data；账号/sub-account、auth token、WS account updates 依赖 signer/auth token；account positions fixture 已补。 | read-only auth token、positions parser、sub-account index 映射和 WS/REST reconciliation。 |
| `mango_markets` | MangoAccount 跟踪 PerpPosition，包含 base lots、quote native、funding settlement、market index 等；Solana/MangoAccount source fixture 和 source-boundary parser 测试已补，矩阵仍为 `get_positions=离线`。 | Solana/MangoAccount ownership/account decoding、PerpPosition parser、health/PnL/funding fields、slot/reorg/indexer reconciliation。 |
| `apex` | ApeX `GET /api/v3/account` account/positions request-spec 和 sanitized positions fixture 已补，矩阵仍为 `get_positions=离线`。 | HMAC auth smoke、zkLink/account scope guard、positions parser、margin/side 字段映射和 REST/private-WS reconciliation。 |
| `apollox_dex` | ApolloX DEX `/fapi/v2/positionRisk` signed futures position request-spec 已补，矩阵仍为 `get_positions=离线`。 | signed private REST guard、position parser、account scope policy 和 REST/user-stream reconciliation。 |
| `arkham` | Arkham `/account/positions` HMAC request-spec 已补，矩阵仍为 `get_positions=离线`。 | HMAC auth smoke、spot/perp account scope guard、positions parser 和 REST/private-stream reconciliation。 |
| `bullish` | Bullish `/trading-api/v1/derivatives-positions` bearer/JWT request-spec 已补，矩阵仍为 `get_positions=离线`。 | JWT/session lifecycle、tradingAccountId scope guard、derivatives position parser 和 REST/private-stream reconciliation。 |
| `bsx` | BSX account-scoped position/margin-state source-boundary fixture 已补，矩阵改为 `get_positions=离线`；稳定 REST path 仍需审计。 | stable endpoint path audit、BSX account headers、read-only permission scope、position parser 和 private REST/WS reconciliation。 |
| `oxfun` | OX.FUN private account/positions/funding 资料有 position readback 线索；已补 `get_positions=离线` private account positions source fixture。 | private positions endpoint 复核、auth smoke、position parser、funding/margin 字段归一和 reconciliation。 |
| `zeta_markets` | 原 Zeta venue 已停止运营，但历史 positions readback 属 wallet-owned Solana margin account state；已补 `get_positions=离线` margin-account source boundary，不要求交易所 API key。 | legacy SDK/account decoder audit、public-key scope guard、perp lot/quote-native parser、slot/reorg policy 和 shutdown-safe reconciliation。 |
| `pacifica` | Pacifica `/api/v1/positions` 与 `account_positions` stream 有仓位读取线索，当前已绑定 request-spec 和 REST/WS parser fixtures，但 adapter runtime 仍显式 `pacifica.positions_request_spec_only`。 | agent-wallet/account scope、read-only source guard、shared `ExchangePosition` 映射、REST/private WS reconciliation 和 freshness/last-order-id 对账。 |

## P2 仓位当前交易所不支持

这些不是项目缺口，而是当前官方资料或当前 profile 不应接 live position runtime。

| adapter | 写法 | 说明 |
| --- | --- | --- |
| `bitkan` | `交易所不支持当前仓位接口 runtime` | 当前未核到稳定官方 BitKan-native exchange API、签名规则和仓位生命周期文档。 |
| `cod3x` | `交易所不支持当前仓位接口 runtime` | Cod3x 是 perps terminal/routing layer；下游 venue 的 positions 走各自 adapter。 |
| `derive_chain_perps` | `交易所不支持当前仓位接口 runtime` | Derive Exchange API runtime 归 `derive` adapter；本 profile 不重复接 Derive account/position runtime。 |
| `equation` | `交易所不支持当前仓位接口 runtime` | Equation 当前 adapter 是 audit-only EVM protocol profile，不启用稳定 private account/position runtime。 |

## P3 账户/余额项目未实现/未启用

这些交易所官方或当前 adapter 官方资料已经有账户/余额 readback 线索，但项目还没有启用 private read runtime。

已完成/运行态记录：`bybiteu` 复用 Bybit V5 guarded signed REST `/v5/account/wallet-balance`；`myokx`、`okxus` 复用 OKX V5 guarded signed REST `/api/v5/account/balance`；`blockchaincom` 复用 Exchange v3 API-token `/accounts`；`cryptomus` 复用 signed `/v2/user-api/exchange/account/balance`；`hitbtc`、`fmfwio` 复用 HitBTC-family guarded HS256 `GET /api/3/spot/balance`；`latoken` 复用 guarded signed `/v2/auth/account?zeros=false`；`backpack`、`coindcx`、`coinw`、`okx`、`orangex` 已补 native balance mapping evidence 和 request/parser fixture 指针，当前矩阵为 `原生`。默认无 credentials/enable flag/API key/secret 时仍 fail-closed，未伪造 live 成功。

| adapter | 官方账户/余额结论 | 必补内容 |
| --- | --- | --- |
| `bit2c` | 已补 `GET /Account/Balance` HMAC request-spec，矩阵 `get_balances=离线`；认证/私有 REST 要求不等于不支持。 | 剩官方 balance response 样例复核、HMAC read-only runtime、balance parser、credential scope guard 和 REST reconciliation。 |
| `bitso` | 已补 `GET /api/v3/balance` HMAC request-spec 和响应样例绑定，矩阵 `get_balances=离线`。 | 剩 HMAC auth smoke、balance parser 归一、fiat/crypto account scope guard 和 reconciliation。 |
| `bitteam` | 已补 `GET /trade/api/ccxt/balance` Basic-auth request-spec 和响应样例，矩阵 `get_balances=离线`。 | 剩 Basic auth read-only runtime、balance parser、credential validation 和 REST reconciliation。 |
| `btcbox` | 已补 `POST /api/v1/balance` HMAC request-spec，矩阵 `get_balances=离线`；私有签名要求不等于不支持。 | 剩官方 balance response 样例复核、HMAC read-only runtime、JPY/crypto balance parser、coin scope guard 和 reconciliation。 |
| `coinmate` | 已补 `POST /balances` signed request-spec 和响应样例绑定，矩阵 `get_balances=离线`。 | 剩 nonce/signature runtime guard、balance parser 归一、account scope policy 和 reconciliation。 |
| `foxbit` | 已补 `GET /balances` HMAC request-spec 和响应样例绑定，矩阵 `get_balances=离线`。 | 剩 HMAC auth smoke、balance parser 归一、account scope guard 和 REST/private-stream reconciliation。 |
| `mercado` | 已补 `GET /api/v4/accounts/{accountId}/balances` bearer request-spec 和响应样例绑定，矩阵 `get_balances=离线`。 | 剩 bearer read-only guard、account-id scope policy、balance parser 和 reconciliation。 |
| `ndax` | 已补 `GET /accounts` signed request-spec，矩阵 `get_balances=离线`；OMS/account 认证要求不等于不支持。 | 剩 account permission audit、fiat ledger response/parser fixture、OMS account scope policy 和 reconciliation。 |
| `novadax` | 已补 `GET /v1/account/getBalance` HMAC request-spec 和响应样例绑定，矩阵 `get_balances=离线`。 | 剩 HMAC read-only auth smoke、balance parser 归一、permission guard 和 reconciliation。 |
| `onetrading` | 已补 `GET /account/balances` bearer request-spec，矩阵 `get_balances=离线`。 | 剩 bearer token scope guard、balance response/parser fixture、spot/futures product eligibility 和 reconciliation。 |
| `p2b` | 已补 `POST /api/v2/account/balances` signed request-spec 和响应样例，矩阵 `get_balances=离线`。 | 剩 signed private read runtime、balances parser、read-only auth smoke 和 REST reconciliation。 |
| `yobit` | 已补 TAPI `getInfo` HMAC-SHA512 request-spec 和响应样例，矩阵 `get_balances=离线`。 | 剩 TAPI HMAC-SHA512 auth smoke、getInfo parser 和 account reconciliation。 |
| `zebpay` | 已补 `GET /wallet/balance?trade_pair=...` bearer-token request-spec 和响应样例，矩阵 `get_balances=离线`。 | 剩 bearer token read-only auth、wallet balance parser 和 onboarding/region guard。 |
| `hollaex` | 已补 `GET /v2/user/balance` HMAC request-spec/signing fixture 和响应样例，矩阵 `get_balances=离线`。 | 剩 HMAC read-only runtime、`/v2/user/balance` parser、白标 profile guard 和 reconciliation。 |
| `bitbns` | 已补 `currentCoinBalance` private request-spec、HMAC header/payload 形状和响应样例，矩阵 `get_balances=离线`。 | 剩 private signing promotion、balance parser、KYC/region guard 和 reconciliation。 |
| `cex` | 已补 CEX.IO legacy `/balance/` request-spec、HMAC body 形状和响应样例，矩阵 `get_balances=离线`。 | 剩 private balance parser、HMAC auth smoke 和 fiat/trading scope guard。 |
| `paymium` | 已补 Paymium `GET /api/v1/user` user-info balance request-spec 和 BTC/EUR 响应样例，矩阵 `get_balances=离线`。 | 剩 API-token/OAuth auth promotion、BTC/EUR balance parser 和 read-only scope guard。 |
| `bitflyer` | 已补 `GET /v1/me/getbalance` HMAC request-spec 和余额响应样例，矩阵 `get_balances=离线`。 | 剩 shared balance parser、product/account scope guard 和 readback reconciliation；当前代码仍返回 `bitflyer.get_balances.rest_parser_pending`。 |
| `bittrade` | 已补 account-scoped `GET /v1/account/accounts/{account-id}/balance` HMAC request-spec，矩阵 `get_balances=离线`。 | 剩 account-id discovery、balance parser、read-only runtime promotion 和 reconciliation；当前代码仍返回 `bittrade.get_balances.offline_request_spec_only`。 |
| `btcturk` | 已补 `GET /api/v1/users/balances` signed request-spec，矩阵 `get_balances=离线`。 | 剩 signed read-only runtime、balance parser 和 account reconciliation；当前代码仍返回 `btcturk.balances_offline_request_spec_only`。 |
| `luno` | 已补 `GET /api/1/balance` Basic-auth request-spec，矩阵 `get_balances=离线`。 | 剩 balance parser、read-only runtime promotion 和 reconciliation；当前代码仍返回 `luno.balances_offline_request_spec_only`。 |
| `tokocrypto` | 已补 `GET /open/v1/account/spot` signed account request-spec，矩阵 `get_balances=离线`。 | 剩 balance parser、read-only runtime promotion 和 reconciliation；当前代码仍返回 `tokocrypto.get_balances.offline_request_spec_only`。 |
| `zaif` | 已补 TAPI `get_info2` HMAC-SHA512 request-spec，矩阵 `get_balances=离线`。 | 剩 balance parser、read-only runtime promotion 和 fiat/trading account scope guard；当前代码仍返回 `zaif.get_balances.offline_request_spec_only`。 |
| `apex` | 已补 `GET /api/v3/account-balance` HMAC request-spec，矩阵 `get_balances=离线`；需要认证不等于不支持。 | 剩 HMAC auth smoke、zkLink/account scope guard、balance parser 和 REST/account reconciliation。 |
| `apollox_dex` | 已补 `GET /fapi/v2/balance` signed futures balance request-spec，矩阵 `get_balances=离线`；DEX/signed private readback 不判为不支持。 | 剩 signed private REST guard、balance parser、account scope policy 和 REST/user-stream reconciliation。 |
| `arkham` | 已补 `GET /account/balances` HMAC request-spec，矩阵 `get_balances=离线`；Arkham private account balance readback 不是不支持。 | 剩 HMAC auth smoke、spot/perp account scope guard、balance parser 和 reconciliation。 |
| `bullish` | 已补 `GET /trading-api/v1/accounts/asset` bearer/JWT request-spec，矩阵 `get_balances=离线`；JWT/session 要求不等于不支持。 | 剩 JWT/session lifecycle、tradingAccountId scope guard、asset balance parser 和 REST/private-stream reconciliation。 |
| `aark` | 已补 `GET /v1/client/holding` signed read request-spec，矩阵 `get_balances=离线`；余额/抵押品读取依赖 Aark/Orderly account scope。 | 剩 Aark/Orderly account onboarding audit、Ed25519 signing runtime、balance/collateral parser、credential scope guard 和 read-only reconciliation。 |
| `modetrade` | 已补 `GET /v1/client/holding` signed read request-spec，矩阵 `get_balances=离线`；ModeTrade/Orderly 余额/抵押品读取依赖 signed account scope。 | 剩 ModeTrade/Orderly account onboarding audit、Ed25519 signing runtime、balance/collateral parser、credential scope guard 和 REST/WS reconciliation。 |
| `aftermath` | Aftermath Sui perpetual account object/SDK 可读取 collateral、equity 和账户状态；已补 `get_balances=离线` account-cap/account-id source fixture。 | Sui account cap/account id runtime、collateral/equity parser 和链上/indexer 对账。 |
| `bsx` | 已补 account-scoped balance/equity source-boundary fixture，矩阵 `get_balances=离线`；稳定 REST path 仍需审计。 | 剩 stable endpoint path audit、BSX account headers、read-only permission scope、balance parser 和 private REST/WS reconciliation。 |
| `d8x` | D8X 链上 perpetual 账户状态需要 wallet/contract/indexer 或 SDK 读取保证金、权益和账户风险；已补 `get_balances=离线` wallet/contract/indexer source fixture。 | EVM wallet/indexer runtime、margin/equity parser、reorg handling 和 reconciliation。 |
| `grvt` | 已补 session/account balance source/request boundary，矩阵 `get_balances=离线`；GRVT 私有账户 readback 需要 session/auth scope。 | 剩 session-cookie auth smoke、account/sub-account scope guard、balance parser 和 private WS/readback reconciliation。 |
| `lighter` | 已补 `AccountApi.account` / `GET /account` balance/equity request-spec，矩阵 `get_balances=离线`；余额/权益读取依赖 API key index、auth token 和 sub-account。 | 剩 bearer token smoke、account/sub-account parser、precision mapping 和 WS/REST reconciliation。 |
| `mango_markets` | MangoAccount 跟踪 collateral、borrows、health、perp account state；已补 `get_balances=离线` Solana/MangoAccount source fixture。 | Solana/MangoAccount runtime decoding、collateral/borrow/health parser、slot/reorg/indexer reconciliation。 |
| `paradex` | 已补 `GET /balance` request-spec 并记录 private WS `user.balance`，矩阵 `get_balances=离线`。 | 剩 JWT/Stark auth guard、balance parser 和 private WS `user.balance` REST reconciliation。 |
| `wavesexchange` | 已补 Waves address/node balance source-boundary fixture，矩阵 `get_balances=离线`；链上地址读取不要求交易所 API key。 | 剩 address/public-key scope、asset precision parser、chain latency/reorg policy 和 matcher/account reconciliation。 |
| `derive_chain_perps` | Derive Chain wallet/address settlement balance 可由链上/indexer 路径读取；已补 `get_balances=离线` chain wallet settlement source fixture，Derive Exchange account runtime 仍委托 `derive` adapter。 | chain/indexer runtime source、asset precision parser、reorg handling 和与 `derive` profile 的边界。 |
| `equation` | EVM wallet/indexer 可读取抵押品、账户权益或协议账户状态；已补 `get_balances=离线` Arbitrum wallet collateral source fixture。 | wallet/indexer runtime source、collateral/equity parser、chain/reorg handling 和 reconciliation。 |
| `bitkan` | 交易所账户余额天然存在；已补 `get_balances=离线` account/balance API 待核验 source fixture，不启用 live private REST。 | 官方 account/balance API 复核、签名规则、balance parser 和 read-only guard。 |
| `kcex` | 交易所账户余额天然存在；已补 `get_balances=离线` OpenAPI 待核验 source fixture，不启用 live private REST。 | 官方 account/balance OpenAPI 复核、签名规则、balance parser 和 read-only reconciliation。 |
| `oxfun` | OX.FUN private account/funding 资料有 account/equity 线索；已补 `get_balances=离线` private account equity source fixture。 | private account/equity endpoint 复核、auth smoke、balance/equity parser 和 reconciliation。 |
| `pacifica` | Pacifica account/order/position streams 和 agent-wallet account 模型有账户状态线索；已补 `get_balances=离线` agent-wallet account equity source fixture。 | account/equity runtime source、agent-wallet/address scope、parser 和 private WS reconciliation。 |
| `cod3x` | Cod3x 是 terminal/routing profile，但余额 readback 可来自选定下游 venue account 或 wallet/account source；已补 `get_balances=离线` downstream-or-wallet source boundary。 | routed venue/wallet account selection、read-only scope guard、asset precision/collateral parser 和 downstream/chain reconciliation。 |
| `zeta_markets` | 原 Zeta venue 已停止运营，但历史余额/权益 readback 属 wallet-owned Solana margin account state；已补 `get_balances=离线` margin-account source boundary，不要求交易所 API key。 | legacy SDK/account decoder audit、public-key scope guard、asset precision/collateral parser、slot/reorg policy 和 shutdown-safe reconciliation。 |

## P3 账户/余额当前 profile 不支持

余额 readback 不因认证、wallet/account-id、node/indexer 或下游 venue source 而判死；当前没有 adapter 留在本段。

| adapter | 写法 | 说明 |
| --- | --- | --- |

## P3 费率项目未实现/未启用

这些 adapter 已有官方或项目证据支持 maker/taker、commission、产品费率、账户费率或协议费率来源，但还没有稳定进入标准 `get_fees` runtime；已有 request-spec、signing vector、response fixture、费率表/config source 或协议源 fixture 的 adapter 在矩阵中写 `get_fees=离线`，后续只按 parser/config loader/account-effective guard 提升 runtime。

已完成/运行态或原生映射记录：`ascendex`、`aster`、`backpack`、`bequant`、`biconomy`、`bigone`、`binance`、`binancecoinm`、`binanceus`、`bingx`、`bitget`、`bitmex`、`bitrue`、`bitstamp`、`bitvavo`、`bithumb`、`blockchaincom`、`blofin`、`btcmarkets`、`bybit`、`bybiteu`、`bydfi`、`coinbase`、`coinbaseexchange`、`coinex`、`coinone`、`coinsph`、`coinspot`、`coinstore`、`cointr`、`coinw`、`cryptocom`、`cryptomus`、`deepcoin`、`delta`、`deribit`、`digifinex`、`exmo`、`fmfwio`、`gateio`、`hashkey_global`、`hibachi`、`hitbtc`、`htx`、`huobi`、`kraken`、`kucoin`、`kucoinfutures`、`latoken`、`lbank`、`mexc`、`myokx`、`okx`、`okxus`、`orangex`、`phemex`、`poloniex`、`tapbit`、`toobit`、`upbit`、`weex`、`whitebit`、`woo`、`xt` 的 `get_fees` 当前矩阵已为 `运行` 或 `原生`。

| adapter | 官方费率结论 | 必补内容 |
| --- | --- | --- |
| `aark` | Orderly/Aark `GET /v1/broker/user_info` 用户费率来源已补 signed read request-spec，矩阵 `get_fees=离线`。 | 补 builder/account scope 审计、Ed25519 签名读、maker/taker parser 和 shared `FeeRateSnapshot` runtime。 |
| `apollox_dex` | ApolloX DEX `/fapi/v1/commissionRate` 已补 signed request-spec 和 commission-rate response fixture，矩阵 `get_fees=离线`。 | 补 read-only credential guard、symbol/category parser、account scope policy 和 REST/user-stream reconciliation 后再提升 runtime。 |
| `arkham` | Arkham `/account/fees` 已补 HMAC request-spec 和 spot/perp fee response fixture，矩阵 `get_fees=离线`。 | 补 HMAC auth smoke、spot/perp account scope guard、maker/taker parser 和 REST/private-stream reconciliation。 |
| `bitbank` | `/spot/pairs` pair fee metadata source-boundary fixture 已补，矩阵 `get_fees=离线`。 | 补 pair fee parser、account-effective tier/scope guard 和 shared `FeeRateSnapshot` runtime；当前代码仍返回 `bitbank.get_fees.rest_parser_pending`。 |
| `bitflyer` | `/v1/me/gettradingcommission` signed request-spec 和 commission response fixture 已补，矩阵 `get_fees=离线`。 | 补 product_code scope policy、single-rate maker/taker normalization、parser 和 account-effective guard；当前代码仍返回 `bitflyer.get_fees.no_stable_standard_mapping`。 |
| `bitmart` | 官方 Spot/Futures VIP maker/taker fee schedule 已补离线 config source fixture，矩阵 `get_fees=离线`。 | fee schedule refresh、VIP/account-effective tier source、Spot/Contract product-scope guard；默认 source 不能用于生产 effective fee。 |
| `bitopro` | public limitations-and-fees/VIP schedule 已补离线 config source fixture，矩阵 `get_fees=离线`。 | VIP/config loader、pair scope、account-effective override 和 `FeeRateSnapshot` 映射。 |
| `bsx` | BSX 官方/账户侧可见 account app `Fee Rate` source，且认证 headers 已核；当前没有稳定 REST fee endpoint path，已补 source-boundary fixture，矩阵 `get_fees=离线`。 | 稳定 fee endpoint 或配置 source、account permission audit、maker/taker parser 和 `FeeRateSnapshot` runtime；未完成前配置兜底。 |
| `bullish` | Bullish trading-account `tradeFeeRate`/market `feeGroupId` 来源已补 request-spec，矩阵 `get_fees=离线`。 | 补 JWT/session lifecycle、tradingAccountId/feeGroup scope guard、maker/taker parser 和 REST/private-stream reconciliation。 |
| `dydx` | dYdX Chain fee tier 参数源已补 chain/config source fixture，矩阵 `get_fees=离线`。 | account/subaccount tier、height guard、chain/indexer reconciliation 和 `FeeRateSnapshot` 转换。 |
| `bitso` | 官方 `GET /api/v3/fees` 返回每个 book 的 customer maker/taker trading fee；signed request-spec、signing vector 和 response fixture 已补，矩阵 `get_fees=离线`。 | 补 maker/taker parser、book 到 symbol 映射、read-only credential guard，再提升 shared `get_fees` runtime。 |
| `btcturk` | 官方 BtcTurk Global fee structure 给出 Market Maker/Taker tier schedule；public fee table/config boundary fixture 已强化，矩阵 `get_fees=离线`。 | 补 fee table config loader/refresh policy、region/profile guard 和 explicit tier override；如无 API readback 保持配置型费率 source。 |
| `cex` | 官方 Spot Trading API 有 `POST /get_my_current_fee`；Legacy REST 有 `POST /get_myfee/`；两类 request-spec/source fixture 已补，矩阵 `get_fees=离线`。 | 补 `tradingFee` parser、legacy buy/sell maker/taker parser、percent 归一和 Spot Trading/legacy profile guard，再提升 shared runtime。 |
| `coinmate` | Coinmate `/traderFees` signed request-spec 已补，矩阵 `get_fees=离线`。 | 补 nonce/signature guard、currencyPair scope、maker/taker parser 和 shared `FeeRateSnapshot` runtime。 |
| `grvt` | 官方 help center active maker/taker fee tier model 已强化离线 source fixture，矩阵 `get_fees=离线`。 | 补 fee tier config source、session/account scope audit；如接 account-effective fee 需 private readback parser。 |
| `hyperliquid` | 官方 fee schedule/user tier source 已补离线协议/config source fixture，矩阵 `get_fees=离线`；代码侧已关闭 `supports_fees`，避免默认 schedule 被当成 account-effective runtime。 | user fee tier readback、asset scope、discount/referral/staking guard；默认 schedule 仅限 backtest/config。 |
| `krakenfutures` | 官方 fee schedule volumes source 已补离线 private/source fixture，矩阵 `get_fees=离线`。 | private auth smoke、fee schedule volume parser、product fee-schedule mapping 和 read-only guard。 |
| `lighter` | 官方 fee/support 资料说明 standard account maker/taker 为 0，premium/API/HFT 账户可能自定义；zero/default 配置源边界 fixture 已强化，矩阵 `get_fees=离线`。 | 补 premium override 配置、account tier guard、account/sub-account scope guard；不要把未验证 auth token 当 readback。 |
| `luno` | `/api/1/fee_info` Basic-auth request-spec 和 fee-info response fixture 已补，矩阵 `get_fees=离线`。 | 补 pair-scoped maker/taker parser、account-effective guard 和 shared `FeeRateSnapshot` runtime；当前代码仍返回 `luno.fees_offline_request_spec_only`。 |
| `mango_markets` | Mango group/market fee params 已补协议 source fixture，矩阵 `get_fees=离线`。 | group/market fee 参数 decoding、MangoAccount context、slot/reorg/indexer 对账。 |
| `mercado` | Mercado Bitcoin API v4 `GET /api/v4/{asset}/fees` 已补 bearer request-spec，矩阵 `get_fees=离线`。 | 补 bearer read-only guard、asset/symbol mapping、maker/taker parser 和 shared runtime。 |
| `modetrade` | Orderly/ModeTrade `GET /v1/broker/user_info` 用户费率来源已补 signed read request-spec，矩阵 `get_fees=离线`。 | 补 builder/account scope 审计、Ed25519 签名读、maker/taker parser 和 shared `FeeRateSnapshot` runtime。 |
| `novadax` | 官方 fees/limits 页面给出 VIP maker/taker table；fee table/config source boundary fixture 已强化，矩阵 `get_fees=离线`。 | 补 fee table refresh、VIP level mapping、account-effective fee 边界；成交 fee amount/role 不等同 fee-rate runtime。 |
| `onetrading` | One Trading `/account/fees` bearer request-spec 已补，矩阵 `get_fees=离线`。 | 补 token scope guard、fee parser、spot/futures product eligibility 和 reconciliation。 |
| `yobit` | public `/api/3/info` pair metadata `fee` 字段已补离线 source fixture，矩阵 `get_fees=离线`。 | public info fee parser、single-rate maker/taker policy、symbol scope 和 `FeeRateSnapshot` 映射。 |
| `zebpay` | ZebPay `/tradefees/{trade_pair}` 已补 private request-spec，矩阵 `get_fees=离线`。 | 补 bearer/signature auth guard、maker/taker parser、onboarding/region guard 和 shared runtime。 |

## P3 费率当前 profile 不支持

这些当前不能作为稳定共享 `get_fees` runtime。实现交易前应使用默认/配置费率，并把 source 写清。

| adapter | 写法 | 说明 |
| --- | --- | --- |
| `aftermath` | `交易所不支持当前费率接口 runtime` | Sui perpetual profile 未建立可映射成 gateway account fee 的稳定来源。 |
| `alpaca` | `交易所不支持当前费率接口 runtime` | Broker crypto fee schedule 不是共享模型需要的 per-symbol trading-fee endpoint。 |
| `apex` | `交易所不支持当前费率接口 runtime` | 未核到可映射成共享 `FeeRateSnapshot` 的稳定 private fee-tier endpoint。 |
| `bit2c` | `交易所不支持当前费率接口 runtime` | 当前 private REST including fees 明确不启用。 |
| `bitbns` | `交易所不支持当前费率接口 runtime` | private fees 被关闭，未完成账户 readback 验证。 |
| `bitfinex` | `交易所不支持当前费率接口 runtime` | 当前仓库没有稳定 Bitfinex fee runtime；零费率/占位不能当 API fee。 |
| `bitkan` | `交易所不支持当前费率接口 runtime` | 未核到稳定官方 BitKan-native fee/account API。 |
| `bitteam` | `交易所不支持当前费率接口 runtime` | private REST 为 request-spec-only，capabilities 默认把 fees 标为 unsupported。 |
| `bittrade` | `交易所不支持当前费率接口 runtime` | 官方/API 资料未核到 account-effective maker/taker fee endpoint；fee 页面不能直接映射 runtime readback。 |
| `bitunix` | `交易所不支持当前费率接口 runtime` | public/private fee endpoint 未验证，当前只返回零费率占位。 |
| `btcbox` | `交易所不支持当前费率接口 runtime` | 当前资料未验证到稳定 BTCBOX fee endpoint。 |
| `cod3x` | `交易所不支持当前费率接口 runtime` | Cod3x-native 不提供本地 fee runtime；费用属于下游 venue。 |
| `coincheck` | `交易所不支持当前费率接口 runtime` | 当前 profile 只记录 fees unsupported boundary。 |
| `coinmetro` | `交易所不支持当前费率接口 runtime` | 当前 Coinmetro profile 未映射稳定 fee endpoint。 |
| `coindcx` | `交易所不支持当前费率接口 runtime` | fee endpoint 未确认，当前只返回零费率占位。 |
| `d8x` | `交易所不支持当前费率接口 runtime` | 当前 profile 未建立稳定账户/链上 fee runtime。 |
| `derive` | `交易所不支持当前费率接口 runtime` | fee 信息只是 adapter-specific metadata，未归一到 shared fee snapshot。 |
| `derive_chain_perps` | `交易所不支持当前费率接口 runtime` | 本 profile 不承接 Derive Exchange fee runtime。 |
| `equation` | `交易所不支持当前费率接口 runtime` | audit-only EVM protocol profile，无稳定 gateway fee runtime。 |
| `foxbit` | `交易所不支持当前费率接口 runtime` | 当前已核资料没有稳定 trading fee-rate endpoint；成交手续费金额不能等同 maker/taker rate readback。 |
| `gemini` | `交易所不支持当前费率接口 runtime` | 当前 shared spot runtime 中 fees 明确 Unsupported。 |
| `hollaex` | `交易所不支持当前费率接口 runtime` | 官方 demo/API profile 未见稳定 maker/taker trading fee-rate endpoint；任意白标 venue 费率不同。 |
| `indodax` | `交易所不支持当前费率接口 runtime` | 当前 Indodax TAPI profile 不暴露稳定 fee runtime。 |
| `kcex` | `交易所不支持当前费率接口 runtime` | 官方交易页可见 fee 文案，但稳定 OpenAPI endpoint、签名和 fee readback 未验证；当前 adapter 明确 `fees_unverified_openapi`。 |
| `ndax` | `交易所不支持当前费率接口 runtime` | 当前 adapter/API 证据只到 public/user API、risk tiers 和 offline private specs，未核到 maker/taker fee readback。 |
| `oxfun` | `交易所不支持当前费率接口 runtime` | private account/orders/funding 仍未验证，未核到稳定 fee endpoint。 |
| `p2b` | `交易所不支持当前费率接口 runtime` | 当前 private fees unsupported，未验证 maker/taker fee readback。 |
| `pacifica` | `交易所不支持当前费率接口 runtime` | agent-wallet account fee 字段未映射，当前只稳定到 public market/book。 |
| `paradex` | `交易所不支持当前费率接口 runtime` | fee 未映射到 shared trait，private JWT/Stark gated。 |
| `paymium` | `交易所不支持当前费率接口 runtime` | 当前 public adapter 未见 fee 文档。 |
| `tokocrypto` | `交易所不支持当前费率接口 runtime` | fee readback 仅保留 offline request-spec 边界，未提升为 runtime。 |
| `wavesexchange` | `交易所不支持当前费率接口 runtime` | matcher asset rates 不是 account-scoped maker/taker fee contract。 |
| `woofipro` | `交易所不支持当前费率接口 runtime` | builder/admin fee routes 不进入普通账户 runtime。 |
| `zaif` | `交易所不支持当前费率接口 runtime` | 当前 Zaif profile 明确不提供稳定 `get_fees` runtime。 |
| `zeta_markets` | `交易所不支持当前费率接口 runtime` | 原 Zeta venue 已停止运营；legacy adapter 不补 live private fee runtime。 |

## P3 费率仍需官方复核

第三批后费率官方复核已清零。后续只剩高级订单能力 `advanced_trading_official_check` 队列。

## P2 产品线项目未实现

这些不能写成 `交易所不支持`。本段以矩阵 `official_gap_check=项目未实现 *_product` 为准：可以是当前 adapter 只接合约/永续而缺 Spot/OpenBook，也可以是 Spot adapter 的标准合约写 `交易所不支持合约`，但 Margin、Leveraged、第三方 Perps、Event Contracts 等独立产品线仍是项目未实现。

Spot CEX adapter 若有官方 Futures/Perpetual/Derivatives/Margin/CFD 线索，必须同时审计 contract metadata、合约盘口、position/risk、private order lifecycle 四组证据；只存在现货 runtime、产品页、合约费率来源或离线 fee request-spec 时，仍保持 `project_unimplemented_product_line`，不能提升为完整合约 runtime。

| adapter | 官方支持 | 当前缺口 | 建议边界 |
| --- | --- | --- | --- |
| `bsx` | Spot + perpetuals | 当前只声明 perpetual；`spot_product` 已写 `project_unimplemented`，并绑定 product-line source-boundary fixture、current_scope/runtime_gap/required_endpoints。 | 如新增 BSX Spot profile，先核 spot products/order book/account/order/open/fills；当前 runtime 不接 Spot，不能写不支持。 |
| `lighter` | Spot market stats / spot market 结构 | 当前只声明 perpetual；`spot_product` 已写 `project_unimplemented`，并绑定 product-line source-boundary fixture、spot market/index/tx/account 缺口。 | 补 spot market stats/metadata、spot order book/trades、spot `sendTx`、nonce/sub-account parser。 |
| `mango_markets` | Spot/OpenBook 线索 | 当前只声明 margin/perpetual；`spot_product` 已写 Spot/OpenBook `project_unimplemented`，并绑定 product-line source-boundary fixture。 | 补 Mango group spot markets、OpenBook market/bids/asks/event queue decoding、MangoAccount spot open-orders、slot/reorg/indexer 对账。 |
| `apex` | Spot account/wallet 线索 | 当前只声明 perpetual；`spot_product` 已写 `project_unimplemented`，并绑定 product-line source-boundary fixture、spot account/wallet/order lifecycle 缺口。 | 补 spot symbols/order book、spot account/wallet reads、spot place/cancel/query/open/fills、zkLink/L2 signer 适配。 |
| `apollox_dex` | ApolloX spot API family | 当前只声明 perpetual；`spot_product` 已写 `project_unimplemented` 并绑定 product-line source-boundary fixture，当前只接 `fapi` V1 futures/perp。 | 补 spot API host、exchangeInfo/depth/ticker/trades、signed spot account/order/open/fills 和 private-stream/listen-key 对账。 |
| `aster` | Spot API + Futures API | 当前只声明 perpetual；`spot_product` 已写 `project_unimplemented` 并绑定 product-line source-boundary fixture，当前只接 Futures V3。 | 补 spot REST/WS host、exchangeInfo/depth/ticker/trades、signed account/order/open/fills、listen-key/private stream。 |
| `blofin` | Spot trading | 当前只声明 perpetual；`spot_product` 已写 `project_unimplemented` 并绑定 product-line source-boundary fixture，当前只核 SWAP/perp OpenAPI。 | 补 stable spot instruments/books/tickers/trades、spot balances/order/open/fills 和 parser fixtures。 |
| `delta` | Spot + Futures + Options | 当前只声明 futures/option/perpetual；`spot_product` 已写 `project_unimplemented` 并绑定 product-line source-boundary fixture，当前只映射 derivatives。 | 补 spot product filter、spot books/tickers、spot wallet/account、spot place/cancel/query/open/fills。 |
| `deribit` | Spot Instruments / `kind=spot` | 当前只声明 futures/option/perpetual；`spot_product` 已写 `project_unimplemented` 并绑定 product-line source-boundary fixture。 | 补 `public/get_instruments kind=spot`、spot book/ticker parser、spot private order/open/user-trade/account lifecycle。 |
| `derive` | Spot trading + options/perpetuals | 当前只声明 option/perpetual；`spot_product` 已写 `project_unimplemented` 并绑定 product-line source-boundary fixture，当前不混用 `derive_chain_perps`。 | 补 spot instrument filter、orderbook/ticker/trades、balances、create/cancel/query/open/fills。 |
| `dydx` | Solana spot trading 已上线 | 当前只声明 perpetual；`spot_product` 已写 `project_unimplemented` 并绑定 product-line source-boundary fixture，当前只接 v4 perpetual Indexer/Node。 | 补 Solana spot market discovery、spot order book/indexer source、wallet tx routing、spot order/fill parser、slot/reorg 对账。 |
| `hyperliquid` | `spotMeta`、spot asset ctx、HIP-1 spot order books | 当前只声明 perpetual；`spot_product` 已写 `project_unimplemented` 并绑定 product-line source-boundary fixture。 | 补 `spotMeta`/spot asset id mapping、HIP-1 spot `l2Book`、spot order/cancel action encoding、open/fills/balance reconciliation。 |
| `paradex` | Spot Trading | 当前只声明 option/perpetual；`spot_product` 已写 `project_unimplemented` 并绑定 product-line source-boundary fixture。 | 补 spot markets filter、spot orderbook、spot order/cancel/query/open/fills、spot balance/account reconciliation。 |
| `bitflyer` | Crypto CFD / FX | `spot_project=声明`；`contract_product/cfd_fx_product=项目未实现` source-boundary 已补并列 required endpoints | 保留 CFD/FX 为 adapter-specific margin/contract-model 缺口；先补 product_code contract model、合约盘口、position/margin/risk、order lifecycle，不把 `FX_BTC_JPY` 暴露成标准 futures/perpetual。 |
| `bitstamp` | Derivatives endpoints | `spot_project=声明`；`contract_product/derivatives_product=项目未实现` source-boundary 已补并列 unsafe promotion 条件 | Derivatives/contracts profile 与 Spot 隔离；剩 metadata、derivatives order book、positions/margin/funding、signed order lifecycle 和 dry-run guard。 |
| `coinex` | Futures API | `spot_project=声明`；`contract_product/futures_product=项目未实现` source-boundary 已补并列 futures required endpoints | CoinEx Futures/Perpetual 不复用 Spot v2 runtime；剩 futures metadata/depth、leverage/risk、positions 和 signed order lifecycle。 |
| `gemini` | Derivatives / Perpetuals | `spot_project=声明`；`contract_product/derivatives_product/perpetual_product=项目未实现` source-boundary 已补并列 eligibility guard | 已明确区域/产品资格 guard；剩 derivatives metadata/order book、account/position/risk parser 和 private order runtime。 |
| `hitbtc` | Futures margin account endpoints | Spot runtime + offline futures fee source；`contract_product/margin_product/futures_product=项目未实现` source-boundary 已补并列 required endpoints | Futures fee spec 仅为离线来源证据，不代表 futures runtime；剩 product-scope guard、metadata/book、positions/risk 和 private order reconciliation。 |
| `bitbank` | Margin status/positions | `spot_project=声明`；`contract_project=不支持`；`official_gap_check=项目未实现 margin_product` | 已重审强化：`unsupported.margin_product` 写 `status=project_unimplemented`、`official_gap=margin_status_positions`、`boundary=project_unimplemented_product_line`、required audit gaps，并绑定带 classification 的 source-boundary fixture；标准 futures/perpetual/options 仍写 `contract_product=unsupported`。 |
| `bitso` | Margin Trading [WIP] | `spot_project=声明`；`contract_project=不支持`；`official_gap_check=项目未实现 margin_product` | 已重审强化：`unsupported.margin_product` 写 `official_gap=margin_trading_wip`、required audit gaps 和官方 Margin Trading source-boundary classification；标准合约仍写 `contract_product=unsupported`。 |
| `independentreserve` | Leveraged trading | `spot_project=声明`；`contract_project=不支持`；`official_gap_check=项目未实现 leveraged_product` | 已重审强化：`unsupported.leveraged_product` 写 `official_gap=leveraged_trading`、required audit gaps，并绑定 source-boundary classification；标准 futures/perpetual/options 仍写 `contract_product=unsupported`。 |
| `bequant` | Margin/Futures/Perpetual | `spot_project=声明`；`contract_product/margin_product/futures_product=项目未实现` source-boundary 已补并列 required endpoints | Bequant Spot profile 与 Margin/Futures/Perpetual runtime 隔离；剩 endpoint specs、合约盘口、positions/margin/risk parser 和 WS/REST reconciliation。 |
| `biconomy` | Futures/Perpetual 产品页 | `spot_project=声明`；`contract_product/futures_product=项目未实现` source-boundary 已补并列 unsafe promotion 条件 | Spot REST/WS 不推断 futures runtime；先核验稳定 endpoint spec，再补 contract metadata/order book、leverage/positions/risk 和 order parser。 |
| `bitbns` | Margin Trading API | `spot_project=声明`；`contract_project=不支持`；`official_gap_check=项目未实现 margin_product` | 已重审强化：`unsupported.margin_product` 写 `official_gap=margin_trading_api`、required audit gaps，并绑定 source-boundary classification；标准合约仍写 `contract_product=unsupported`。 |
| `blockchaincom` | Perps/第三方接口边界 | `spot_project=声明`；`contract_project=不支持`；`official_gap_check=项目未实现 perps_third_party_product` | 已重审强化：`perps_third_party_product` 写 `project_unimplemented`、`official_gap=app_perps_third_party_interface`、`scope=separate_from_exchange_spot_adapter`、required audit gaps，并绑定 source-boundary classification；Exchange v3 标准合约仍 unsupported。 |
| `coinmetro` | Margin/Swap/TRAM-like product boundary | `spot_project=声明`；`contract_project=不支持`；`official_gap_check=项目未实现 margin_product` | 已重审强化：`unsupported.margin_product` 写 `official_gap=margin_swap_tram_like_product_boundary`、required audit gaps，并绑定 source-boundary classification；标准合约仍写 `contract_product=unsupported`。 |
| `exmo` | Margin | `spot_project=声明`；`contract_project=不支持`；`official_gap_check=项目未实现 margin_product` | 已重审强化：`unsupported.margin_product` 写 `official_gap=exmo_margin_api`、required audit gaps，并绑定 source-boundary classification；`perpetual_product`/`futures_product` 明确 unsupported，perps 上线后重核。 |
| `fmfwio` | Margin/Futures/Perpetual | Spot runtime + offline futures fee source；`contract_product/margin_product/futures_product=项目未实现` source-boundary 已补并列 required endpoints | Futures fee spec 仅为离线来源证据，不代表 futures runtime；剩 product-scope guard、metadata/book、positions/risk 和 private order reconciliation。 |
| `latoken` | Futures endpoint spec 线索 | `spot_project=声明`；`contract_product/futures_product=项目未实现` source-boundary 已补 | Spot REST/STOMP 不根据产品页线索启用 futures；剩 official futures endpoint spec、auth、metadata、positions/risk 和 guard。 |
| `myokx` | Margin/Swap/Futures/Options/Events | `spot_project=声明`；`contract_project=项目未实现`；`official_gap_check=项目未实现 contract/event/margin/market_type_*` | 已重审强化：Margin、SWAP、FUTURES、OPTION、Event Contracts 都写 `project_unimplemented_product_line`，绑定带 classification 的 `product_line_source_boundary.json`，并列出 EEA domain、credential scope、product eligibility、account-mode、metadata/order lifecycle/reconciliation audit gaps；等待 EEA scope/credential audit 或委托主 `okx` adapter。 |
| `ndax` | Margin account semantics | `spot_project=声明`；`contract_project=不支持`；`official_gap_check=项目未实现 margin_product` | 已重审强化：`unsupported.margin_product` 写 `status=project_unimplemented`、`official_gap=margin_account_semantics`、`boundary=project_unimplemented_product_line`，并绑定带 classification 的 `product_line_source_boundary.json` required audit gaps；`perpetual_product`/`futures_product`/`options_product` 是明确 unsupported operation。 |
| `okxus` | Perpetual Futures/Event Contracts/Margin boundary | `spot_project=声明`；`contract_project=项目未实现`；`official_gap_check=项目未实现 contract/event/futures/margin/perpetual product` | 已重审强化：OKX US Margin、Perpetual、Futures、Event Contracts 和 non-spot market scope 均写 `project_unimplemented_product_line`，绑定带 classification 的 `product_line_source_boundary.json`，并列出 US domain、credential scope、state eligibility、account-mode、metadata/order lifecycle/reconciliation audit gaps。 |
| `onetrading` | Futures/Perpetual | `spot_project=声明`；`contract_product/futures_product=项目未实现` source-boundary 已补，fixture 已列 required endpoints/runtime gaps | 当前继续强制 `type=SPOT`；剩 futures/perp instrument/order-book mapping、positions/margin/fees、order lifecycle、token permission audit 和 reconciliation。 |
| `zebpay` | Futures/Perpetual | `spot_project=声明`；`contract_product/futures_product=项目未实现` source-boundary + `capabilities_v2` runtime-disabled placeholders 已补 | Scan-only Spot adapter 与 Futures/Perpetual 隔离；fixture 已列 required market metadata、public market data、margin/positions/risk 和 order/open/fills；剩 API onboarding、futures endpoint specs、positions/margin parser、bearer-token dry-run 和 post-write reconciliation。 |

## P2 明确交易所不支持

这些已经可以写进单交易所文档，避免后续重复核验：

| adapter | 写法 | 说明 |
| --- | --- | --- |
| `binancecoinm` | `交易所不支持现货` | COIN-M adapter 只对应 Binance COIN-M derivatives API。 |
| `kucoinfutures` | `交易所不支持现货` | KuCoin Futures adapter 只对应 futures API。 |
| `krakenfutures` | `交易所不支持现货` | Kraken Futures adapter 只对应 futures API。 |
| `coinbaseexchange` | `交易所不支持合约` | Coinbase Exchange API 是 spot exchange API。 |
| `binanceus` | `交易所不支持合约` | 本轮官方 API 资料只见 Spot；后续如官方开放合约需重核。 |
| `cod3x` | `交易所不支持现货` | Cod3x 是 AI perps trading terminal；下游 venue 不并入 Cod3x adapter。 |
| `derive_chain_perps` | `交易所不支持现货` | 该 adapter 是 Derive Chain perpetual profile；Derive 平台其他产品另走后续任务。 |
| `equation` | `交易所不支持现货` | Equation V3 官方定位为 perpetual protocol。 |
| `zeta_markets` | `交易所不支持现货`；`交易所不支持当前交易/私有接口/公共 WS runtime` | 官方说明 Zeta Markets 已于 2025-05 停止运营。 |
| `aark` | `交易所不支持现货` | Aark Perpetual Mode 是 Orderly-powered perpetual venue。 |
| `aftermath` | `交易所不支持现货` | 当前 adapter 对应 Aftermath Perpetuals。 |
| `d8x` | `交易所不支持现货` | D8X 是 perpetual futures engine。 |
| `grvt` | `交易所不支持现货` | 当前公开交易主线为 perpetuals；spot 等官方上线后重核。 |
| `hibachi` | `交易所不支持现货` | 官方交易文档是 perpetual contract/orderbook/funding。 |
| `modetrade` | `交易所不支持现货` | Mode Trade 是 Mode Perpetuals DEX / Orderly EVM profile。 |
| `oxfun` | `交易所不支持现货` | OX.FUN 官方主线是 perpetual swaps/options-style scope，未见共享 Spot 订单生命周期。 |
| `pacifica` | `交易所不支持现货` | Pacifica 官方 Contract Specifications 是 perpetual contracts。 |
| `woofipro` | `交易所不支持现货` | 按 WOOFi Pro profile 口径；WOOFi Swap 不是共享 Spot 订单簿。 |
| `cex` | `交易所不支持合约` | 当前 CEX.IO 公开交易文档是 Spot Trading REST/WS。 |
| `bitbank` | `交易所不支持合约` | 已记录：当前官方 REST API 有 Spot/Margin 相关路径，未见标准 futures/perpetual/options；Margin 单独写 `项目未实现`。 |
| `bitso` | `交易所不支持合约` | 当前官方 Trading API 有 spot/OTC/RFQ/conversions 和 Margin Trading [WIP]，未见标准合约。 |
| `bithumb` | `交易所不支持合约` | 当前官方 Open API 未见标准 futures/perpetual/options。 |
| `bitopro` | `交易所不支持合约` | 当前官方 Open API 覆盖现货交易对和钱包，未见标准合约。 |
| `btcmarkets` | `交易所不支持合约` | 当前官方 API v3 未见标准合约。 |
| `coincheck` | `交易所不支持合约` | 当前官方 Exchange API 未见标准合约。 |
| `coinone` | `交易所不支持合约` | 当前官方 API v2/v2.1 未见标准合约。 |
| `independentreserve` | `交易所不支持合约` | 当前官方资料有 leveraged trading，未见标准 futures/perpetual/options 统一 API；leveraged trading 单独写 `项目未实现`。 |
| `luno` | `交易所不支持合约` | 当前官方 API 未见 futures/margin。 |
| `upbit` | `交易所不支持合约` | 当前官方 API reference 未见标准 futures/perpetual/options。 |
| `alpaca` | `交易所不支持合约` | 按 crypto adapter 口径；Alpaca 证券 Options 不纳入本 adapter。 |
| `bit2c` | `交易所不支持合约` | 当前官方 API 只见 NIS 现货 ticker/orderbook/trades/order/cancel。 |
| `bitbns` | `交易所不支持合约` | 标准 futures/perpetual/options 未见稳定官方 API；Margin Trading 单独写 `项目未实现`。 |
| `bitteam` | `交易所不支持合约` | 当前官方资料覆盖 Spot/P2P 和 CCXT-style spot endpoints。 |
| `bittrade` | `交易所不支持合约` | 官方 API 介绍明确当前不支持杠杆交易，账户示例为 spot。 |
| `blockchaincom` | `交易所不支持合约` | 按 Exchange API 口径；Perps/第三方接口单独写项目未实现边界。 |
| `btcbox` | `交易所不支持合约` | 当前官方 API 只见 JPY 现货 ticker/depth/orders/trade endpoints。 |
| `btcturk` | `交易所不支持合约` | 当前官方 API 是现货 market data/trading/account management。 |
| `coinmate` | `交易所不支持合约` | 当前官方 API 覆盖 EUR/CZK 现货交易和 WS。 |
| `coinmetro` | `交易所不支持合约` | 当前官方资料未见标准 futures/perpetual/options；Margin/Swap 单独写项目未实现。 |
| `coinsph` | `交易所不支持合约` | 当前官方 API/帮助中心明确 Spot Trade API，未见标准合约。 |
| `coinspot` | `交易所不支持合约` | 当前官方 API/V2 API 覆盖 AUD 现货市场、余额、订单和历史。 |
| `cryptomus` | `交易所不支持合约` | Exchange 文档覆盖现货 orderbook/orders/WS；payment/conversion 不并入合约。 |
| `exmo` | `交易所不支持合约` | 当前标准 futures/perpetual/options 未上线；Margin 单独写 `项目未实现`，perps 上线后重核。 |
| `foxbit` | `交易所不支持合约` | Foxbit Exchange API 是 spot；Rispar 抵押借贷不是标准合约仓位。 |
| `hollaex` | `交易所不支持合约` | 按官方 demo/API profile 写；任意 HollaEx 白标 venue 需单独核验。 |
| `indodax` | `交易所不支持合约` | 当前官方 public/private API 覆盖 IDR 现货/TAPI，未见标准合约。 |
| `mercado` | `交易所不支持合约` | Mercado Bitcoin `positions` 语义是 open orders，不是合约仓位。 |
| `ndax` | `交易所不支持合约` | 当前 instrument type 为 standard，未见标准 futures/perpetual/options；margin account semantics 单独写项目未实现。 |
| `novadax` | `交易所不支持合约` | 当前官方 API 覆盖 symbols、market data、orders、account、wallet，未见标准合约。 |
| `p2b` | `交易所不支持合约` | 当前官方 API/Swagger 覆盖 spot/P2P；P2P 不并入中心化订单簿。 |
| `paymium` | `交易所不支持合约` | 当前官方 API 是 BTC/EUR spot public/private REST 与 socket.io。 |
| `tokocrypto` | `交易所不支持合约` | Tokocrypto spot/Binance-family surface；`marginTradingEnable=0`。 |
| `wavesexchange` | `交易所不支持合约` | WX matcher 是 Waves spot asset pair matcher，没有标准合约仓位/资金费/mark price。 |
| `yobit` | `交易所不支持合约` | 当前 API v3/TAPI 覆盖 spot/Trade API/Defi swap/Yobicode，未见标准合约。 |
| `zaif` | `交易所不支持合约` | 信用取引当前停止中；标准合约当前不可接入，恢复后重核。 |

## P4 高级订单能力项目未实现/未启用

这些 adapter 已确认官方支持批量、改单或等价 endpoint，但当前项目还没有稳定 runtime，或当前仍停留在 spec/request-fixture 离线边界。已完成/运行态或明确 unsupported 边界的 adapter 不再放在本段：`backpack`、`xt`、`ascendex`、`biconomy`、`coindcx`、`alpaca`、`bigone`、`bitfinex`、`bitflyer`、`bitget`、`bitmart`、`bitmex`、`bitrue`、`bitstamp`、`bittrade`、`bitunix`、`blofin`、`btcmarkets`、`bybit`、`bydfi`、`coinbase`、`coinbaseexchange`、`coincheck`、`coinex`、`coinstore`、`cointr`、`coinw`、`deepcoin`、`deribit`、`digifinex`、`gateio`、`hashkey_global`。

| adapter | 官方高级订单结论 | 必补内容 |
| --- | --- | --- |
| `grvt` | 官方 trading/auth 资料支持 create/cancel/bulk orders；`amend_order`、`bulk_orders`、`batch_place_orders`、`batch_cancel_orders` 已收窄为 `spec_only` request-spec、ack/partial parser fixture 与 `capabilities_v2` runtime-disabled 边界。 | 保留离线：代码级错误已细化为 session renewal、EIP-712/external signer、partial parser、write/readback reconciliation 和 dry-run guard 缺口；order-list/OCO 保持不支持。 |
| `hibachi` | 官方 trade REST 支持 `PATCH /trade/order` 和 `POST/DELETE /trade/orders`；`amend_order`、`batch_orders`、`batch_place_orders`、`batch_cancel_orders` endpoint 已补 `spec_only` request-spec/parser fixtures 与 `capabilities_v2` runtime-disabled 边界。 | 保留离线：`amend_order` 缺 shared price/full replace 字段，`batch_place_orders` 缺 contract-id/scale signer，`batch_cancel_orders` 缺 batch signature/parser/reconciliation；补 nonce、readback 和 live-dry-run guard 后再提升。 |
| `lighter` | 官方 API Trading 支持 `/sendTx` modify 与 `/sendTxBatch`/`/batchOrders`；`amend_order`、`batch_tx`、`batch_cancel`、`batch_place_orders`、`batch_cancel_orders` 已补 request-spec/source、fixture-only ack/partial parser、`capabilities_v2` native/partial runtime-disabled 边界和显式 runtime guard。 | 保留离线：代码级错误已细化为 SDK-compatible signed tx builder、API-key private key/index/nonce、partial parser、write/readback reconciliation 和 broadcast dry-run guard；order-list/OCO 保持不支持。 |
| `bullish` | 官方 REST command 支持普通 amend/cancel-all；当前 `amend_order`/`cancel_all_orders` 已补 request-spec/signing fixture、显式 offline guard 和 `capabilities_v2` signed command endpoint 边界，live private write runtime 未提升。 | 保留离线：补 Bullish JWT lifecycle、nonce-window guard、amend/cancel-all response parser、reconciliation 和 dry-run guard 后再提升；native batch/OCO/order-list 继续留在不支持段。 |
| `aark` | Orderly `PUT /v1/order` edit 与 `POST /v1/batch-order` batch-create 已补 signed-write request-spec、parser fixture 和 `capabilities_v2` runtime-disabled 离线边界；当前 Aark profile 未接 shared amend/batch runtime。 | 保留离线：缺 Aark/Orderly account onboarding、audited Ed25519 signer runtime、trade permission scope guard、post-write query/open/fills reconciliation、partial-failure/missing-item reconciliation 和 dry-run guard；order-list 和 batch-cancel 保持不支持。 |
| `modetrade` | Orderly `PUT /v1/order` edit 与 `POST /v1/batch-order` batch-create 已补 signed-write request-spec、ack parser fixture 和 `capabilities_v2` runtime-disabled 离线边界；当前 ModeTrade profile 未接 shared amend/batch runtime。 | 保留离线：缺 ModeTrade/Orderly account onboarding、audited Ed25519 signer runtime、trade permission scope guard、write 后 query/open/fills reconciliation、partial-failure/missing-item reconciliation 和 live-dry-run guard；order-list 和 batch-cancel 保持不支持。 |
| `bsx` | BSX cancel/update 线索已补 signed source-boundary fixtures；`amend_order` 与 `batch_cancel_orders` 已整理为矩阵可识别的 `spec_only` 离线 endpoint 边界，并补 fixture-only parser 与 `capabilities_v2` runtime-disabled endpoint。 | 保留离线：缺 BSX account headers、EIP-712/session signing、safe non-overcancel cancel shape、sequence/dry-run guard、post-write order-state reconciliation 和 partial-failure reconciliation runtime；batch-place/order-list 保持不支持。 |
| `coinmate` | buy/sell replace-limit amend request-spec、ack parser、`capabilities_v2` 离线 endpoint 和 live guard 已补，矩阵仍为离线；native batch place/list cancel 不支持，cancel-all 仅作为 cancel-all。 | 保留离线：shared `AmendOrderRequest` 缺 replacement side/price，live private writes 仍缺 serialized nonce/dry-run guard/REST+WS reconciliation；batch/order-list 保持 unsupported 边界。 |
| `oxfun` | 官方 private WebSocket `placeorders` 支持批量下单；`batch_place_orders` 已有 WS request-spec、login/signing fixture、单/partial ack parser fixture、reconciliation report、`unsupported_boundary` project/unsupported separation 和 `capabilities_v2` native/partial runtime-disabled 边界，矩阵仍为离线。 | 保留离线：缺 authenticated private WS session lifecycle、request tag/order-id reconciliation、dry-run guard 和 REST/private-stream readback；`amend_order`、`place_order_list`、`batch_cancel_orders` 保持 explicit unsupported，不进入 runtime。 |
| `derive` | 官方 `private/replace` 存在；`amend_order` 已补 JSON-RPC request spec、replace signing vector、source-boundary fixture、parser fixture，并暴露到 `capabilities_v2` 的 runtime-disabled 离线边界。 | 保留离线：代码级错误已细化为 session/JWT/Stark signer、shared amend 完整 replace 字段、replace response reconciliation 和 live dry-run guard 缺口；batch/order-list 保持不支持。 |
| `dydx` | v4 链上交易支持 placing/replacing/canceling 线索；`amend_order`、`batch_place_orders`、`batch_cancel_orders` 已复核为 `spec_only` 链上 tx request/source/parser fixture 与 `capabilities_v2` runtime-disabled 离线边界，runtime 写路径返回 `project_unimplemented`，矩阵可与 `place_order_list` unsupported 分离。 | 保留离线：代码级错误已细化为 wallet signer、account number/sequence、chain id/subaccount guard、batch tx builder、partial tx parser、dry-run-safe broadcast guard 和 Indexer reconciliation 缺口；order-list/OCO 保持不支持。 |

## P4 高级订单能力当前不支持

这些已经可以写入单交易所文档，避免把普通下单/撤单误当成批量、改单或 OCO/OTO。

| adapter | 写法 | 说明 |
| --- | --- | --- |
| `apex` | `交易所不支持当前高级订单能力 runtime` | 已审计官方资料未找到 native amend、batch place/cancel、OCO/OTO order-list endpoint。 |
| `binanceus` | `交易所不支持当前 native batch/OCO/改单/order-list runtime` | cancel-replace 和 legacy OCO 已有 unsupported shared-semantics fixture；当前 Binance.US Spot profile 未验证可无损映射到 shared gateway 的 native batch place/cancel endpoint，不复用 Binance.com batch route。 |
| `bullish` | `交易所不支持当前 native batch/OCO/OTO runtime` | 未找到 native batch place/cancel 和 OCO/OTO order-list endpoint；普通 amend 是离线项目未实现，单独在 P4 项目段跟踪。 |
| `bitkan` | `交易所不支持当前高级订单能力 runtime` | 当前未核到稳定 BitKan-native private trading API；高级订单全部是 unverified boundary。 |
| `zeta_markets` | `交易所不支持当前高级订单能力 runtime` | 原 Zeta Markets venue 已停止运营；legacy scan adapter 不补 live private trading。 |
| `wavesexchange` | `交易所不支持当前高级订单能力 runtime` | WX matcher 当前 profile 是 scan-only；matcher signed writes、批量、改单、订单列表不进入 shared gateway runtime。 |
| `d8x` | `交易所不支持当前高级订单能力 runtime` | D8X 链上交易需要 EVM signer、ABI/calldata、gas/nonce/reorg 设计；当前无 gateway-safe atomicity mapping。 |
| `aftermath` | `交易所不支持当前高级订单能力 runtime` | Aftermath Perpetuals 交易需要 Sui transaction 构造、签名和链上对账；当前只做 public REST scan。 |
| `aark` | `交易所不支持当前 order-list/batch-cancel runtime` | amend 与 batch-place 是项目未实现；order-list 和 batch-cancel 当前没有无损 shared runtime。 |
| `modetrade` | `交易所不支持当前 order-list/batch-cancel runtime` | amend 与 batch-place 是项目未实现；order-list 和 batch-cancel 当前没有无损 shared runtime。 |
| `arkham` | `交易所不支持当前高级订单能力 runtime` | Arkham private REST 仍是 offline request-spec；未验证原生 batch place/cancel 或 OCO/OTO/order-list。 |
| `cex` | `交易所不支持当前高级订单能力 runtime` | CEX.IO Spot Trading 支持普通 order/cancel 和 cancel-all，但未见 shared amend、OCO/OTO、generic batch place/cancel 语义。 |
| `p2b` | `交易所不支持当前高级订单能力 runtime` | P2B private order/new 和 order/cancel 是 request-spec-only；未验证原生 batch、amend 或 OCO/OTO。 |
| `bsx` | `交易所不支持当前 batch-place/order-list runtime` | amend 与 batch-cancel 已是离线 endpoint 边界；当前未确认可无损映射 generic batch-place 或 OCO/OTO order-list。 |
| `aster` | `交易所不支持当前高级订单能力 runtime` | 当前 profile 已接普通 order/cancel/cancel-all，但 endpoint mapping 没有 batch endpoint，且 order-list/amend 显式 unsupported。 |
| `bequant` | `交易所不支持当前高级订单能力 runtime` | Spot profile 支持普通 order/cancel/cancel-all，但没有映射 native batch create、batch cancel-by-list 或 generic amend。 |
| `bit2c` | `交易所不支持当前高级订单能力 runtime` | 当前官方/API profile 只保留普通私有 REST request-spec；没有共享 batch/amend/order-list runtime。 |
| `bitbns` | `交易所不支持当前高级订单能力 runtime` | private REST 仍 disabled；当前 adapter 不启用 place/cancel/cancel-all/amend/order-list/batch。 |
| `bithumb` | `交易所不支持当前高级订单能力 runtime` | Spot 普通 order/cancel 已映射；未确认 native amend、batch 或 order-list。 |
| `bitso` | `交易所不支持当前高级订单能力 runtime` | private REST 是 spec-only；native batch place unsupported，未建立 amend/order-list runtime。 |
| `bitteam` | `交易所不支持当前高级订单能力 runtime` | private endpoints 是 request-spec-only；batch place/cancel 未验证，不进入 runtime。 |
| `blockchaincom` | `交易所不支持当前高级订单能力 runtime` | private writes 多为 spec-only；batch place/cancel、amend 和 order-list 明确 unsupported。 |
| `btcbox` | `交易所不支持当前高级订单能力 runtime` | private writes 是 spec-only；无 native batch place/cancel、amend 或 OCO/OTO。`unsupported_boundary.json` 和 adapter tests 已固定 capability=false 与 runtime `btcbox.*.unsupported`。 |
| `btcturk` | `交易所不支持当前高级订单能力 runtime` | private endpoints 是 spec_only；batch place/cancel、改单、order-list、cancel-all 不进入共享高级 runtime。`unsupported_boundary.json` 和 adapter tests 已固定 `btcturk.*_unsupported` guard。 |
| `bybiteu` | `交易所不支持当前高级订单能力 runtime` | 区域 profile 默认私有 REST/写接口未通过 broker scope 审计，advanced batch/amend/order-list/cancel-all 维持 default Unsupported；fixture 和 tests 固定 `bybiteu.*` private credential guard。 |
| `cod3x` | `交易所不支持当前高级订单能力 runtime` | 已加固：routing/terminal profile 未验证 Cod3x-native batch/amend/order-list/cancel-all API；`unsupported_boundary.json`、mapping 和 tests 覆盖 capability flags 与 runtime `Unsupported.operation`。 |
| `coinmetro` | `交易所不支持当前高级订单能力 runtime` | 已加固：当前 adapter 不连接 amend、OCO/OTO、native batch place/cancel、cancel-all、post-only/client order id 等高级交易能力；fixture/mapping/tests 固定 unsupported。 |
| `coinone` | `交易所不支持当前高级订单能力 runtime` | 已加固：Spot 单笔交易与 native cancel-all 运行；amend、order-list、batch place/cancel 明确 unsupported，且不再把 sequential cancel 暴露为 batch capability。 |
| `coinsph` | `交易所不支持当前高级订单能力 runtime` | 现货普通交易运行；amend、order-list、batch place/cancel、cancel-all 已有 mapping/fixture/test 级 unsupported 边界。 |
| `coinspot` | `交易所不支持当前高级订单能力 runtime` | side-specific order API 不能安全映射到 generic amend/query/batch；cancel-all/order-list 也保持 explicit unsupported。 |
| `cryptomus` | `交易所不支持当前高级订单能力 runtime` | private writes 为 spec_only；amend、order-list、batch place/cancel、cancel-all explicitly unsupported。 |
| `derive` | `交易所不支持当前 batch/order-list runtime` | amend/replace 是项目未实现；native batch place/cancel 与 OCO/OTO order-list 未验证为无损 shared runtime。 |
| `derive_chain_perps` | `交易所不支持当前高级订单能力 runtime` | 当前 profile 不承接 Derive Exchange API runtime；batch/amend/order-list 全部 unsupported_unverified。 |
| `equation` | `交易所不支持当前高级订单能力 runtime` | Arbitrum protocol audit profile 未验证 stable gateway API/indexer contract。 |
| `exmo` | `交易所不支持当前高级订单能力 runtime` | Spot adapter 不暴露 native batch place/cancel、amend、OCO/OTO order-list。 |
| `fmfwio` | `交易所不支持当前 generic batch-place / shared cancel-list runtime` | amend 和 order-list 已运行；generic native batch-place 未找到无损语义，`DELETE /spot/order` 仅支持 cancel-all/filter-by-symbol，作为 audit fixture 保留但不映射 shared batch_cancel_orders，避免 over-cancel。 |
| `foxbit` | `交易所不支持当前高级订单能力 runtime` | private writes 为 spec_only；native batch place/cancel unsupported。 |
| `gemini` | `交易所不支持当前高级订单能力 runtime` | Spot 未 advertised batch，当前 shared spot runtime 不暴露 composed batch/amend/order-list。 |
| `hitbtc` | `交易所不支持当前 generic batch-place / shared cancel-list runtime` | amend 和 order-list 已运行；generic native batch-place 未找到无损语义，`DELETE /spot/order` 仅支持 cancel-all/filter-by-symbol，作为 audit fixture 保留但不映射 shared batch_cancel_orders，避免 over-cancel。 |
| `hollaex` | `交易所不支持当前高级订单能力 runtime` | demo/API profile private trading/readback 是 offline request-spec；runtime 仍 unsupported。 |

## 验收标准

每个补全任务完成时，至少满足：

1. 单交易所文档写清官方产品线结论，未支持项使用 `交易所不支持`，项目缺口使用 `项目未实现`。
2. `endpoint_mapping.yaml` 包含对应 REST/WS operation、support、product、market_types、auth、rate_limit_bucket。
3. WebSocket 行情包含订阅方式、推流间隔、档位、sequence/checksum、snapshot 重建。
4. 至少补一个成功 parser fixture；写接口还要补 request spec 和 signing vector。
5. 重新运行 `python3 scripts/generate_exchange_gateway_matrix.py`，确认矩阵状态变化符合预期。
6. 定向运行对应 adapter 测试；公共逻辑变化再跑 `cargo check -p rustcta-exchange-gateway --lib`。
