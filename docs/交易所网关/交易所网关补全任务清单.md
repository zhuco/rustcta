# 交易所网关补全任务清单

状态日期：2026-06-08

本文件把已收集的数据转成后续可执行任务。机器可读版本见 [交易所网关补全任务清单.csv](交易所网关补全任务清单.csv)。

如果要按单个交易所开工，先看 [adapter 工作包索引](adapter工作包索引.md)，它会合并当前实现、明确任务和剩余核验项。

## 数据来源

- 当前项目实现证据：[交易所功能盘点矩阵](交易所功能盘点矩阵.md)。
- 官方能力结论：[官方能力核验台账](官方能力核验台账.md)。
- 产品线核验：[产品线官方核验 P0 主流交易所](产品线官方核验_P0_主流交易所.md)。
- 链上合约产品线核验：[产品线官方核验 P1 链上合约交易所](产品线官方核验_P1_链上合约交易所.md)。
- 衍生品产品线核验：[产品线官方核验 P2 衍生品交易所](产品线官方核验_P2_衍生品交易所.md)。
- CEX 合约边界核验：[产品线官方核验 P3 CEX 合约边界](产品线官方核验_P3_CEX合约边界.md)。
- 区域现货交易所核验：[产品线官方核验 P4 区域现货交易所](产品线官方核验_P4_区域现货交易所.md)。
- 区域现货 CEX 第二批核验：[产品线官方核验 P5 区域现货 CEX 第二批](产品线官方核验_P5_区域现货_CEX第二批.md)。
- 剩余区域现货 CEX 核验：[产品线官方核验 P6 剩余区域现货 CEX](产品线官方核验_P6_剩余区域现货_CEX.md)。
- WebSocket 细项：[P0 第一批](WebSocket官方核验_P0_第一批.md)、[P0 第二批](WebSocket官方核验_P0_第二批.md)、[P1 第三批](WebSocket官方核验_P1_第三批.md)、[P2 区域现货](WebSocket官方核验_P2_区域现货交易所.md)、[P3 P2 公共 WS 缺口](WebSocket官方核验_P3_P2公共WS缺口交易所.md)、[P4 CEX 盘口细项](WebSocket官方核验_P4_CEX盘口细项.md)、[P5 衍生品/链上盘口细项](WebSocket官方核验_P5_衍生品链上盘口细项.md)、[P6 补充交易所盘口细项](WebSocket官方核验_P6_补充交易所盘口细项.md)。

## 当前任务分布

| 优先级 | 类型 | 数量 | 说明 |
| --- | --- | ---: | --- |
| P0 | 公共订单簿 WebSocket | 13 | 官方已确认支持，当前项目未接入或缺关键低延迟细项。 |
| P0 | 产品线项目未实现 | 6 | 官方支持合约/期权，但当前项目未接。 |
| P1 | 已有 WS 的结构化细项 | 9 | 项目已有 native/spec/parser 证据，但缺 interval/depth/channel/sequence。 |
| P2 | 产品线项目未实现 | 35 | 官方有 spot/OpenBook/合约/margin/leveraged/perps 线索，但当前 adapter 未接或需要独立 profile。 |
| P2 | 明确交易所不支持 | 56 | 已核验可写 `交易所不支持现货` 或 `交易所不支持合约`。 |
| P2 | 公共订单簿 WebSocket | 5 | 官方已确认支持公共订单簿 WS，但当前项目未接 runtime。 |
| P2 | 已有 WS 的结构化细项 | 1 | 官方已确认细项，当前项目已有 payload/parser 证据但缺 interval/depth/resync 结构化字段。 |
| P2 | 明确公共 WS 不支持 | 12 | 已核验可写 `交易所不支持公共 WS 行情` 或 profile 不承接公共 WS runtime。 |
| P3 | 区域现货 WS 结构化细项 | 10 | 官方已确认 orderbook WS 细项，当前 mapping 缺 interval/depth/channel/sequence 风险字段。 |
| P3 | CEX WS 结构化细项 | 12 | 官方已确认 CEX orderbook WS 细项，当前 mapping 缺 interval/depth/channel/sequence/checksum 结构化字段。 |
| P3 | 衍生品/链上 WS 结构化细项 | 12 | 官方已确认衍生品、链上永续和 Orderly profile orderbook WS 细项，当前 mapping 缺 interval/depth/channel/sequence/checksum 结构化字段。 |
| P3 | 补充交易所 WS 结构化细项 | 11 | 官方已确认补充 CEX/经纪商/链上 profile 的 WS 边界，当前 mapping 缺 interval/depth/channel/sequence/checksum 结构化字段。 |

## P0 公共订单簿 WebSocket

这些直接影响跨所套利上线。每个任务至少要补：

- `endpoint_mapping.yaml` 结构化字段：channel、订阅方式、推流间隔、档位、sequence/checksum、snapshot 重建。
- WebSocket 订阅 payload 和 parser fixture。
- REST snapshot + delta 重建策略。
- 断线重连后的重新订阅和 book resync。

| adapter | 产品线 | 当前项目状态 | 必补内容 |
| --- | --- | --- | --- |
| `binance` | Spot | public WS unsupported | Spot depth/bookTicker、100ms/1000ms、5/10/20 档、update id 重建。 |
| `okx` | Spot/Swap/Futures/Option | public WS unsupported | books5、bbo-tbt、10ms/100ms、VIP 限制 channel 标注。 |
| `bitget` | Spot | public WS unsupported | books1/books5/books15、10ms/200ms、CRC32。 |
| `gateio` | Spot | public WS unsupported | `spot.order_book_update`、20ms/100ms、20/100 档。 |
| `mexc` | Spot | public WS unsupported | 10ms/100ms depth、bookTicker、fromVersion/toVersion。 |
| `binanceus` | Spot | public WS unsupported | 按 Binance Spot 同类补 100ms depth/bookTicker。 |
| `binancecoinm` | COIN-M | public WS unsupported | COIN-M partial/diff depth、100/250/500ms、`U/u/pu`。 |
| `bybit` | Spot/Linear/Inverse/Option | spec_only | 1/50/200/1000 档 interval、`seq`、snapshot/delta。 |
| `kucoin` | Spot/Futures | native 但缺细项 | `obu` BBO/5/50/increment、sequence、snapshot replay；2026-06-17 UTC 后补 UTA `depth=increment@10ms` 500 档迁移。 |
| `htx` / `huobi` | Spot/USDT-M | declared/parser evidence | Spot MBP、合约 high_freq depth、seqNum/prevSeqNum/version。 |
| `lbank` | Spot | native 但缺细项 | 10/50/100 档、官方未给固定 ms、无 sequence 风险。 |
| `coinw` | Spot/Futures | parser_only | Spot snapshot/incremental depth；Futures 100 档；seq/startSeq/endSeq。 |

## P0 产品线项目未实现

这些不能写成 `交易所不支持`。官方已确认支持，当前项目需要补 adapter 或扩展现有 adapter。

| 交易所 | 官方支持 | 当前缺口 | 建议边界 |
| --- | --- | --- | --- |
| Binance | USD-M Futures、Options | 没有 `binanceusdm` / `binanceoption` adapter | 优先新建 USD-M；Options 单独评估是否进网关。 |
| OKX | Swap、Futures、Options | `okx` 当前只接 Spot | 先补 Swap，再补 Futures/Options。 |
| Bitget | Futures | `bitget` 当前只接 Spot | 可以扩展现有 adapter 或新增 futures profile。 |
| Gate.io | Perpetual、Delivery、Options | `gateio` 当前只接 Spot | 先补 Perpetual Futures。 |
| MEXC | Contract/Futures | `mexc` 当前只接 Spot | 新增 Contract profile 或 adapter。 |

## 高速盘口优先候选

这些官方已经给出 10ms、20ms 或 50ms 级别盘口能力，补 WebSocket 时优先落地：

| 交易所 | 最快盘口 | 当前项目处理 |
| --- | --- | --- |
| OKX | `bbo-tbt` 10ms；受限 `books-l2-tbt`/`books50-l2-tbt` 10ms。 | 先补 BBO/5 档，VIP/登录通道单独标限制。 |
| Bybit | 1 档 10ms，50 档 20ms。 | 当前 spec_only，补 `orderbook.1` 和 `orderbook.50`。 |
| Bitget | Spot `books1` 10ms。 | 当前 public WS unsupported，补 `books1`、CRC32。 |
| MEXC | bookTicker 和 aggregated depth 可选 10ms。 | 当前 public WS unsupported，补 protobuf parser 和 fromVersion/toVersion。 |
| WOO X | `bbo` 10ms，`orderbookupdate` 200ms。 | 当前 native 但缺结构化字段。 |
| Coinbase Exchange | `level2_batch` 50ms。 | 当前 native，补 50ms batch 与 sequence 边界。 |
| KuCoin UTA | 2026-06-17 UTC 上线 `depth=increment@10ms`，500 档。 | 2026-06-17 前只作为迁移任务，不写成当前已可用。 |
| Crypto.com | `book` delta 可选 10ms，depth 10/50。 | 当前 native 但缺结构化字段，补 `SNAPSHOT_AND_UPDATE`、`u/pu`。 |
| Phemex | orderbook depth 约 20ms，depth 0/1/5/10/30。 | 当前 native 但缺结构化字段，补 sequence 和 60s snapshot 自校验。 |
| BitMart | bookTicker BBO real-time；`depth/increase100` 100ms。 | 当前 spec_only，补 BBO 和 100 档增量 book。 |
| Lighter | `order_book/{MARKET_INDEX}` 每 50ms。 | 当前 parser_only，补 nonce continuity、snapshot/update 和 gap resubscribe。 |
| Paradex | order_book 可选 50ms/100ms，固定 15 levels。 | 当前 native，补 `seq_no`、feed_type 和 50ms/100ms refresh_rate。 |
| Aster | partial/diff depth 可 100ms，bookTicker real-time。 | 当前 declared，补 5/10/20 档和 `U/u/pu` 重建规则。 |
| BloFin | `books`/`books5` 100ms。 | 当前 native，补 200 depth/5 depth 和 `prevSeqId/seqId`。 |
| ApolloX DEX/APX | partial/diff depth 可 100ms，bookTicker real-time。 | 当前 spec_only，补 5/10/20 档、100ms 和 `U/u/pu`。 |
| BitTrade | BBO 变化推送，带 `seqId`。 | 当前 native，补 BBO/depth channel、step0-step5 和 gzip。 |

## P1 已有 WS 但缺结构化细项

这些适合先补 `endpoint_mapping.yaml`，再补 fixture，不一定要先重写 runtime。

| adapter | 当前证据 | 必补内容 |
| --- | --- | --- |
| `backpack` | parser_only | realtime depth、200/600/1000ms depth、`U/u`、snapshot replay。 |
| `bitmex` | native | orderBookL2_25 100ms、orderBook10、orderBookL2 table diff。 |
| `deribit` | native | `book.{instrument}.{group}.{depth}.{interval}`、1/10/20、100ms/agg2、change_id。 |
| `orangex` | spec_only | 当前官方只见 `raw`，复核旧 100ms 证据来源。 |
| `whitebit` | native | 100ms 增量、10s snapshot keepalive、update_id 连续性。 |
| `woo` | native | bbo 10ms、orderbookupdate 200ms、orderbook 1s/100 levels。 |
| `kraken` | native | Spot CRC32/depth；Futures seq/snapshot/delta；必要时拆清 futures 边界。 |
| `coinbase` | native | Advanced Trade level2、heartbeat、无固定 book ms。 |
| `coinbaseexchange` | native | level2_batch 50ms、level2/full/level3 sequence 边界。 |

## P2 公共订单簿 WebSocket

这些交易所官方已经确认有公共订单簿 WS 或等价 socket stream，但当前项目没有可直接用于套利的 runtime，或缺关键结构化字段。

| adapter | 官方盘口结论 | 必补内容 |
| --- | --- | --- |
| `ascendex` | `depth:<symbol>` 默认 300ms throttle，`bbo:<symbol>` 变更推送；snapshot 最多 500 档，`seqnum` 必须连续。 | 补 interval、depth-snapshot/depth-snapshot-top100、seqnum 断档重建和 fixture。 |
| `cex` | Spot Trading WS `order_book_subscribe` 首包 snapshot，后续 `order_book_increment`，`seqId` 顺序应用。 | 补 ws-public runtime、订阅 payload、seqId parser 和 REST snapshot/断线重建。 |
| `coinsph` | bookTicker real-time；partial depth 5/10/20 档可 100ms，200 档 1000ms；diff depth 100ms/1000ms。 | 补 public stream endpoint、bookTicker、partial/diff depth、`U/u` snapshot 重建。 |
| `latoken` | STOMP `/v1/book/{base}/{quote}`，官方未给固定 ms/depth，靠 nonce 连续性检测。 | 补 STOMP public book runtime 或明确 spec-only 边界；记录 nonce gap reconnect。 |
| `p2b` | `depth.subscribe` limit 1-100；increment 每 1s，full flag 每 60s；无 sequence/checksum。 | 补 depth.update parser、1s/60s 重建策略和无 sequence/checksum 风险字段。 |
| `paymium` | socket.io public stream 推 `bids`/`asks` 价位变更；官方未给固定 ms/depth/sequence。 | 补 socket.io v1.3 public stream、REST depth 初始化和断线全量重建。 |

## P2 明确公共 WS 不支持

这些已经可以写进单交易所文档，避免后续重复核验公共订单簿 WS：

| adapter | 写法 | 说明 |
| --- | --- | --- |
| `bit2c` | `交易所不支持公共 WS 行情` | 当前官方 API 只见 REST public/private。 |
| `bitteam` | `交易所不支持公共 WS 行情` | 当前官方 docs/OpenAPI 只给 REST/CCXT-style endpoints，未给 WSS endpoint 和订单簿订阅规格。 |
| `btcbox` | `交易所不支持公共 WS 行情` | 当前官方 API 资料是 HTTP Public/Private API。 |
| `cod3x` | `交易所不支持公共 WS 行情` | Cod3x-native profile 未建立稳定订单簿 WS API；下游 venue 走各自 adapter。 |
| `coinspot` | `交易所不支持公共 WS 行情` | 当前官方 API/V2 API 是 public/private REST。 |
| `derive_chain_perps` | `交易所不支持公共 WS 行情` | 本 profile 不承接 Derive Exchange WS runtime，避免和 `derive` adapter 重复。 |
| `equation` | `交易所不支持公共 WS 行情` | 当前 audit-only adapter 未见稳定 exchange-gateway 公共订单簿 WS。 |
| `indodax` | `交易所不支持公共 WS 行情` | 官方 PDF/GitHub API 覆盖 Public API 和 TAPI REST。 |
| `yobit` | `交易所不支持公共 WS 行情` | 官方 API v2/v3/TAPI/Defi API 是 REST。 |
| `zebpay` | `交易所不支持公共 WS 行情` | 当前官方 docs 覆盖 Spot/Futures REST market/book/orderBook，未见 WS stream。 |
| `zeta_markets` | `交易所不支持当前公共 WS runtime` | 官方文档声明 Zeta Markets 已于 2025-05 停止运营。 |
| `bitkan` | `交易所不支持公共 WS 行情` | 当前未找到 BitKan 官方稳定 API 文档公开说明订单簿 WebSocket，不启用 unverified runtime。 |

## P3 区域现货 WS 结构化细项

这些交易所官方已经查到公共订单簿 WS 细项，但多数没有 10ms/20ms；适合在 P0/P1 主流套利行情之后补入结构化 mapping。

| adapter | 官方盘口结论 | 必补内容 |
| --- | --- | --- |
| `bitbank` | `depth_diff`/`depth_whole`；whole 正常模式 200 asks/200 bids；无固定 ms。 | Socket.IO join-room、200 档、非连续 sequence、whole/diff 重建。 |
| `bithumb` | `orderbook` Public WS；snapshot/realtime 开关；无固定 ms。 | orderbook request、level 聚合说明、无 sequence/checksum 风险。 |
| `bitopro` | 更新时每秒推全量 order book；默认 5 档，可选 1/5/10/20/30/50。 | 1s interval、limit 参数、snapshot-only、无 sequence 风险。 |
| `bitso` | `orders` top20 和 `diff-orders`；sequence 严格递增。 | orders/diff-orders、top20、REST snapshot + sequence replay。 |
| `btcmarkets` | WS v2 `orderbook` 最新状态，最多 50 bids/50 asks；无固定 ms。 | 50 档、heartbeat 5s、无 sequence/checksum 的最新快照语义。 |
| `coincheck` | `[pair]-orderbook`；发生交易时约 0.1s；无固定 depth。 | 0.1s、orderbook diff、无 sequence/checksum、REST snapshot 兜底。 |
| `coinone` | ORDERBOOK 首包最后订单簿，之后有变更才推；无固定 ms/depth。 | ORDERBOOK topic、首包 snapshot、order book id、无 checksum 风险。 |
| `independentreserve` | `orderbook-{crypto}` 事件流；Nonce 严格递增；heartbeat 当前 60s。 | Subscribe/query 订阅、事件模型、Nonce 丢包重建。 |
| `luno` | market stream 先推当前订单簿，再尽快推 updates；需要 API key credentials。 | stream URL、sequence、全量初始 book、错序重连。 |
| `upbit` | orderbook units 支持 1/5/15/30；无固定 ms。 | region URL、`{code}.{count}`、1/5/15/30、无 checksum 风险。 |

## P3 CEX WS 结构化细项

这些交易所已有 native/spec/payload/parser 证据，但缺套利需要的结构化字段。优先补 `endpoint_mapping.yaml` 的 channel、interval、depth、sequence/checksum 和重建策略。

| adapter | 官方盘口结论 | 必补内容 |
| --- | --- | --- |
| `bitfinex` | `book` 支持 `freq=F0/F1`、`len=1/25/100/250`、`prec=P0-P4`，可选 checksum/sequence/bulk。 | `prec/freq/len`、`OB_CHECKSUM`、`SEQ_ALL`、snapshot/update 重建。 |
| `bitflyer` | JSON-RPC `lightning_board_snapshot_{product_code}` + `lightning_board_{product_code}`；无固定 ms/depth。 | snapshot+diff channel、REST getboard 重建、无 sequence/checksum 风险。 |
| `bitmart` | `spot/depth/increase100` 100ms，`spot/depth5/20/50` 500ms，bookTicker BBO real-time。 | depth increase、full depth、bookTicker、`version` 断档重建。 |
| `bitstamp` | WebSocket v2 real-time；项目已记录 `order_book`/`diff_order_book`；官方未给固定 ms/depth。 | REST order_book 和 order_data gap recovery、无固定 ms/depth 风险。 |
| `bitvavo` | `book` 变化推送，`nonce` 顺序重建；官方未给固定 ms。 | `nonce +1` 校验、snapshot depth 初始化、无 checksum 风险。 |
| `cryptocom` | `book.{instrument}.{depth}` delta 10/100ms，depth 10/50，`u/pu` 重建。 | `SNAPSHOT_AND_UPDATE`、10ms/100ms、10/50 档、`u/pu`。 |
| `phemex` | `orderbook.subscribe`/`orderbook_p.subscribe` 约 20ms，depth 0/1/5/10/30，带 `sequence`。 | 20ms/120ms/full 100ms、depth 参数、60s snapshot 自校验。 |
| `poloniex` | Spot/Futures V3 `book_lv2` real-time 20 level，`id/lastId` 或 `id/lid`。 | 20 档、spot/futures sequence 字段、重订阅重建。 |
| `coinex` | `depth.subscribe` 约 200ms，limit 5/10/20/50，带 signed CRC32 checksum。 | limit、merge interval、full/incremental、checksum。 |
| `coinmate` | `order_book-{PAIR}`；官方未给固定 ms/depth/sequence/checksum。 | 明确无固定 ms/depth/sequence/checksum，REST snapshot fallback。 |
| `coinmetro` | `pairs` query 订阅 `bookUpdate`，有 `seqNumber` 和 CRC32 checksum；`tick` 含 ask/bid。 | query-string subscribe、checksum rounding、BBO tick、无固定 ms/depth。 |
| `coindcx` | Socket.IO depth update；现货示例 `@orderbook@20`，项目记录 spot/futures `@50`。 | Socket.IO join、20/50 depth 边界、无 sequence/checksum、REST snapshot 重建。 |

## P3 衍生品/链上 WS 结构化细项

这些交易所或 profile 已有 native/spec/payload/parser 证据，但缺跨所套利需要的结构化字段。`lighter` 和 `paradex` 有明确 50ms 级别盘口，应在主流 10ms/20ms 任务之后优先处理。

| adapter | 官方盘口结论 | 必补内容 |
| --- | --- | --- |
| `aark` | Orderly profile；`{symbol}@orderbookupdate` 每 200ms，`prevTs` 连续性，无 checksum。 | Orderly EVM WS endpoint/account_id、200ms、request orderbook snapshot 重建。 |
| `modetrade` | Orderly profile；`{symbol}@orderbookupdate` 每 200ms，`prevTs` 连续性，无 checksum。 | 同 Orderly 共享 mapping，补 account_id path、prevTs 和 snapshot fallback。 |
| `woofipro` | WOOFi Pro perps 走 Orderly；`orderbookupdate` 每 200ms。 | 同 Orderly 共享 mapping，避免和 WOO X/WOOFi Swap 混用。 |
| `apex` | `orderBook{limit}.H.{symbol}` high-frequency topic；limit 25/200；官方未给固定 ms。 | 25/200 档、snapshot+delta、乱序/缺包 snapshot 重建。 |
| `aster` | Binance-style bookTicker/partial/diff depth；100/250/500ms；5/10/20 档；`U/u/pu`。 | depth channel、100ms 档、REST snapshot + delta buffer 重建。 |
| `blofin` | `books` 200 depth 增量 100ms，`books5` 5 depth 100ms；`prevSeqId/seqId`。 | books/books5、100ms、200/5 档、prevSeqId 连续性和断档重建。 |
| `bullish` | `l1Orderbook`/`l2Orderbook` live realtime；有 sequenceNumber，未给固定 ms/depth。 | JSON-RPC subscribe、L1/L2、sequence/reconnect 和 REST hybrid snapshot。 |
| `derive` | public orderbook/ticker/trades WS；官方未给固定 ms/depth/sequence/checksum。 | 写清无固定 ms/depth/checksum 风险，REST/JSON-RPC snapshot 兜底。 |
| `dydx` | `v4_orderbook` 首包 snapshot 后推 price level 更新；`message_id`/`version`/`clobPairId`。 | v4_orderbook initial/update、断线重新订阅 snapshot 重建。 |
| `hyperliquid` | `l2Book`/`bbo`；REST `l2Book` snapshot 最多 20 levels/side；无传统 sequence/checksum。 | BBO/L2、20 levels REST snapshot、stale book 和 resync 风险字段。 |
| `lighter` | `order_book/{MARKET_INDEX}` 每 50ms；首包 snapshot 后只推变化；nonce 连续；无 checksum。 | 50ms、nonce continuity、gap resubscribe 和高速盘口标记。 |
| `paradex` | order_book 固定 15 levels，refresh_rate 50ms/100ms；`seq_no`。 | 50/100ms、15 levels、feed_type、seq_no 和高速盘口标记。 |

## P3 补充交易所 WS 结构化细项

这些交易所或 profile 已查到公共 orderbook WS、官方 SDK stream 或明确“不应启用”的边界。除 `apollox_dex` 外，多数没有固定 10ms/20ms；实现时重点是把无固定毫秒、无 checksum、snapshot fallback 写清楚。

| adapter | 官方盘口结论 | 必补内容 |
| --- | --- | --- |
| `aftermath` | `/perpetuals/ws/updates` 支持 orderbook deltas；未给固定 ms/depth/sequence/checksum。 | orderbook subscribe/unsubscribe、无固定 ms/depth、REST `getOrderbook` snapshot fallback。 |
| `alpaca` | Crypto Data API 支持 `orderbooks` WS；需 auth 后 subscribe；未给固定 ms/depth/sequence/checksum。 | auth+subscribe、REST latest orderbooks 重建和无 checksum 风险。 |
| `apollox_dex` | Binance-style bookTicker/partial/diff depth；100/250/500ms；5/10/20 档；`U/u/pu`。 | 100ms、5/10/20、bookTicker、REST snapshot + delta buffer 重建。 |
| `arkham` | Arkham Exchange 有 Websocket API，但公开页未给订单簿 channel 细项。 | 补完整 Exchange API reference；未补前不启用 runtime。 |
| `biconomy` | `depth.subscribe`，depth 5/10/15/20/30/50/100，`depth.update` 有 `is_full`。 | depth/precision、full/incremental、无 sequence/checksum。 |
| `bigone` | `subscribeMarketDepthRequest`，snapshot 后 update，`changeId/prevId` 连续。 | MarketDepth、changeId/prevId、protobuf 选项和断档重建。 |
| `bitbns` | 旧官方 SDK 资料无稳定 WS 规格，仅保留 socket 示例线索。 | 记录旧 SDK 风险，保持 spec-only，REST orderbook 重建。 |
| `bitrue` | Spot `market_${symbol}_simple_depth_step0`，gzip payload，server ping 15s。 | channel、gzip、ping 15s、REST depth fallback。 |
| `bittrade` | `market.<symbol>.depth.<type>` step0-step5 和 `market.<symbol>.bbo`；BBO 有 `seqId`。 | depth/BBO、step0-step5、seqId、gzip。 |
| `bitunix` | Futures `depth_books/book1/book5/book15`；未给固定 ms/sequence/checksum。 | futures 1/5/15 档、15s ping、REST depth fallback。 |
| `blockchaincom` | Exchange WS 支持 L2/L3 order book，消息带 `seqnum`。 | `l2`/`l3` subscribe、seqnum、snapshot/update 和 message limit。 |

## P2 产品线项目未实现

这些不能写成 `交易所不支持`。官方有 spot/OpenBook 线索，但当前 adapter 只接合约/永续或只做 scan-only。

| adapter | 官方支持 | 当前缺口 | 建议边界 |
| --- | --- | --- | --- |
| `bsx` | Spot + perpetuals | 当前只声明 perpetual | 决定是否新增 BSX Spot profile/adapter。 |
| `lighter` | Spot market stats / spot market 结构 | 当前只声明 perpetual | 决定是否扩展 lighter spot market mapping。 |
| `mango_markets` | Spot/OpenBook 线索 | 当前只声明 margin/perpetual | 当前 adapter 不接入 spot；如接入需单独 OpenBook/Solana 任务。 |
| `apex` | Spot account/wallet 线索 | 当前只声明 perpetual | 写项目未实现 Spot；先补 spot account/market/order lifecycle 核验。 |
| `apollox_dex` | ApolloX spot API family | 当前只声明 perpetual | 写项目未实现 Spot/当前 adapter 不接 spot API family。 |
| `aster` | Spot API + Futures API | 当前只声明 perpetual | 决定是否新增 Aster Spot mapping。 |
| `blofin` | Spot trading | 当前只声明 perpetual | 写项目未实现 Spot；单独核验 Spot API 下单/撤单是否公开稳定。 |
| `delta` | Spot + Futures + Options | 当前只声明 futures/option/perpetual | 写项目未实现 Spot；补 Delta spot 产品线。 |
| `deribit` | Spot Instruments / `kind=spot` | 当前只声明 futures/option/perpetual | 补 spot instrument、orderbook、order lifecycle。 |
| `derive` | Spot trading + options/perpetuals | 当前只声明 option/perpetual | 写项目未实现 Spot；不要和 `derive_chain_perps` profile 混淆。 |
| `dydx` | Solana spot trading 已上线 | 当前只声明 perpetual | 核验 Solana spot API/前端 routing 是否进入 gateway。 |
| `hyperliquid` | `spotMeta`、spot asset ctx、HIP-1 spot order books | 当前只声明 perpetual | 补 spotMeta、asset id、spot order path 映射。 |
| `paradex` | Spot Trading | 当前只声明 option/perpetual | 补 Paradex spot markets/order lifecycle。 |
| `bitflyer` | Crypto CFD / FX | 当前声明 margin/spot | 保留 CFD/FX 为 margin/adapter-specific 边界；如要进统一合约需单独设计。 |
| `bitstamp` | Derivatives endpoints | 当前只声明 spot | 补 Bitstamp derivatives/contracts profile。 |
| `coinex` | Futures API | 当前只声明 spot | 补 CoinEx Futures profile 或扩展 adapter。 |
| `gemini` | Derivatives / Perpetuals | 当前只声明 spot | 补 Gemini Derivatives/Perpetuals，并标区域限制。 |
| `hitbtc` | Futures margin account endpoints | 当前只声明 spot | 补 HitBTC Futures/Margin derivatives。 |
| `kucoin` | Futures API | 当前 `kucoin` 只声明 spot；项目另有 `kucoinfutures` | 文档写合约走 `kucoinfutures`，不要写交易所不支持。 |
| `bitbank` | Margin status/positions | 当前只声明 spot | 写项目未实现 Margin；标准合约写 `交易所不支持合约`。 |
| `bitso` | Margin Trading [WIP] | 当前只声明 spot | 写项目未实现 Margin [WIP]；标准合约写 `交易所不支持合约`。 |
| `independentreserve` | Leveraged trading | 当前只声明 spot | 写项目未实现 Leveraged trading/Margin-like product；标准合约写 `交易所不支持合约`。 |
| `bequant` | Margin/Futures/Perpetual | 当前只声明 spot | 写项目未实现 Margin/Futures/Perpetual；补仓位、保证金、订单和 WS。 |
| `biconomy` | Futures/Perpetual 产品页 | 当前只声明 spot | 写项目未实现 Futures/Perpetual；先补官方 futures endpoint spec 核验。 |
| `bitbns` | Margin Trading API | 当前只声明 spot | 写项目未实现 Margin Trading；标准合约写 `交易所不支持合约`。 |
| `blockchaincom` | Perps/第三方接口边界 | 当前只声明 Exchange Spot | 若纳入则写项目未实现并单独设计；不要并入现货 adapter。 |
| `coinmetro` | Margin/Swap/TRAM-like product boundary | 当前只声明 spot | 写项目未实现 Margin/Swap/TRAM-like product；标准合约写 `交易所不支持合约`。 |
| `exmo` | Margin | 当前只声明 spot | 写项目未实现 Margin；标准合约当前写 `交易所不支持合约`，perps 上线后重核。 |
| `fmfwio` | Margin/Futures/Perpetual | 当前只声明 spot | 写项目未实现 Margin/Futures/Perpetual；补仓位、保证金、订单和 WS。 |
| `latoken` | Futures endpoint spec 线索 | 当前只声明 spot | 写项目未实现 Futures/需官方 endpoint spec 核验；不能写交易所不支持合约。 |
| `myokx` | Margin/Swap/Futures/Options/Events | 当前只声明 spot | 写项目未实现或委托主 `okx` adapter；先补区域 scope 和凭证资格核验。 |
| `ndax` | Margin account semantics | 当前只声明 spot | 写项目未实现 Margin account semantics；标准合约写 `交易所不支持合约`。 |
| `okxus` | Perpetual Futures/Event Contracts/Margin boundary | 当前只声明 spot | 写项目未实现并补区域可用性/API 域名/资格核验。 |
| `onetrading` | Futures/Perpetual | 当前只声明 spot | 写项目未实现 Futures/Perpetual；补 futures mapping、仓位和保证金。 |
| `zebpay` | Futures/Perpetual | 当前只声明 spot | 写项目未实现 Futures/Perpetual；不能写交易所不支持合约。 |

## P2 明确交易所不支持

这些已经可以写进单交易所文档，避免后续重复核验：

| adapter | 写法 | 说明 |
| --- | --- | --- |
| `binancecoinm` | `交易所不支持现货` | COIN-M adapter 只对应 Binance COIN-M derivatives API。 |
| `kucoinfutures` | `交易所不支持现货` | KuCoin Futures adapter 只对应 futures API。 |
| `krakenfutures` | `交易所不支持现货` | Kraken Futures adapter 只对应 futures API。 |
| `coinbaseexchange` | `交易所不支持合约` | Coinbase Exchange API 是 spot exchange API。 |
| `binanceus` | `交易所不支持合约` | 本轮官方 API 资料只见 Spot；后续如官方开放合约需重核。 |
| `cod3x` | `交易所不支持现货` | Cod3x 是 AI perps trading terminal；下游 venue 不并入 Cod3x adapter。 |
| `derive_chain_perps` | `交易所不支持现货` | 该 adapter 是 Derive Chain perpetual profile；Derive 平台其他产品另走后续任务。 |
| `equation` | `交易所不支持现货` | Equation V3 官方定位为 perpetual protocol。 |
| `zeta_markets` | `交易所不支持现货`；`交易所不支持当前交易/私有接口/公共 WS runtime` | 官方说明 Zeta Markets 已于 2025-05 停止运营。 |
| `aark` | `交易所不支持现货` | Aark Perpetual Mode 是 Orderly-powered perpetual venue。 |
| `aftermath` | `交易所不支持现货` | 当前 adapter 对应 Aftermath Perpetuals。 |
| `d8x` | `交易所不支持现货` | D8X 是 perpetual futures engine。 |
| `grvt` | `交易所不支持现货` | 当前公开交易主线为 perpetuals；spot 等官方上线后重核。 |
| `hibachi` | `交易所不支持现货` | 官方交易文档是 perpetual contract/orderbook/funding。 |
| `modetrade` | `交易所不支持现货` | Mode Trade 是 Mode Perpetuals DEX / Orderly EVM profile。 |
| `oxfun` | `交易所不支持现货` | OX.FUN 官方主线是 perpetual swaps/options-style scope，未见共享 Spot 订单生命周期。 |
| `pacifica` | `交易所不支持现货` | Pacifica 官方 Contract Specifications 是 perpetual contracts。 |
| `woofipro` | `交易所不支持现货` | 按 WOOFi Pro profile 口径；WOOFi Swap 不是共享 Spot 订单簿。 |
| `bitvavo` | `交易所不支持合约` | 当前官方 API 是 Exchange REST/WS/FIX 和市场数据/机构 API，未见标准合约交易接口。 |
| `cex` | `交易所不支持合约` | 当前 CEX.IO 公开交易文档是 Spot Trading REST/WS。 |
| `bitbank` | `交易所不支持合约` | 当前官方 REST API 有 Spot/Margin 相关路径，未见标准 futures/perpetual/options；Margin 单独写 `项目未实现`。 |
| `bitso` | `交易所不支持合约` | 当前官方 Trading API 有 spot/OTC/RFQ/conversions 和 Margin Trading [WIP]，未见标准合约。 |
| `bithumb` | `交易所不支持合约` | 当前官方 Open API 未见标准 futures/perpetual/options。 |
| `bitopro` | `交易所不支持合约` | 当前官方 Open API 覆盖现货交易对和钱包，未见标准合约。 |
| `btcmarkets` | `交易所不支持合约` | 当前官方 API v3 未见标准合约。 |
| `coincheck` | `交易所不支持合约` | 当前官方 Exchange API 未见标准合约。 |
| `coinone` | `交易所不支持合约` | 当前官方 API v2/v2.1 未见标准合约。 |
| `independentreserve` | `交易所不支持合约` | 当前官方资料有 leveraged trading，未见标准 futures/perpetual/options 统一 API；leveraged trading 单独写 `项目未实现`。 |
| `luno` | `交易所不支持合约` | 当前官方 API 未见 futures/margin。 |
| `upbit` | `交易所不支持合约` | 当前官方 API reference 未见标准 futures/perpetual/options。 |
| `alpaca` | `交易所不支持合约` | 按 crypto adapter 口径；Alpaca 证券 Options 不纳入本 adapter。 |
| `bit2c` | `交易所不支持合约` | 当前官方 API 只见 NIS 现货 ticker/orderbook/trades/order/cancel。 |
| `bitbns` | `交易所不支持合约` | 标准 futures/perpetual/options 未见稳定官方 API；Margin Trading 单独写 `项目未实现`。 |
| `bitteam` | `交易所不支持合约` | 当前官方资料覆盖 Spot/P2P 和 CCXT-style spot endpoints。 |
| `bittrade` | `交易所不支持合约` | 官方 API 介绍明确当前不支持杠杆交易，账户示例为 spot。 |
| `blockchaincom` | `交易所不支持合约` | 按 Exchange API 口径；Perps/第三方接口单独写项目未实现边界。 |
| `btcbox` | `交易所不支持合约` | 当前官方 API 只见 JPY 现货 ticker/depth/orders/trade endpoints。 |
| `btcturk` | `交易所不支持合约` | 当前官方 API 是现货 market data/trading/account management。 |
| `coinmate` | `交易所不支持合约` | 当前官方 API 覆盖 EUR/CZK 现货交易和 WS。 |
| `coinmetro` | `交易所不支持合约` | 当前官方资料未见标准 futures/perpetual/options；Margin/Swap 单独写项目未实现。 |
| `coinsph` | `交易所不支持合约` | 当前官方 API/帮助中心明确 Spot Trade API，未见标准合约。 |
| `coinspot` | `交易所不支持合约` | 当前官方 API/V2 API 覆盖 AUD 现货市场、余额、订单和历史。 |
| `cryptomus` | `交易所不支持合约` | Exchange 文档覆盖现货 orderbook/orders/WS；payment/conversion 不并入合约。 |
| `exmo` | `交易所不支持合约` | 当前标准 futures/perpetual/options 未上线；Margin 单独写 `项目未实现`，perps 上线后重核。 |
| `foxbit` | `交易所不支持合约` | Foxbit Exchange API 是 spot；Rispar 抵押借贷不是标准合约仓位。 |
| `hollaex` | `交易所不支持合约` | 按官方 demo/API profile 写；任意 HollaEx 白标 venue 需单独核验。 |
| `indodax` | `交易所不支持合约` | 当前官方 public/private API 覆盖 IDR 现货/TAPI，未见标准合约。 |
| `mercado` | `交易所不支持合约` | Mercado Bitcoin `positions` 语义是 open orders，不是合约仓位。 |
| `ndax` | `交易所不支持合约` | 当前 instrument type 为 standard，未见标准 futures/perpetual/options；margin account semantics 单独写项目未实现。 |
| `novadax` | `交易所不支持合约` | 当前官方 API 覆盖 symbols、market data、orders、account、wallet，未见标准合约。 |
| `p2b` | `交易所不支持合约` | 当前官方 API/Swagger 覆盖 spot/P2P；P2P 不并入中心化订单簿。 |
| `paymium` | `交易所不支持合约` | 当前官方 API 是 BTC/EUR spot public/private REST 与 socket.io。 |
| `tokocrypto` | `交易所不支持合约` | Tokocrypto spot/Binance-family surface；`marginTradingEnable=0`。 |
| `wavesexchange` | `交易所不支持合约` | WX matcher 是 Waves spot asset pair matcher，没有标准合约仓位/资金费/mark price。 |
| `yobit` | `交易所不支持合约` | 当前 API v3/TAPI 覆盖 spot/Trade API/Defi swap/Yobicode，未见标准合约。 |
| `zaif` | `交易所不支持合约` | 信用取引当前停止中；标准合约当前不可接入，恢复后重核。 |

## 验收标准

每个补全任务完成时，至少满足：

1. 单交易所文档写清官方产品线结论，未支持项使用 `交易所不支持`，项目缺口使用 `项目未实现`。
2. `endpoint_mapping.yaml` 包含对应 REST/WS operation、support、product、market_types、auth、rate_limit_bucket。
3. WebSocket 行情包含订阅方式、推流间隔、档位、sequence/checksum、snapshot 重建。
4. 至少补一个成功 parser fixture；写接口还要补 request spec 和 signing vector。
5. 重新运行 `python3 scripts/generate_exchange_gateway_matrix.py`，确认矩阵状态变化符合预期。
6. 定向运行对应 adapter 测试；公共逻辑变化再跑 `cargo check -p rustcta-exchange-gateway --lib`。
