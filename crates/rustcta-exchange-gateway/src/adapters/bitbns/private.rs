// Bitbns private REST is intentionally not wired to ExchangeClient operations.
// The official SDK documents authenticated headers, but this adapter stays
// scan-only until private order semantics and regional account constraints are
// separately reviewed with read-only credentials.
